# MEGA: Moving-base End-effector Guidance and Agility for Aerial Manipulation

> 方向：D07 Isaac RL 机械臂控制 | 目标会议：ICRA/IROS 2027 | 状态：🔴 草稿
> 最后更新：2026-06-01

---

## Abstract

[TODO: 150-250词]

---

## 1. Introduction

### 1.1 Problem Statement

Current aerial manipulation research is usually trapped between two unsatisfying routes. The first route pursues **whole-body unified control**, asking one large controller to absorb flight stabilization, arm motion, contact preparation, and post-contact recovery all at once; this is expressive but hard to train, difficult to verify, and often expensive to deploy. The second route follows **traditional decoupled control**, where the UAV is stabilized first and the arm is treated as a downstream manipulator; this is easier to engineer, but its precision degrades rapidly once base drift, delay, or outdoor localization error enters the loop.

This paper argues for a third route: **the UAV only needs to remain stable within a bounded envelope, while a high-frequency agile arm actively compensates the lower-frequency drift of the moving base**. The key physical claim is not that the aerial platform becomes perfectly still, but that the arm can move fast enough, and with sufficiently guidance-aware control, to preserve end-effector accuracy even when the base continues to oscillate or drift. Under this view, the main deployment bottleneck is no longer full-body optimality, but whether the manipulator can absorb base-motion error quickly enough to maintain precise guidance, contact establishment, and post-contact stability without relying on motion-capture-grade external localization.

This framing is especially relevant for outdoor aerial manipulation. In realistic deployment, centimeter-level base pose certainty is expensive, and whole-body control policies may overfit to simulator-specific couplings or require brittle system identification. By contrast, a fast compensatory arm route only assumes that the UAV controller can keep the platform inside a manageable disturbance band; the remaining precision burden is shifted to end-effector guidance. Recent owner-side validation further supports this direction: a 5-axis arm plus UAV yaw can already form an effective 6-DoF manipulation system, and MuJoCo experiments suggest that maintaining a front-facing horizontal gripper while co-solving yaw is a viable operating geometry. The core scientific question of D07 therefore becomes: **can a high-frequency arm preserve guidance quality across increasing base-drift amplitudes, so that end-effector error remains bounded even when UAV drift does not vanish?**

### 1.2 Contributions

1. **C1: Third-Route Problem Formulation for Aerial Manipulation**: We formalize aerial manipulation as **high-frequency arm compensation over low-frequency UAV drift**, and position it explicitly against whole-body unified control and traditional decoupled control.
2. **C2: MEGA Control Stack for Moving-Base End-Effector Guidance**: We design a guidance-centered learning and control stack in Isaac Lab where the manipulator is optimized as the primary fast compensator, while the UAV is only required to stay within a bounded stability envelope.
3. **C3: Drift-Amplitude-Decoupling Evaluation Protocol**: We introduce a deployment-facing evaluation protocol centered on whether end-effector guidance error remains stable as base-drift amplitude increases, together with contact-establishment, post-contact stabilization, and payload-bearing guidance metrics.

---

## 2. Related Work

### 2.1 Isaac Lab RL for Manipulation

Isaac Lab has emerged as the de facto GPU-native successor to Isaac Gym for large-scale robot learning, providing modular environments, batched simulation, and a cleaner bridge between reinforcement learning, imitation learning, and sim-to-real deployment [REF: 2511.04831]. For D07, its key value is not merely faster training throughput, but the ability to standardize environment definition, disturbance injection, reset logic, and deployment-facing evaluation under one reproducible stack. This makes Isaac Lab a suitable substrate for aerial-arm control, where controller comparison is otherwise confounded by inconsistent simulators and reward plumbing.

Beyond pure infrastructure, recent Isaac-Lab-centered works also show that training workflow quality matters as much as policy architecture. AGILE [REF: 2603.20147] emphasizes deterministic pre-training validation, scenario-based testing, and unified evaluation pipelines, while WHOLE-MoMa [REF: 2604.12509] shows that control priors and offline RL can be staged rather than learned end-to-end from scratch. These works collectively suggest that D07 should treat Isaac Lab not as a benchmark wrapper, but as the backbone for a staged research protocol: environment verification first, then warm-started policy learning, and only then deployment-oriented stress testing.

A more recent signal comes from GPU-Parallel Multi-Task Reinforcement Learning with Demonstration Guided Policy Optimization [REF: 2606.03335], which instantiates MT-Libero in Isaac Lab and shows that demonstration-guided on-policy optimization can make heterogeneous manipulation-task families trainable under a shared GPU-parallel RL shell. For D07, the paper is valuable not because multi-task PPO automatically solves moving-base guidance, but because it sharpens a missing middle layer between single-task warm starts and full deployment claims: **a demonstration-guided multi-task curriculum can act as a stronger optimization and task-coverage shell**. Under the MEGA title, however, this family should still default to support-side interpretation unless its gains survive as flatter `drift_amp \mapsto BDEE` curves and better `CET/PCST/PGFR` under coupled disturbance.

The newly surfaced NVIDIA Isaac Sim Survey [REF: 2606.03551] plays a different role. Its main value for D07 is not method-side evidence, but infrastructure-side synthesis: it consolidates how Isaac Sim supports sensor modeling, physics fidelity, synthetic-data generation, and large-scale robotics experimentation across manipulation and embodied-AI settings. In our framing, this survey should therefore be treated as **platform-comparison and infrastructure support**. It can justify why D07 anchors its experiments in the Isaac Sim / Isaac Lab ecosystem, but it should not be over-read as evidence that moving-base end-effector guidance has itself become stronger.

### 2.2 Aerial Manipulation Control

Aerial manipulation sits in a uniquely difficult regime because the arm controller cannot be analyzed independently from the UAV base: arm motion perturbs base attitude, while flight disturbances degrade end-effector precision. Aerial Manipulator RL [REF: 2512.21085] provides a strong hybrid baseline by combining an RL outer loop with INDI/PID inner-loop stabilization, achieving centimeter-level end-effector precision under payload perturbation. This result is important because it reframes hybrid inner loops as a first-class method family rather than an engineering patch.

Recent comparative evidence further warns against evaluating only within the RL family. A Comparison of Reinforcement Learning and Optimal Control for Aerial Manipulators [REF: 2604.12628] directly places RL and pseudo-spectral optimal control in the same problem setting, showing that the real design question is not “which RL algorithm wins,” but which control interfaces should remain learned, which should remain optimized, and where hybridization provides the best stability-to-complexity trade-off. For D07, this motivates a three-way baseline structure: pure RL, fixed-flight or manipulator-only local compensation, and deployable hybrid control.

### 2.3 Sim-to-Real Transfer for Fast Manipulation

Sim-to-real transfer for manipulation has recently shifted from naive domain randomization toward more structured bridges spanning perception, reward, and adaptation. RL-Based Sim-Real Co-Training for VLA [REF: 2602.12628] shows that simulation RL can be paired with real-data anchoring instead of replaced by it, suggesting that sim and real should be treated as a co-training loop rather than two isolated stages. Squint [REF: 2602.21203] adds another important signal: for low-cost arms and limited compute, the main win may come from sample-efficient visual RL and rapid adaptation, not maximal simulator realism.

Perception-side transfer is equally critical. ViserDex [REF: 2604.11138] demonstrates that 3D Gaussian Splatting can serve as a structured visual bridge for robust sim-to-real manipulation, outperforming ordinary rendered data in downstream policy transfer. Although developed for dexterous in-hand reorientation, its deeper message for D07 is that perception gap reduction can become an independent source of transfer gain, and therefore should not be conflated with control-policy improvement. In aerial-arm settings, this implies that visual bridge modules, warm-start policies, and hybrid controllers must be evaluated as separable contributors.

Recent Isaac-Lab multi-task evidence also suggests a subtler transfer route: a shared GPU-parallel benchmark and demonstration-guided curriculum may reduce adaptation cost before one even changes the disturbance interface itself. MT-Libero + DGPO [REF: 2606.03335] is especially relevant here because it shows that heterogeneous manipulation suites can be organized under one on-policy shell with demonstration bias while preserving online improvement. For D07, the honest reading is still conservative: such a curriculum may strengthen initialization quality, task-family coverage, and transfer readiness, but it should default to a **support-side curriculum shell** unless it measurably improves moving-base guidance outcomes rather than only reducing adaptation burden.

Find the Fruit [REF: 2505.16547] further strengthens this separation by showing that zero-shot sim-to-real success can emerge from a carefully staged manipulation stack—high-level interaction planning, low-level compliant control, and plant-property randomization—without yet proving a stronger controller interface. Its main lesson for D07 is that structured sim-to-real robustness may come from exposing the policy to a broader uncertainty set and decomposition-aware execution protocol, rather than from a better late-window disturbance interface per se. This is especially relevant for aerial-arm tasks involving occlusion, contact preparation, or obstacle clearing, where a row may look deployment-ready simply because the environment decomposition is stronger.

### 2.4 Geometry-Aware Structure Priors

Not all improvements in manipulation come from larger policies or richer rewards; some arise from better structural priors over robot-object interaction. RoTri-Diff [REF: 2603.07165] explicitly models triadic robot-object spatial relations and shows that geometric interaction constraints can reduce collision and instability in coordinated manipulation. For D07, the direct takeaway is not to adopt diffusion imitation learning wholesale, but to recognize that relation-aware pose structure may be the right abstraction for local disturbance compensation around grasping and contact.

This observation is especially relevant because aerial-arm control often fails in the final approach and contact phases, where relative pose consistency matters more than long-horizon planning capacity. Thus, geometry-aware priors should be treated as a local compensation layer that complements, rather than replaces, RL controllers. In our framing, they help explain when D07 should be written as relation-aware fast manipulation under coupled disturbance, instead of an overclaimed whole-body autonomy story.

### 2.5 Shared Autonomy and Human-Guided Adaptation

A growing body of manipulation work suggests that deployment-oriented control should not be framed purely as autonomous policy optimization, but also as a question of how human guidance is represented, denoised, and reused. Real-to-Sim-to-Real shared autonomy [REF: 2603.17016] shows that a small amount of real teleoperation can be converted into a human surrogate and residual copilot policy, indicating that human guidance can serve as a structured adaptation prior rather than only an evaluation tool. DiSCo [REF: 2603.22787] extends this logic from single-step correction to sequence-level action refinement through diffusion-based inpainting, suggesting that assistance can be applied over temporally coherent action chunks instead of per-step overrides.

For D07, the main implication is not that shared autonomy should replace reinforcement learning, but that it provides a plausible interface between noisy teleoperation, demonstration collection, and warm-started control. In aerial-arm settings, this matters because human operators may express goal intent correctly while failing to maintain stable high-frequency compensation under delay and coupled disturbance. A sequence-level copilot can therefore be interpreted as a data-quality and adaptation layer that improves demonstration usefulness before or during RL fine-tuning, rather than as the final controller itself.

Hand-in-the-Loop [REF: 2605.15157] sharpens this boundary by showing that intervention quality itself is a first-order variable in dexterous manipulation: seamless blending between autonomous actions and human correction can drastically reduce takeover jitter and generate cleaner corrective data for later policy refinement. For D07, this means human-in-the-loop benefit should be decomposed into at least two parts—**intervention-hygiene gain** and **controller-interface gain**. If a method improves mainly because takeover trajectories become smoother and more learnable, the honest narrative is still data shaping or bounded assistance support rather than a stronger autonomous aerial-arm interface.

### 2.6 Semantic State and Knowledge-Guided Manipulation

Most Isaac-Lab manipulation pipelines still rely on RGB, proprioception, and hand-crafted task variables, leaving object-object and robot-object relations implicit inside the policy backbone. KG-M3PO [REF: 2603.24083] argues for a different route: online 3D scene-graph maintenance, relation-aware updates, and knowledge-guided querying inside the reinforcement-learning loop. Its value for D07 is not that scene graphs should replace control, but that semantic relations can become a compact intermediate state for long-horizon or partially occluded manipulation settings.

For aerial-arm control, this matters in two ways. First, relation-centric state can help separate base drift from task-relevant geometry, e.g., `end-effector ↔ target ↔ obstacle/container` relations, instead of forcing the policy to infer everything from raw pixels. Second, such structure offers a more natural bridge to disturbance-aware local compensation: the controller can reason over whether failure comes from pose misalignment, occlusion, or contact-stage instability. We therefore treat semantic relation state as a candidate support layer for relation-aware local compensation, not as a standalone autonomy claim.

### 2.8 Zero-Shot Sim2Real Structure and Environment-Decomposition Routes

Find the Fruit [REF: 2505.16547] adds an important D07 anchor that sits between perception bridge and deployment narrative. Its zero-shot sim-to-real plant manipulation results come not from a stronger late-window disturbance interface alone, but from a staged decomposition of the manipulation problem into interaction planning, compliant low-level control, and broad randomization over plant geometry, occlusion, and lighting. For D07, this means a row can look surprisingly deployment-ready simply because the environment decomposition is cleaner and the uncertainty set is broader, even when the moving-base disturbance interface itself has not fundamentally improved.

We therefore treat Find-the-Fruit-style results as an **environment-decomposition support family**. Gains from this family are allowed to explain stronger occlusion robustness, interaction-stage preparation quality, and zero-shot uncertainty exposure, but they are not by themselves sufficient evidence for moving-base end-effector guidance, post-contact disturbance survival, or deployable hybrid aerial-arm control. Any such gain must still be routed through the same W1/W2/W3 and disturbance-source audits as the rest of D07.

### 2.9 Sim-Real Co-Training is a transfer protocol, not yet a guidance-bearing interface

RL-Based Sim-Real Co-Training for VLA [REF: 2602.12628] is highly relevant to D07 because it turns Isaac-Lab-based pretraining, domain randomization, and downstream real-world adaptation into a single **sim-real co-training protocol** rather than three loosely connected steps. Its strongest message for our setting is methodological: one can use simulation to cheaply learn high-level intent-conditioned control structure while reserving scarce real interaction for the final adaptation hop. This is directly useful for aerial-arm research where collecting disturbance-rich real data is expensive and potentially unsafe.

However, this route should still be interpreted conservatively under the MEGA title. A co-training protocol can improve representation alignment, optimization stability, and final transfer readiness without yet proving that the controller has become better at **moving-base end-effector guidance** itself. In our reading, sim-real co-training is therefore a **transfer-protocol support family**: it is allowed to explain lower adaptation cost, smaller transfer gap, and stronger initialization for later disturbance training, but it cannot by itself support claims about lower base-drift end-effector error, faster disturbed contact establishment, or better post-contact guidance preservation unless those gains are explicitly observed in the guidance tuple.

### 2.9 Guidance-Bearing versus Guidance-Adjacent Families under the MEGA Title

After switching the paper title to **MEGA: Moving-base End-effector Guidance and Agility for Aerial Manipulation**, D07 needs a stricter related-work reading rule: not every aerial-manipulation gain is allowed to speak in the title's language. We therefore separate prior work into **guidance-bearing** and **guidance-adjacent** families. Guidance-bearing families are those whose gains can, in principle, directly improve moving-base end-effector guidance quality under disturbance, i.e., they may reduce base-drift end-effector error, shorten contact-establishment time, stabilize post-contact motion, or lower payload-bearing guidance failure. In our current anchor set, Reactive Dexterous Grasping [REF: 2605.03363] and DSAM-style hybrid stabilization [REF: 2512.21085] are the clearest guidance-bearing routes, because they explicitly constrain how a disturbed manipulator reaches, contacts, and remains stable around the target.

By contrast, many otherwise valuable routes remain only guidance-adjacent by default. Q2RL-style BC-to-Q gating can preserve warm-start competence and reduce forgetting, but unless it materially improves disturbed end-effector guidance, it remains an **anti-forgetting bridge**. DiSCo-style sequence copilots can smooth demonstrations or bounded online assistance, but unless they improve disturbed end-effector guidance, they remain a **data-shaping or bounded-assistance support route**. KG-M3PO-style semantic relation state can disambiguate target alignment and obstacle relations, but unless it improves guidance metrics under moving-base disturbance, it remains a **state-support route**. Find-the-Fruit-style decomposition can improve uncertainty exposure and occlusion handling, but unless it improves guidance metrics under payload-bearing disturbance, it remains an **environment-decomposition support route**. This distinction is crucial because D07 should not let support-side gains silently occupy a title that now explicitly promises moving-base end-effector guidance.

### 2.10 Limitations of Existing Work

Despite rapid progress, four gaps remain unresolved. First, most existing manipulation papers do not explicitly separate training acceleration, perception-bridge gain, shared-autonomy data shaping, semantic-state gain, and deployable control gain, making it hard to tell whether success comes from better policy learning or simply cleaner transfer and supervision conditions. Second, aerial manipulation remains underexplored compared with tabletop or dexterous-hand settings, so the role of coupled base-arm disturbance is still weakly characterized in sim-to-real studies. Third, current literature provides many ingredients—warm starts, geometry priors, semantic relation state, hybrid inner loops, perception bridges, shared-autonomy copilot layers—but lacks a decision protocol for determining when each ingredient deserves to occupy the main narrative of a deployable aerial-arm paper. Fourth, there is still no clear protocol for deciding when teleoperation assistance should be used only for data collection, when it should remain as an online copilot, and when it should be distilled away into a fully autonomous controller.

A fifth gap, sharpened by the recent Isaac Lab platform paper [REF: 2511.04831], is that infrastructure quality is usually treated as neutral background rather than an experimental variable with claim-level consequences. Isaac Lab unifies batched GPU physics, sensor simulation, actuator models, data pipelines, and multi-platform training under one modular stack, but existing aerial-arm drafts rarely specify whether an apparent control gain depends on this stronger infrastructure or would survive under weaker validation hygiene. For D07, this means the field still lacks a clean way to separate **platform-enabled reproducibility gain** from **controller-interface gain**. Without that separation, better resets, disturbance injection, and evaluation tooling can be accidentally over-credited as method innovation.

A sixth gap is that recent positive results in reward modeling and human correction quality still do not cleanly answer the controller question. Robo-Dopamine [REF: 2512.23703] shows that process reward modeling can dramatically improve online manipulation learning with minimal task adaptation, while Hand-in-the-Loop [REF: 2605.15157] shows that seamless corrective blending can sharply improve intervention quality and downstream refinement. Both are highly relevant to D07, but they also expose a methodological risk: a row may improve because **reward credit becomes denser** or because **human correction trajectories become smoother**, even when the underlying aerial-arm control interface has not fundamentally advanced. Existing work rarely forces reward-origin gain, intervention-hygiene gain, and late-window disturbance-interface gain into the same accounting frame.

A seventh gap, made clearer by the recent local D07 anchor set, is that current routes still do not explicitly separate **moving-base guidance-bearing gains** from **guidance-adjacent support gains**. Q2RL-style BC-to-Q gating [REF: 2605.05172] may preserve competence and reduce forgetting, Reactive Dexterous Grasping [REF: 2605.03363] may improve safe projection and local recovery, and DiSCo [REF: 2603.22787] may clean intervention trajectories or stabilize bounded assistance; however, these gains do not automatically imply better moving-base end-effector guidance. Existing work still lacks a protocol for asking whether a route first improves **base-drift end-effector error**, **contact-establishment time**, **post-contact stabilization**, or **payload-bearing guidance survival**, instead of only improving optimization, safety filtering, or demonstration hygiene. Under the MEGA title, this missing distinction becomes a first-order limitation rather than a wording detail.

## 3. Method

### 3.1 Overview

We study aerial manipulation through the lens of a **frequency-separated compensatory interface** rather than as a monolithic whole-body controller. The core hypothesis is simple: if the UAV controller can keep the platform inside a bounded low-frequency disturbance envelope, then a sufficiently agile manipulator can act as the primary precision layer and actively cancel the residual base-motion error at the end effector. Under this framing, the arm is not merely a downstream tool attached to a stabilized drone; it becomes the main high-frequency guidance module that protects contact quality and post-contact stability against residual drift.

This leads to a three-route interpretation of the problem. **Route A** is whole-body unified control, where one integrated policy or optimization stack absorbs both base and arm dynamics. **Route B** is traditional decoupled control, where the UAV first stabilizes itself and the arm then executes manipulation on top of whatever pose quality remains. **Route C**, which is our target route, assumes only bounded base stability and asks whether the arm can compensate the remaining drift fast enough to preserve moving-base end-effector guidance. D07 is therefore not trying to prove that full-body learning is unnecessary in every setting; instead, it asks whether a lighter and more deployable interface can win once the main KPI is **bounded end-effector error under increasing base-drift amplitude**.

Accordingly, our method is organized around four evidence layers: **L1 warm-started RL training**, **L2 relation-aware local compensation**, **L3 perception-bridge sim-to-real transfer**, and **L4 deployable hybrid stabilization**. These layers are retained not as equally title-worthy components, but as candidate explanations for why the compensatory route may work. Warm start may improve optimization speed, relation-aware structure may sharpen near-contact correction, perception bridges may reduce visual transfer loss, and hybrid stabilization may keep the compensation loop controller-consumable. Only the residual gain that survives after these support explanations are subtracted is allowed to support the stronger claim that the arm itself functions as a high-frequency stabilizer over low-frequency base drift.

### 3.2 Layered Control Formulation

At the training layer, we initialize a policy in Isaac Lab using task-specific demonstrations, behavior-cloning warm starts, or real-data anchored pretraining when available. This layer is designed to answer a narrow question: does warm-starting reduce optimization cost and improve early learning stability relative to scratch RL? Any benefit observed here is counted only as **sample-efficiency gain**, not as deployable control evidence.

At the execution layer, we introduce a relation-aware local compensation module operating on end-effector pose, object-relative pose, and short-horizon disturbance state. Inspired by triadic interaction modeling in RoTri-Diff [REF: 2603.07165], this module emphasizes near-contact geometric consistency rather than full-scene autonomy. It is therefore evaluated as a local manipulation stabilizer under UAV-induced perturbation, especially in the final approach, grasp, and lift phases.

At the transfer layer, we allow structured visual bridging between simulation and real deployment using perception-side adaptation modules, including 3DGS-style rendered randomization motivated by ViserDex [REF: 2604.11138]. This layer isolates how much sim-to-real gain can be obtained by reducing visual distribution mismatch before changing the low-level controller. Finally, at the deployment layer, we combine the learned outer-loop controller with a stabilizing inner loop, following the logic of RL-plus-classical-control hybridization in aerial manipulation [REF: 2512.21085]. Only this final layer is allowed to support claims about deployable hybrid aerial-arm control.

A key addition motivated by recent onboard aerial-manipulation evidence is that the compensatory arm route should not be written as a pure control abstraction detached from state estimation. Contact-aware onboard perception [REF: 2602.08251] shows that once contact or near-contact interaction begins, the dominant failure mode is often not just insufficient control authority, but the coupled degradation of state estimation, contact consistency, and local force-motion regulation. For D07, this means the manipulator cannot be treated as a high-frequency stabilizer in isolation: the stabilizer must be fed by a **base-motion-aware, contact-aware local guidance state** that remains usable without motion-capture-grade global localization.

We therefore refine the MEGA stack into three tightly coupled inner interfaces around the arm-side compensator: **(i) bounded base stabilization**, where the UAV controller only guarantees that platform drift remains inside a controllable envelope; **(ii) arm-side guidance compensation**, where the manipulator executes high-frequency residual correction against that envelope; and **(iii) onboard contact-aware perception**, where visual, proprioceptive, and contact-consistency signals maintain an actionable local target state during approach, touch, and post-contact stabilization. Under this formulation, a negative result can be interpreted more honestly: failure may come from insufficient arm agility, insufficient local state quality, or excessive arm-induced back-reaction on the UAV, rather than from an abstract “RL controller did not work.”

### 3.3 Narrative Decision Protocol

We define a narrative decision protocol to prevent overclaiming from partial gains. Let **SEG** denote sample-efficiency gain from warm start, **PBG** denote perception-bridge gain under sim-to-real transfer, **RLG** denote relation-aware local compensation gain near contact, and **DHG** denote deployable hybrid gain under disturbance and deployment-oriented evaluation. Then the paper narrative is selected by the dominant stable gain category rather than by absolute best-case success rate.

If only `SEG` improves while disturbance robustness and deployment coverage remain weak, the paper must be framed as a **data-gated acceleration recipe** rather than a new deployable controller. If `RLG` and/or `PBG` dominate, while full hybrid robustness is not yet established, the paper should be framed as **relation-aware local compensation with a perception bridge**. Only when `DHG` remains positive under coupled-disturbance stress tests, risk coverage checks, and maintenance-cost constraints may the method be presented as a **deployable hybrid control pipeline**. This protocol is crucial for D07 because it keeps warm-start gains, structured perception transfer, and true deployment readiness from collapsing into a single inflated claim.

### 3.4 Minimal System Instantiation for the First Round

For the first experimental round, we instantiate the four-layer view with one concrete design choice per layer so that the draft can immediately map onto an executable research plan. **L1 warm-started RL** uses Isaac-Lab batched simulation together with demonstration or behavior-cloning initialization, motivated by the sample-efficiency logic behind Squint [REF: 2602.21203]. **L2 relation-aware local compensation** augments the policy input with short-horizon end-effector pose, object-relative pose, and contact-stage indicators, following the geometric-prior intuition of RoTri-Diff [REF: 2603.07165]. **L3 perception bridge** adds structured visual randomization and deployment-facing visual adaptation, with 3DGS-style bridge modules treated as an optional but separately measurable source of transfer gain following ViserDex [REF: 2604.11138]. **L4 deployable hybrid stabilization** combines the learned outer-loop policy with a disturbance-rejection inner loop for flight-base attitude and high-frequency arm stabilization.

This minimal instantiation is intentionally asymmetric: lower layers are allowed to improve optimization speed, local contact quality, or transfer robustness, but they are not automatically promoted into a deployable-control claim. In particular, the first-round system is designed so that every added module maps to exactly one dominant gain family by default—warm start to `SEG`, perception bridge to `PBG`, relation-aware local structure to `RLG`, and hybrid stabilization to `DHG`. This explicit mapping is necessary because aerial manipulation papers often bundle all useful ingredients into one large stack before it is clear which ingredient actually explains the main stable gain.

### 3.5 Layer-Specific Hypotheses

We formulate three first-round hypotheses that can be falsified independently. **H1 (Warm-Start Efficiency)**: behavior-cloning or demonstration initialization reduces time-to-threshold and stabilizes early training, but may not substantially change disturbance robustness after convergence. **H2 (Relation-Aware Contact Robustness)**: adding local robot-object geometric structure mainly improves final approach, grasp onset, and lift stability under D2/D3 disturbance, even if nominal D0/D1 success changes little. **H3 (Bridge-to-Deployment Promotion)**: only when perception bridging and hybrid stabilization together preserve gains under disturbance-stratified and deployment-oriented evaluation should the paper be promoted from a local-compensation or transfer story to a deployable hybrid-control story.

These hypotheses are useful not because they guarantee a positive result, but because they determine how negative results should be interpreted. If H1 is positive while H2 and H3 remain weak, the draft should narrow toward acceleration rather than deployment. If H2 is positive without H3, the correct narrative is relation-aware local compensation under coupled disturbance. If H3 is also positive, then the paper earns a deployment-facing title and abstract. Writing these hypotheses directly into the method section keeps the paper aligned with evidence instead of retrospective ambition.

### 3.6 Training Regimes and Interface Variants

The first experimental round instantiates D07 with a deliberately small but decision-relevant training matrix. At the optimization level, we compare **scratch PPO**, **warm-start PPO**, and a **real-data-anchored co-training variant** motivated by RL-Based Sim-Real Co-Training for VLA [REF: 2602.12628]. The goal is not to optimize for raw leaderboard score, but to answer whether early policy shaping through demonstrations or limited real anchoring changes only convergence speed or also improves downstream disturbance robustness and transfer stability. In this framing, any improvement that disappears after matched-compute evaluation remains a `SEG`-only gain.

At the control-interface level, we explicitly separate three families that are often blurred together in aerial-arm papers: **pure learned control**, **fixed-flight / manipulator-only local compensation**, and **hybrid outer-policy + inner-loop stabilization**. This split is directly motivated by the existing local D07 evidence base: DSAM-like results suggest that inner-loop stabilization can be a real method family rather than an engineering patch, while manipulator-only planning under a fixed flight envelope remains a strong intermediate baseline for testing whether the current bottleneck is truly whole-body coupling. In practice, this means D07 should not ask "which policy wins" before asking "which interface layer deserves to learn at all."

We additionally treat **Isaac-Lab-native infrastructure discipline** as an explicit protocol axis rather than invisible background. Following Isaac Lab [REF: 2511.04831], every training regime inherits the same reset semantics, disturbance injection API, logging schema, and multi-platform environment definition so that differences across B1-B4 cannot be attributed to ad hoc simulator glue. This matters for D07 because aerial-arm claims are especially sensitive to hidden platform variance: if a row only looks stronger because its disturbance slicer, actuator model, or replay logging is cleaner, then the result should be interpreted as infrastructure-enabled reproducibility rather than controller-interface superiority. By writing this protocol lock into the method section, the paper commits to comparing learning and control routes on a common validated substrate.

We also preserve one additional axis for later extension: **shared-autonomy-assisted warm start**, where teleoperation is filtered or refined by a sequence-level copilot before being used as demonstration data or online corrective prior. Motivated by DiSCo [REF: 2603.22787] and Real-to-Sim-to-Real shared autonomy [REF: 2603.17016], this axis is not yet expanded into a full large-grid comparison in the first round; instead, it is treated as a structured fallback if naive demonstrations prove too noisy for stable warm-starting under aerial disturbance. A second optional extension is a **demonstration-guided multi-task curriculum shell** inspired by MT-Libero + DGPO [REF: 2606.03335], whose main role is to test whether task-family breadth and optimization stability can be improved before any stronger moving-base guidance claim is made. By writing the training regimes and interface variants explicitly into the method section, the paper commits in advance to an evidence-first decomposition of gain sources rather than a post hoc fusion narrative.

### 3.7 Human-Guided Data Shaping as an Optional Adaptation Layer

We introduce an optional human-guided data-shaping layer that sits before or alongside RL fine-tuning. The purpose of this layer is not to claim that the final aerial-arm system requires a human copilot online, but to test whether sequence-level refinement of teleoperated actions yields better initialization and safer adaptation under disturbance. Concretely, raw human action sequences can be passed through a sequence copilot that preserves high-level intent while denoising unstable local corrections, in the spirit of diffusion-based action inpainting [REF: 2603.22787]. The refined sequences are then used either as behavior-cloning warm-start data or as a bounded online correction prior during early training.

This layer is evaluated under a narrow interpretation. If it mainly reduces demonstration noise and speeds up optimization, the gain is recorded under `SEG` or a teleoperation-data-quality auxiliary metric rather than being promoted to a controller contribution. If it materially improves transfer robustness or disturbance recovery only when kept online, then the correct narrative shifts toward shared-autonomy-assisted deployment rather than fully autonomous control. In other words, D07 treats human-guided data shaping as a diagnostic interface for deciding whether teleoperation should remain part of the deployed system, not as an automatic source of inflated autonomy claims.

### 3.8 Semantic Relation State as a Support Layer for Local Compensation

We further introduce an optional semantic relation state layer that augments the controller with compact, disturbance-aware relational structure. Inspired by KG-M3PO [REF: 2603.24083], this layer maintains task-relevant relations such as `end-effector ↔ target`, `target ↔ container`, and `arm ↔ obstacle` rather than requiring the policy to infer every failure mode from pixels and proprioception alone. The purpose is not to turn D07 into a full scene-graph reasoning paper, but to test whether relation-centric state helps local compensation identify which geometric inconsistency is causing failure under base drift and partial occlusion.

This support layer is especially relevant for aerial-arm operation because the same raw image perturbation may correspond to very different control errors: target misalignment, base-induced viewpoint shift, or imminent obstacle conflict. By exposing these relations explicitly, the policy may allocate capacity more efficiently to disturbance compensation near contact. However, any gain from this layer is conservatively routed to `RLG` unless it survives deployment-oriented evaluation together with the rest of the stack.

### 3.9 Disturbance-Bounded Shared-Autonomy Interface

We further formalize a disturbance-bounded shared-autonomy interface motivated by recent local evidence that sequence-level human guidance can act as a data-shaping or bounded online correction layer rather than an always-on teleoperation fallback. Building on DiSCo [REF: 2603.22787] and Real-to-Sim-to-Real shared autonomy [REF: 2603.17016], we assume that human intent is often globally correct but locally noisy under aerial delay, base drift, and contact transients. Instead of interpreting human intervention as a binary switch between autonomy and teleoperation, we model it as a constrained interface that may operate in three modes: **offline demonstration refinement**, **early-training correction prior**, and **deployment-time bounded copilot assistance**.

This formulation matters for D07 because noisy aerial teleoperation can pollute behavior-cloning warm starts and obscure whether the observed gain comes from better policy learning or merely better demonstration hygiene. By explicitly separating these three modes, the method can ask a sharper question: when does human guidance improve only the initialization distribution, and when must it remain online to stabilize disturbance-heavy contact phases? Any benefit that appears only through cleaner initialization is routed to `SEG`, while persistent online benefit is treated as evidence for a shared-autonomy-assisted deployment route rather than a fully autonomous hybrid-control claim.

### 3.10 Gain Attribution Under Coupled Disturbance

To keep the paper evidence-first, we define a simple gain-attribution rule over the main candidate modules introduced above. Let the measured first-round deltas be `ΔSEG`, `ΔPBG`, `ΔRLG`, `ΔDHG`, together with auxiliary human-guidance benefit `ΔHAG` and semantic-state benefit `ΔSSG`. We do not allow these gains to be pooled blindly. Instead, each gain is only considered valid if it appears under its intended evaluation gate: `ΔSEG` under matched-compute acceleration tests, `ΔPBG` under sim-to-real bridge evaluation, `ΔRLG` under D2/D3 contact-heavy disturbance slices, `ΔDHG` under deployment-oriented hybrid evaluation, `ΔHAG` under teleoperation-quality or bounded-copilot ablations, and `ΔSSG` under semantic-state ablations.

The practical consequence is a hard narrative constraint. If the strongest stable deltas are `ΔSEG + ΔHAG`, then the paper must be framed around data shaping and acceleration. If `ΔRLG + ΔSSG` dominate while `ΔDHG` remains weak, then the correct story is relation-aware local compensation with structured state support. If `ΔPBG` dominates, the story becomes a perception-bridge paper. Only when `ΔDHG` remains positive after bridge cost, risk coverage, and maintenance overhead are all accounted for may the draft escalate to a deployable hybrid-control narrative. This explicit attribution rule is necessary because D07 otherwise risks collapsing data quality, visual transfer, geometric stabilization, and controller architecture into one oversized claim.

### 3.11 Disturbance Regimes and Phase-Aligned State Partition

We partition aerial-arm execution into four disturbance regimes and three manipulation phases so that D07 can localize gains instead of over-reporting aggregate success. The disturbance regimes are **D0 nominal hover**, **D1 low-amplitude drift**, **D2 sustained platform disturbance**, and **D3 contact-compounded disturbance**. The manipulation phases are **P1 pre-contact alignment**, **P2 contact establishment**, and **P3 post-contact stabilization / lift**. Every reported gain must therefore be attached to a regime-phase pair `(Di, Pj)` rather than only to a whole-task average.

This partition matters because different ingredients in D07 should help in different places. Warm-start initialization should primarily act in optimization space and may only weakly affect `(D0, P1)` to `(D1, P1)` execution behavior after convergence. Relation-aware geometric priors and semantic relation state are expected to matter most in `(D2, P2)` and `(D3, P2/P3)`, where end-effector–object consistency and contact-sensitive correction dominate. Hybrid inner-loop stabilization, by contrast, only earns a deployment-facing claim if it preserves gains across the full disturbance ladder rather than winning in one narrow phase slice.

We therefore define a phase-aligned state summary `s_t = (x_base, x_arm, r_geo, r_sem, d_t, p_t)`, where `x_base` captures base attitude and translational drift, `x_arm` captures arm and gripper state, `r_geo` denotes local geometric relations, `r_sem` denotes optional semantic relation state, `d_t` is the disturbance regime tag, and `p_t` is the current phase tag. This state partition is not introduced as a new representation novelty by itself; instead, it serves as the accounting scaffold that lets D07 distinguish optimization acceleration from actual disturbance-phase robustness.

### 3.12 Phase-Bounded Compensation Interface

We formalize the local compensation layer as a phase-bounded interface instead of an unconstrained auxiliary policy. Let the main controller output an action proposal `a_t^π`, and let the compensation module output a correction `δ_t` conditioned on local relation state and phase context. The executed action is `a_t = a_t^π + M(d_t, p_t) ⊙ δ_t`, where `M(d_t, p_t)` is a regime-phase gate that only allows substantial correction in disturbance-heavy or contact-sensitive slices. In particular, D07 should allow stronger correction in `(D2, P2)`, `(D3, P2)`, and `(D3, P3)` than in nominal `(D0, P1)` motion.

This interface is important because it prevents the compensation layer from silently becoming a second full controller. If `δ_t` dominates everywhere, then any measured gain can no longer be honestly attributed to warm start, perception bridge, or hybrid stabilization. By bounding compensation to phase-appropriate windows, we make a narrower and more falsifiable claim: relation-aware structure helps mainly where geometric and contact transients destabilize aerial manipulation. If gains later appear only inside these gates, D07 should be written as local compensation under coupled disturbance, not as a broad whole-body autonomy story.

### 3.13 Shared-Autonomy Sequence Copilot as a Bounded Adaptation Interface

The recent D07 evidence base suggests that shared autonomy should not be treated merely as a teleoperation convenience layer. DiSCo [REF: 2603.22787] shows that sequence-level diffusion copilots can repair noisy human action sequences while preserving high-level intent, and Real-to-Sim-to-Real shared autonomy [REF: 2603.17016] suggests that small amounts of human interaction can be recycled into structured adaptation priors instead of discarded after demonstration collection. For aerial-arm manipulation, this matters because operators may still specify the correct target and phase intent under uncertainty, yet fail to provide stable low-level compensation under delay, base drift, or contact transients.

We therefore model shared autonomy as a bounded adaptation interface with three modes: **offline sequence refinement**, **early-training correction prior**, and **deployment-time bounded copilot assistance**. In the first mode, noisy teleoperation trajectories are denoised into behavior-cloning warm starts; in the second, a sequence copilot provides limited corrective priors during early RL fine-tuning; and in the third, human intent remains online but the copilot regularizes low-level execution over short action chunks. This formulation prevents D07 from collapsing all human contribution into one vague “HITL helps” statement. Any gain that only improves demonstration quality or convergence speed is routed to `SEG` or teleoperation-data quality, while any gain that remains necessary online must be interpreted as evidence for a shared-autonomy-assisted deployment route rather than fully autonomous control.

### 3.14 Semantic Relation State as Disturbance-Disambiguating Support

The local D07 reading queue also suggests that relation-centric state may be useful not because it adds more semantics in the abstract, but because it helps the controller disambiguate different failure causes under coupled disturbance. KG-M3PO [REF: 2603.24083] indicates that online scene-graph or relation-aware state can provide compact task structure under partial observability. For aerial-arm control, this structure is especially useful when visually similar observations may correspond to distinct control failures: viewpoint drift from base motion, target misalignment before grasp, obstacle conflict during approach, or contact-stage instability after closure.

We therefore introduce a semantic relation support layer that augments the local state with a small set of task-relevant relations, such as `end-effector ↔ target`, `target ↔ container`, and `arm ↔ obstacle`. This layer is not presented as a standalone reasoning contribution. Instead, it serves as a support interface for relation-aware local compensation, and any gain obtained from it is conservatively attributed to `RLG` unless it also survives deployment-oriented evaluation together with the rest of the stack. In this way, D07 can test semantic structure without allowing it to inflate the paper into a generic knowledge-guided manipulation narrative.

### 3.15 Window-Localized Gain Attribution Contract

To prevent first-round evidence from being over-upgraded, we define three execution windows for gain attribution: **W1 pre-contact alignment**, **W2 contact establishment**, and **W3 post-contact stabilization**. Warm-start and perception-bridge modules are expected to help mostly in W1 and, at most, the W1→W2 transition, whereas relation-aware local compensation and semantic support should matter primarily in W2 and W3 under D2/D3 disturbance. Hybrid stabilization only earns a deployment-facing interpretation if its gains remain positive across all three windows together with acceptable transfer and maintenance characteristics.

Formally, each baseline is summarized not only by `(SEG, PBG, RLG, DHG)` but also by a localized tuple `Δloc = (Δalign, Δcontact, Δstabilize)`. A result with large `Δalign` but weak `Δcontact` and `Δstabilize` is interpreted as acceleration or transfer support rather than robust manipulation control. A result with strong `Δcontact` and `Δstabilize` under D2/D3 but weak hybrid promotion is interpreted as relation-aware local compensation. This contract is essential for D07 because it forces every positive result to declare where in the aerial manipulation pipeline the gain actually occurs, instead of letting pre-contact improvements masquerade as deployable whole-task robustness.

## 3.16 World-Model and Training-Free Planning Families as Strong but Bounded Control Baselines

The recent local D07 anchor set adds two unusually strong control-side baselines that D07 should absorb early rather than treat as optional citations. TD-MPC2 [REF: 2310.16828] shows that a scalable implicit world model plus latent-space MPC can unify multi-task, multi-embodiment continuous control under a single optimization backbone. DIAL-MPC [REF: 2409.15610] shows an orthogonal route: full-order sampling-based MPC can be substantially strengthened through diffusion-style annealing without any policy pretraining. For D07, these two works matter because they collectively sharpen a missing baseline axis in the current MEGA framing: if moving-base aerial manipulation can already be improved by **better world-model planning** or **training-free full-order control**, then a learned high-frequency arm stabilizer must prove that its gain is not merely inherited from stronger planner-side prediction or stronger optimizer-side refinement.

We therefore introduce two additional support/control families into the D07 method reading rule. TD-MPC2-style methods are treated as a **world-model planning support family (WMPS)**: by default they are allowed to explain stronger sample efficiency, better cross-regime adaptation, smoother local trajectory proposals, and improved receding-horizon disturbance handling through learned latent dynamics. DIAL-MPC-style methods are treated as a **training-free optimizer support family (TFOS)**: by default they are allowed to explain stronger full-order tracking, lower solver-side local error, and better short-horizon disturbance rejection without any RL warm start. Under the MEGA title, however, neither family automatically earns the high-frequency stabilizer narrative. Unless a row still flattens `drift_amp \mapsto BDEE`, pushes `g^\dagger` to `W3`, and lowers payload-bearing `PGFR` after matched subtraction against WMPS or TFOS baselines, the honest claim ceiling remains **world-model/planning support** or **training-free control support**, not moving-base end-effector guidance.

### 3.17 Planner-Side versus Arm-Side Compensation Attribution under MEGA

The addition of TD-MPC2 and DIAL-MPC also forces a more explicit attribution split between **planner-side disturbance handling** and **arm-side residual compensation**. Planner-side methods can reduce tracking error by generating smoother local trajectories, better anticipation under latent dynamics, or stronger full-order online refinement. Arm-side MEGA methods, by contrast, claim something narrower and harder: even after the base remains imperfect and the planner is already reasonably competent, the manipulator still acts as the decisive high-frequency layer that preserves end-effector guidance during contact-heavy and payload-bearing windows.

To preserve this distinction, D07 should read every strong result through a two-step question. First, could the gain be mostly explained by a stronger planner or optimizer—e.g., a better world-model rollout, a more stable MPC proposal, or a training-free full-order solver that already absorbs much of the drift? Second, after that explanation is subtracted, does the arm-specific compensatory mechanism still reduce `BDEE`, improve `CET/PCST`, and survive to `s_p`? If the answer to the second question is weak, then the row must be frozen below the MEGA title, even if its aggregate success rate is high. This rule is especially important for D07 because the paper now positions itself not as “another strong aerial controller,” but as evidence that **mechanical-arm agility itself** can be the decisive precision-preserving layer over bounded UAV drift.

### 3.18 Deployment-Risk Coverage as a Post-Training but Pre-Claim Gate

The local ROBOGATE evidence [REF: 2603.22126] adds a missing but increasingly central layer to D07: before a row is allowed to occupy the strongest MEGA wording, it should not only survive support-family subtraction, but also pass a **deployment-risk coverage** audit. ROBOGATE is valuable precisely because it does not claim a better controller; instead, it shows that a policy can look strong under average-case metrics while still hiding sharp failure boundaries in transition regions of the disturbance space. For aerial manipulation, this is especially dangerous because moving-base contact success can collapse abruptly once drift amplitude, contact timing error, payload shift, or latency crosses a narrow threshold.

We therefore treat ROBOGATE as a **post-training verification layer** rather than a baseline policy family. Concretely, after a candidate row has cleared matched-compute comparison and initial guidance-first audits, it should be exposed to a two-stage boundary-focused sweep over D07-relevant disturbance axes: base-drift amplitude, contact-timing offset, payload variation, state-estimation degradation, and actuation latency. The purpose is not to inflate the benchmark shell again, but to ask a reviewer-facing question that average success cannot answer: **does the claimed high-frequency stabilizer still behave predictably near the failure boundary, or is its apparent gain concentrated only in easy interiors of the disturbance set?**

Under the MEGA reading rule, this layer supplies a new residual constraint rather than a new promotion family. A row that still shows a flatter `drift_amp \mapsto BDEE` curve under ordinary sweeps but reveals a narrow, brittle transition zone under boundary-focused coverage should be frozen below the strongest headline until the risk surface is better understood. In other words, D07 should not let “looks deployable on mean metrics” outrun “has acceptable deployment-risk coverage near the boundary.”

### 3.19 Current Evidence-Consistent Next Step after Re-reading TD-MPC2, DIAL-MPC, and ROBOGATE

After re-reading the strongest recent local D07 anchors across planner-side and deployment-risk families—**TD-MPC2** [REF: 2310.16828], **DIAL-MPC** [REF: 2409.15610], and **ROBOGATE** [REF: 2603.22126]—the most evidence-consistent next step is not to expand the method story further, but to tighten the first trustworthy bundle around a single residual question: **after stronger planning/optimization explanations are matched out, does the arm still act as the decisive high-frequency stabilizer, and does that residual survive boundary-focused deployment-risk coverage rather than only average-case evaluation?**

This means the next D07 write-up priority should remain guidance-first and subtraction-heavy, but now with a firmer post-training gate. WMPS and TFOS should be granted the first right to explain smoother disturbance handling through better planning or full-order online refinement. ROBOGATE should then be granted the right to challenge the surviving residual with boundary-focused risk discovery. Only if a row still keeps a flatter `drift_amp \mapsto BDEE` curve, survives to `g^\dagger=W3`, lowers `PGFR`, and does not collapse under boundary-focused deployment-risk coverage may the paper continue to route it upward toward **deployable hybrid** or the stronger **mechanical-arm high-frequency stabilizer** reading promised by MEGA.

### 3.20 Transfer-Protocol Support versus Guidance Residual for Isaac-Lab-Centered Co-Training

The local D07 archive also contains a more immediately actionable support family for the current paper draft: **RL-Based Sim-Real Co-Training for VLA** [REF: 2602.12628]. This line of work is highly relevant because it suggests that Isaac-Lab-native pretraining, domain randomization, and limited real anchoring can be organized into one continuous protocol instead of three loosely connected stages. For D07, that directly strengthens the route from safe large-scale simulation to hardware adaptation. However, the most honest first interpretation is still protocol-side rather than controller-side: a row may look much stronger simply because it starts closer to the real distribution, adapts with fewer unsafe trials, or preserves sim-real continuity more gracefully before the moving-base disturbance interface itself has improved.

We therefore explicitly freeze this family as **transfer-protocol support (TPS)**. By default, TPS is allowed to explain stronger initialization alignment, lower real-world adaptation cost, cleaner sim-real continuity, and earlier deployment readiness in mild disturbance regimes. Under the MEGA reading rule, however, TPS should not be allowed to silently occupy the main title narrative. Unless a row still flattens `drift_amp \mapsto BDEE`, pushes `g^\dagger` to `W3`, and lowers payload-bearing `PGFR` after matched subtraction against initialization, adaptation-cost, and sim-real continuity advantages, the honest ceiling remains **protocol-side support** rather than moving-base end-effector guidance.

This addition is important because D07 is now mature enough that deployment-flavored gains can easily be over-credited. A row that transfers more smoothly from Isaac Lab to hardware is clearly valuable, but it may still owe most of its improvement to better protocol continuity rather than a stronger arm-side residual guidance mechanism. Under MEGA, only the residual that survives TPS subtraction should continue to count as evidence for the manipulator being the decisive high-frequency stabilizer over bounded UAV drift.

### 3.21 Four-Way Support Audit before Promoting the High-Frequency Stabilizer Claim

With TPS made explicit, D07 should now treat its first trustworthy bundle as a four-way support subtraction problem rather than a simple leaderboard comparison. Before any row is allowed to support the strongest MEGA wording, it should be audited against **platform/reproducibility support (PRS)**, **reward-origin support (ROS)**, **verification-shell support (VSS)**, and **transfer-protocol support (TPS)**. The purpose of this audit is not to weaken the paper, but to ensure that the claim “the arm acts as a high-frequency stabilizer over low-frequency UAV drift” only appears after the strongest easier explanations have already been exhausted.

Operationally, every submission-facing row should therefore be interpreted through a compact residual summary

\[
\Lambda_{row}^{MEGA} = (PRS,\; ROS,\; VSS,\; TPS,\; BDEE,\; CET,\; PCST,\; PGFR,\; g^{+},\; g^{\dagger},\; blocker),
\]

where each support family is first granted the right to explain its natural gain pattern and only the remaining residual is considered title-bearing evidence. A row that mainly wins because resets are cleaner, reward credit is denser, the benchmark shell is broader, or sim-real adaptation is smoother should freeze at the corresponding support ceiling even if its aggregate success rate is attractive. Only residual gains that continue to flatten `drift_amp \mapsto BDEE`, shorten disturbed contact establishment, stabilize post-contact execution, and lower payload-bearing failure after PRS/ROS/VSS/TPS subtraction deserve to occupy the strongest MEGA language.

### 3.22 Current Evidence-Consistent Next Step after Re-reading RL Sim-Real Co-Training and the Strong Local Support Families

After re-reading **RL-Based Sim-Real Co-Training for VLA** together with the strongest local D07 support families—platform-side Isaac-Lab infrastructure, reward-side ARM-style crediting, verification-side MobileManiBench, and the existing strong control/planning baselines—the most evidence-consistent next step is not to widen the method stack again, but to force the first executable bundle to answer one narrower question: **after PRS/ROS/VSS/TPS are all given first-pass explanatory rights, is there still a non-trivial arm-side guidance residual left?**

This directly changes the writing priority for D07. The paper no longer most needs more candidate modules or more paper names; it needs a cleaner residual audit strong enough to show that the manipulator is not merely easier to train, easier to verify, or easier to transfer, but actually preserves moving-base end-effector guidance once those weaker explanations are removed. In practice, that means the next trustworthy D07 milestone should remain guidance-first, subtraction-heavy, and explicitly centered on whether residual gains still survive to `g^\dagger=W3` while keeping a flatter `drift_amp \mapsto BDEE` curve and lower `PGFR`.

### 3.23 BC-to-Q Warm Start and Gated Online Recovery

The recent Q2RL evidence [REF: 2605.05172] suggests that D07 should not treat behavior-cloning warm start as a binary choice between “keep BC fixed” and “discard BC for online RL.” Instead, a more deployment-relevant route is to extract a lightweight Q-function from the BC policy and use it as a *gated bridge* between stable initialization and bounded online improvement. For aerial-arm control, this matters because catastrophic forgetting under disturbance can erase a usable warm start long before a pure RL fine-tuning loop learns robust contact recovery.

We therefore introduce a **BC-to-Q warm-start interface** for D07. Let `π_BC` denote the demonstration-initialized policy and `Q_BC` denote a Q-estimator distilled from the same behavior manifold. During online fine-tuning, the controller samples between the BC action and the RL action under a gated selection rule
\[
a_t = \begin{cases}
a_t^{\text{BC}}, & Q_{BC}(s_t, a_t^{\text{BC}}) \ge Q_{RL}(s_t, a_t^{\text{RL}}) - \epsilon_t \\
a_t^{\text{RL}}, & \text{otherwise},
\end{cases}
\]
where `ε_t` is a disturbance- and phase-aware tolerance. In low-risk windows or when the RL branch has already established stable gains, the gate may favor `a_t^{RL}`; in high-risk W2/W3 slices under D2/D3 disturbance, the gate is allowed to fall back to BC-consistent actions when the learned online branch becomes unstable.

This interface is intentionally narrow in narrative scope. If the gain appears mainly as faster early recovery after warm start and reduced catastrophic forgetting, it is routed to `SEG` plus online-retention support rather than being promoted to a deployable hybrid-control claim. Only when BC-to-Q gating improves W2/W3 contact robustness under disturbance, *and* those gains survive the hybrid and transfer gates, may it support a stronger deployment-facing story. In other words, Q-guided gating is first treated as an **honest anti-forgetting bridge** between offline skill and online improvement, not as automatic proof of full aerial-arm autonomy.

### 3.18 Hierarchical RL Planning with Analytic Safety Projection

The newly added reactive dexterous grasping evidence [REF: 2605.03363] sharpens another D07 design choice: a high-dimensional manipulation stack may benefit from explicitly separating **task-space intent generation** from **joint-space safety enforcement**. Rather than asking a single policy to jointly discover fast response, collision avoidance, joint-limit satisfaction, and disturbance recovery, D07 can instantiate a two-stage interface in which the learned policy proposes short-horizon task-space motion and an analytic controller projects that proposal into a safe executable action.

Concretely, we let the high-level policy output a task-space velocity proposal `u_t^{task}` conditioned on phase-aligned disturbance state, while a low-level projection layer solves
\[
a_t^* = \arg\min_{a_t} \; \|J(q_t)a_t - u_t^{task}\|_2^2 + \lambda_{safe} \mathcal{C}(a_t; q_t, o_t, c_t),
\]
where `J(q_t)` is the manipulator Jacobian and `\mathcal{C}` encodes joint-limit, self-collision, obstacle-clearance, and contact-safety constraints. The goal is not to turn D07 into a QP-control paper, but to create a clean experimental boundary between *RL planning gain* and *analytic safety-shell gain*.

This decomposition is valuable for D07 because it gives the hybrid line a stronger mechanistic interpretation. If the analytic projection mainly prevents unsafe local actions without improving disturbance-window robustness, its gain belongs to a safety-shell baseline. If, however, the planner-plus-projection stack materially improves W2/W3 stability, reduces catastrophic contact failures, and survives sim-to-real transfer, then D07 can legitimately argue for a **deployable hybrid control route** rather than a pure warm-start or local-compensation paper. This section therefore strengthens the method-side distinction between `RLG` and `DHG`: relation-aware priors explain *where* to compensate, while analytic projection layers explain *how* to keep that compensation controller-consumable and safe.

### 3.19 Infrastructure-Supported Reproducibility versus Controller-Side Guidance Residual

The current D07 evidence base also makes a quieter but important attribution issue impossible to ignore: some rows may look more stable simply because the **training/evaluation shell is cleaner**, not because the controller-side guidance interface is stronger. Isaac Lab [REF: 2511.04831] is especially valuable here because it standardizes disturbance injection, reset semantics, batched simulation, sensor/actuator plumbing, and logging under one GPU-native stack. This greatly improves reproducibility and experimentation speed, but under the MEGA title it should still default to an **infrastructure-supported reproducibility support** family rather than being silently merged into controller gain.

We therefore introduce a platform-side subtraction variable

\[
\Pi_t = (q_t^{\mathrm{reset}},\; q_t^{\mathrm{disturb}},\; q_t^{\mathrm{log}},\; q_t^{\mathrm{replay}},\; q_t^{\mathrm{guidance-resid}}),
\]

where `reset` measures whether episode semantics and reset determinism are matched across rows, `disturb` measures whether disturbance injection fidelity and sweep coverage are matched, `log` measures whether the same guidance-relevant telemetry is available, `replay` measures whether a row benefits mainly from stronger replayability/debuggability, and `guidance-resid` denotes the residual guidance gain after these infrastructure-side advantages are subtracted. In our reading rule, a row that wins mainly because the platform shell exposes cleaner disturbances, more stable resets, or better replay-driven debugging should freeze as **platform/reproducibility support** rather than being promoted to a stronger moving-base end-effector guidance claim.

This distinction matters because D07 is unusually sensitive to hidden tooling effects. Better disturbance APIs can make a controller look more robust by smoothing the training curriculum; better logging can make a method easier to tune into a good local optimum; cleaner reset semantics can reduce variance enough to mimic algorithmic progress. Under MEGA, only the residual that still improves `BDEE`, `CET`, `PCST`, and `PGFR` after these platform-side factors are matched deserves to be described as controller-side guidance gain.

### 3.20 Reward-Origin Credit versus Guidance-Side Credit

A second subtraction boundary comes from long-horizon reward modeling. ARM: Advantage Reward Modeling for Long-Horizon Manipulation [REF: 2604.03037] shows that a row can improve substantially because **reward credit is assigned more honestly across recovery and backtracking**, not necessarily because the disturbance-handling interface itself has become stronger. This is directly relevant for D07 because moving-base manipulation contains many deceptive partial wins: the arm may briefly recover after drift, maintain contact for a few steps, or salvage a near-failure trajectory, and advantage-style reward modeling can turn those events into more learnable credit signals.

We therefore introduce a reward-origin subtraction tuple

\[
\mathcal{R}_t = (q_t^{\mathrm{rew-shape}},\; q_t^{\mathrm{recovery-credit}},\; q_t^{\mathrm{backtrack-credit}},\; q_t^{\mathrm{guidance-credit}},\; q_t^{\mathrm{guidance-resid}}),
\]

where `rew-shape` measures whether the row benefits mainly from denser or more structured reward shaping, `recovery-credit` measures whether recovery events are credited more cleanly, `backtrack-credit` measures whether partial regress-and-return trajectories are revalued more favorably, `guidance-credit` measures whether the guidance tuple itself (`BDEE/CET/PCST/PGFR`) changes before subtraction, and `guidance-resid` denotes the remaining guidance improvement after reward-origin effects are discounted. In our attribution rule, a row that mainly wins because ARM-style reward credit makes long-horizon optimization easier should freeze as **reward-origin support** rather than being promoted directly to high-frequency stabilizer evidence.

This addition keeps D07 honest in a way that reviewer-facing result tables often are not. A controller may appear better under coupled disturbance because the optimizer now recognizes recovery as useful, not because the manipulator truly contributes more arm-side residual guidance. Under the MEGA title, only rows whose gains survive subtraction against both **platform/reproducibility support** and **reward-origin support** may continue upward toward a controller-side moving-base guidance claim.

### 3.21 Verification-Shell Support versus Moving-Base Guidance Residual

The recent local D07 anchor set also highlights a quieter but increasingly important confound: some apparent progress may come from a **better verification shell** rather than a stronger arm-side guidance mechanism. MobileManiBench [REF: 2602.05233] is especially useful here because it standardizes synthetic task generation, multi-view observation export, failure-slice exposure, and verification workflow construction inside Isaac Sim. For D07, this is highly valuable, but its default contribution is still verification-side: it can make rows easier to compare, easier to debug, and more likely to expose brittle disturbance slices without yet changing the controller interface itself.

We therefore introduce a verification-shell subtraction tuple

\[
\mathcal{V}_t = (q_t^{\mathrm{task-cover}},\; q_t^{\mathrm{fail-cover}},\; q_t^{\mathrm{slice-cover}},\; q_t^{\mathrm{build-cost}},\; q_t^{\mathrm{guidance-resid}}),
\]

where `task-cover` measures task-family and scene coverage, `fail-cover` measures surfaced failure-mode diversity, `slice-cover` measures whether drift/contact/payload slices are adequately represented, `build-cost` measures the benchmark-construction burden, and `guidance-resid` denotes the remaining `BDEE/CET/PCST/PGFR` improvement after verification-shell advantages are discounted. In our reading rule, a row that wins mainly because the benchmark shell is broader, cleaner, or more failure-sensitive should freeze as **verification-shell support (VSS)** rather than being promoted to a stronger MEGA claim.

This distinction matters because D07 is now rich enough in support routes that better measurement can easily masquerade as better control. A richer Isaac-Sim validation substrate may reveal brittle retention-only, projection-only, or reward-origin rows earlier and more cleanly, yet that is still evidence hygiene rather than controller innovation. Under MEGA, only the residual that still flattens `drift_amp \mapsto BDEE`, improves `CET/PCST`, and lowers payload-bearing `PGFR` after matched verification-shell subtraction deserves to be written as arm-side moving-base guidance gain.

### 3.22 Verification-Shell-Subtracted Promotion Rule before the MEGA Claim

The addition of MobileManiBench-style verification support means that D07 can no longer treat "better exposed results" and "better guidance mechanisms" as interchangeable. Before any row is promoted toward the MEGA headline, it should therefore pass a three-way subtraction against **platform/reproducibility support (PRS)**, **reward-origin support (ROS)**, and **verification-shell support (VSS)**. PRS asks whether a row mainly benefits from a cleaner training/evaluation substrate, ROS asks whether it mainly benefits from more faithful long-horizon reward credit, and VSS asks whether it mainly benefits from a broader and more failure-sensitive validation shell.

Operationally, every first-round row should now be interpreted through a compact summary

\[
\Omega_{row}^{MEGA} = (PRS,\; ROS,\; VSS,\; BDEE,\; CET,\; PCST,\; PGFR,\; g^{+},\; g^{\dagger},\; blocker),
\]

where `VSS` records whether verification-shell advantages remain a sufficient explanation after matched guidance audits. If a row becomes persuasive only because task coverage is broader, failure slices are cleaner, or benchmark construction is more standardized, it should freeze as **verification-shell support** even when its aggregate result table looks stronger. Only residual gains that still survive PRS/ROS/VSS subtraction and continue to flatten the `drift_amp \mapsto BDEE` curve, shorten disturbed contact establishment, preserve post-contact stabilization, and reduce payload-bearing guidance failure are allowed to occupy the MEGA title route.

### 3.23 Metric-First Support-Family Rights under the Current Local Anchor Set

The current local D07 anchor set further suggests that support families should not only be subtracted abstractly; they should also be given **metric-first explanatory rights** before any stronger title escalation is allowed. In other words, each weaker family is entitled to first explain improvement on the guidance metric that is most naturally aligned with its mechanism. A row may only be promoted beyond that family if it still produces residual gains outside the family’s most natural metric footprint after matched subtraction.

Under this rule, **perception-bridge support** such as ViserDex-style sim-real visual alignment should first be allowed to explain `BDEE` reduction under matched disturbance, because cleaner visual state is most directly expressed as lower moving-base end-effector error. **Deployable hybrid support** such as DSAM-style inner-loop stabilization should first be allowed to explain `PCST` and `PGFR`, because hybrid attitude/manipulator coupling naturally targets post-contact stability and payload-bearing survival. **Reward-origin support** such as ARM should first be allowed to explain late-window recovery and `CET/PCST` improvements that arise from cleaner long-horizon credit assignment. **Verification-shell support** such as MobileManiBench should first be allowed to explain broader failure exposure, slice coverage, and lower blocker ambiguity, not controller-side gains themselves.

This metric-first rights doctrine makes D07 more honest in two directions. First, it prevents weak families from being unfairly dismissed when they do explain the metric they are naturally supposed to help. Second, it prevents those same families from silently over-expanding into a MEGA headline. A row that mainly lowers `BDEE` through visual bridge quality should first freeze at perception-bridge support unless it also survives into `CET/PCST/PGFR` residual improvement under matched subtraction. Likewise, a row that mainly improves payload-bearing survival because the inner loop is cleaner should first freeze at hybrid support unless the residual still demonstrates arm-side high-frequency guidance gain beyond that explanation.

### 3.24 Sim-Real Co-Training Protocol versus Guidance-Bearing Residual

The re-read of RL-Based Sim-Real Co-Training for VLA [REF: 2602.12628] also sharpens a distinct subtraction boundary that sits between platform quality and controller design. Sim-real co-training can make a row look stronger because initialization is better aligned, adaptation is cheaper, and the representation bridge between simulation and real execution is already partially solved before moving-base disturbance is even stressed. For D07, this is directly relevant because Isaac-Lab-native pretraining plus limited real anchoring may substantially improve transfer readiness without yet proving that the manipulator has become a better high-frequency stabilizer over UAV drift.

We therefore introduce a transfer-protocol subtraction tuple

\[
\mathcal{T}_t = (q_t^{\mathrm{init-align}},\; q_t^{\mathrm{adapt-cost}},\; q_t^{\mathrm{repr-bridge}},\; q_t^{\mathrm{guidance-credit}},\; q_t^{\mathrm{guidance-resid}}),
\]

where `init-align` measures how much a row benefits from stronger initialization or pretraining alignment, `adapt-cost` measures whether the gain is mainly reduced real-world adaptation burden, `repr-bridge` measures whether cross-domain representation matching remains the dominant explanation, `guidance-credit` measures the pre-subtraction change in `BDEE/CET/PCST/PGFR`, and `guidance-resid` denotes the remaining guidance-side gain after transfer-protocol effects are discounted. In our reading rule, rows that mostly win because they start from a better sim-real protocol should freeze as **transfer-protocol support (TPS)** unless they still improve moving-base guidance after `\mathcal{T}_t` subtraction.

This distinction is useful because D07 now contains several routes that can improve deployment readiness without yet changing the core compensatory physics. Sim-real co-training may reduce the number of dangerous real trials, shorten the road from Isaac Lab to hardware, and stabilize representation learning across domains; all of that is valuable. But under the MEGA title, those wins should still default to protocol-side credit unless the residual continues to flatten the `drift_amp \mapsto BDEE` curve, shorten disturbed contact establishment, stabilize post-contact holding, and reduce payload-bearing failure in ways that cannot be explained by initialization or transfer readiness alone.

### 3.25 Four-Way Support Subtraction before High-Frequency Stabilizer Promotion

With the addition of transfer-protocol support, D07 now requires a stricter pre-promotion audit than a simple guidance score comparison. Before any row is allowed to support the claim that the manipulator functions as a **high-frequency stabilizer over low-frequency UAV drift**, it should pass a four-way subtraction against **platform/reproducibility support (PRS)**, **reward-origin support (ROS)**, **verification-shell support (VSS)**, and **transfer-protocol support (TPS)**. The purpose is not to make promotion impossible, but to ensure that each stronger claim only emerges after weaker but plausible explanations have been exhausted.

Operationally, we refine the MEGA accounting summary to

\[
\Lambda_{row}^{MEGA} = (PRS,\; ROS,\; VSS,\; TPS,\; BDEE,\; CET,\; PCST,\; PGFR,\; g^{+},\; g^{\dagger},\; blocker),
\]

where `TPS` records whether sim-real co-training, initialization alignment, or adaptation-cost reduction still fully explains the apparent gain. A row that mainly improves because it trains on a cleaner protocol, transfers more smoothly, or requires fewer real correction episodes should freeze as protocol-side support even if its aggregate deployment story looks attractive. Only residual gains that survive PRS/ROS/VSS/TPS subtraction and still improve the moving-base guidance tuple are eligible for promotion toward the MEGA headline.

### 3.26 Metric-First Rights for PRS/ROS/VSS/TPS under the Current D07 Bundle

For the first submission-facing bundle, the four subtraction families should each be assigned one default explanatory frontier before any escalation to controller-side guidance is allowed. **PRS** is first allowed to explain stability that comes from cleaner resets, disturbance sweeps, logging parity, and replayability; it therefore owns the default explanation for variance reduction and reproducibility-sensitive improvements. **ROS** is first allowed to explain late-window recovery and backtrack-sensitive gains, especially when `CET/PCST` improve without a commensurate flattening of the `drift_amp \mapsto BDEE` curve. **VSS** is first allowed to explain broader failure coverage, better slice exposure, and lower blocker ambiguity. **TPS** is first allowed to explain reduced adaptation burden, better initialization, and smoother sim-real transition.

This metric-first assignment is not just bookkeeping. It defines a reviewer-facing fairness rule for D07: every weaker family gets the first chance to explain the gain pattern it is naturally supposed to produce. Only after that explanation is matched and subtracted may the row claim a stronger arm-side residual guidance improvement. This keeps the paper from over-rewarding attractive deployment stories that are actually driven by protocol, reward, infrastructure, or validation-shell advantages.

### 3.27 Minimal Four-Row Bundle and Family-Specific Promotion Ceilings

To make the subtraction logic executable, the first trustworthy D07 bundle should remain minimal: a **scratch or warm-start baseline**, a **sim-real / transfer-strengthened row**, a **reward- or verification-strengthened row**, and a **guidance-targeted hybrid row**. The reason for keeping only four rows is that D07’s current problem is no longer lack of candidate methods, but lack of disciplined attribution. A small bundle is easier to interpret under the MEGA audit because each row can be assigned a natural first explanation family before stronger narratives are considered.

Under this minimal bundle, the default promotion ceilings are explicit. The warm-start row is first capped at acceleration or retention-style support unless it improves late-window guidance metrics. The sim-real row is first capped at **TPS** unless its residual survives transfer subtraction. The reward- or verification-strengthened row is first capped at **ROS** or **VSS** unless its residual survives those subtractions. The hybrid row is first capped at deployable-interface support unless it still demonstrates arm-side residual guidance gain after all weaker explanations are matched. This structure turns D07’s first results into a decision instrument rather than a loose collection of promising curves.

### 3.28 High-Frequency Stabilizer Residual after Support-Family Matching

The final purpose of the support-family machinery is to isolate one very narrow but valuable residual: whether the manipulator still contributes a distinct **high-frequency stabilizer residual** after platform quality, reward credit, verification shell, sim-real protocol, and stronger training-shell advantages are all matched. We denote this residual by

\[
\Delta^{HF}_{row} = \Delta \mathcal{G}_{row} \setminus (PRS \cup ROS \cup VSS \cup TPS \cup MCS),
\]

where `\Delta \mathcal{G}_{row}` is the observed change in the guidance tuple `(BDEE, CET, PCST, PGFR)` and the set subtraction is conceptual rather than algebraic: it asks whether the gain still survives after every weaker family has been given its strongest reasonable explanation.

This residual is the only evidence that should speak in the strongest MEGA language. If `\Delta^{HF}_{row}` remains positive, reaches `g^{\dagger}=W3`, survives payload-bearing disturbance, and continues to flatten the `drift_amp \mapsto BDEE` curve, then D07 earns the right to say that the manipulator is acting as a high-frequency stabilizer over low-frequency UAV drift. If not, the paper should still report the result proudly—but under the honest narrower route that the evidence actually supports.

### 3.29 Sim-Real Co-Training as a Transfer-Protocol Ceiling rather than a Guidance Interface

RL-Based Sim-Real Co-Training for VLA [REF: 2602.12628] offers one of the closest local anchors to the current D07 toolchain because it couples Isaac-Lab-native pretraining, domain randomization, and real-data adaptation into one continuous protocol. For D07, this route is directly useful: it can reduce unsafe real-world exploration, shorten adaptation cycles, and provide a cleaner bridge from simulator-trained manipulation priors to hardware execution. However, under the MEGA framing, its first honest interpretation should remain **transfer-protocol support** rather than **guidance-bearing control evidence**.

We therefore sharpen the transfer-side reading rule with an explicit ceiling. A row strengthened by sim-real co-training is first allowed to explain **initialization alignment**, **adaptation-cost reduction**, **representation continuity**, and **deployment-readiness under mild disturbance**. It is not yet allowed to claim that the manipulator itself has become the decisive high-frequency stabilizer over bounded UAV drift. In other words, even if sim-real co-training yields smoother hardware adaptation and better average transfer success, those gains should still default to `TPS` unless they survive matched subtraction and continue to improve `BDEE`, `CET`, `PCST`, and `PGFR` in moving-base disturbance slices.

This distinction is important because D07 is now mature enough that deployment-flavored wins can easily be over-credited. A method that transfers more cleanly from Isaac Lab to hardware is clearly valuable, but that may still mean the **protocol** is better rather than the **arm-side residual guidance mechanism** being stronger. Under MEGA, only the residual remaining after `TPS` is granted first-pass explanatory rights should continue upward toward the headline claim.

### 3.30 Isaac-Lab Infrastructure as Platform/Reproducibility Support, not Controller Credit

Isaac Lab and the broader Isaac Sim ecosystem are foundational to D07, but the platform itself should not silently occupy controller-side credit. The local reading set—especially the Isaac-Lab framework note and the Isaac-Sim survey family—shows that better reset semantics, stronger batched disturbance injection, cleaner logging, and more reproducible experiment wiring can substantially improve training stability and evaluation clarity. These are real scientific advantages. Yet under the MEGA title they should still default to **platform/reproducibility support (PRS)** rather than being merged into arm-side guidance gain.

For this reason, D07 should keep treating platform quality as an explicit subtraction family. A row that looks more stable because the simulator shell exposes cleaner disturbances, supports better replay/debugging, or reduces variance through more controlled resets should first freeze at the PRS ceiling. Only if the same row still improves the guidance tuple after platform-side advantages are matched can it claim controller-side evidence.

This platform-first discipline matters especially for D07 because moving-base manipulation is unusually sensitive to tooling hygiene. A better disturbance API or more faithful replay path can make a method look substantially more robust even when the underlying compensatory interface is unchanged. The paper therefore needs to say, in effect: **we value Isaac Lab as the substrate, but we refuse to count substrate quality itself as proof that the mechanical arm is the decisive high-frequency stabilizer.**

### 3.31 Current Evidence-Consistent Next Step after Re-reading 2602.12628 and the Local Isaac-Lab Family

After re-reading **RL-Based Sim-Real Co-Training for VLA (2602.12628)** together with the local Isaac-Lab / Isaac-Sim infrastructure anchors, the most evidence-consistent next step for D07 is not to widen the method story again, but to tighten the transfer-versus-guidance boundary inside the paper draft. The current local evidence is already strong enough to justify two conservative statements: **(i)** sim-real co-training is a credible way to reduce adaptation burden and improve protocol continuity for aerial-arm research; **(ii)** Isaac-Lab-native infrastructure is a credible way to improve reproducibility and evaluation hygiene. What remains unproven is the stronger statement that these same gains automatically imply better moving-base end-effector guidance.

This directly changes the writing priority for the next bundle. D07 should keep the first trustworthy readout centered on one narrow residual question: **after PRS and TPS are granted their natural explanatory rights, is there still an arm-side gain that flattens `drift_amp \mapsto BDEE`, improves `CET/PCST`, and lowers payload-bearing `PGFR`?** If not, the honest paper route is still valuable—but it should be written as a transfer-protocol or infrastructure-strengthened manipulation stack, not yet as proof that the manipulator itself is the decisive high-frequency stabilizer.

### 3.32 Demonstration-Guided Multi-Task Curriculum as Support-Side Optimization Shell

The newly surfaced MT-Libero + DGPO result [REF: 2606.03335] adds one more strong but still bounded family that D07 should absorb early: a **demonstration-guided multi-task curriculum shell** built directly in Isaac Lab. Its main value is not that multi-task PPO automatically solves moving-base aerial manipulation, but that GPU-parallel heterogeneous task training plus matched-demonstration guidance can significantly improve initialization quality, task-family coverage, and optimization stability before the moving-base guidance bottleneck is even stressed. In other words, this family can make a row look much more trainable and much less brittle without yet proving that the arm itself has become a better high-frequency stabilizer.

We therefore treat this route as a **multi-task curriculum shell (MCS)** support family. By default, MCS is allowed to explain stronger demonstration-aligned initialization, broader task coverage, smoother on-policy learning under sparse success signals, and lower adaptation burden across related manipulation tasks. Under the MEGA title, however, these gains should remain support-side unless the same row still flattens `drift_amp \mapsto BDEE`, pushes `g^{\dagger}` to `W3`, and lowers payload-bearing `PGFR` after curriculum-side advantages are matched out. A row that mainly wins because the optimization shell is better organized should therefore freeze at **curriculum support**, not be silently promoted into a moving-base guidance claim.

### 3.30 Metric-First Rights and Promotion Ceiling for MCS Rows

The addition of MCS also sharpens D07's metric-first reading discipline. A demonstration-guided multi-task curriculum is most naturally entitled to first explain **adaptation-cost reduction**, **seed variance reduction**, and occasionally cleaner early `BDEE` improvement through better shared representations. It is not, by default, entitled to explain stronger late-window contact establishment, post-contact stabilization, or payload-bearing guidance survival. Therefore, if an MCS-strengthened row looks better in aggregate, the reviewer-facing first question should be whether the gain stops at optimization-shell metrics or actually propagates into the guidance tuple under moving-base disturbance.

Concretely, D07 should only let an MCS row rise above the curriculum-support ceiling if its residual remains positive after matched subtraction against task-family coverage and demonstration-guided initialization quality, and if that residual still improves `CET/PCST/PGFR` while keeping the `drift_amp \mapsto BDEE` slope flatter. If not, the honest readout is that MCS improved the **training shell** rather than the **moving-base guidance interface** itself. This is exactly the kind of distinction D07 now needs, because the paper is no longer about general trainability inside Isaac Lab; it is about whether arm-side agility remains the decisive precision-preserving layer after the easy explanations have been removed.

### 3.31 Four-Plus-One Support Audit before High-Frequency Stabilizer Promotion

With MCS added, the pre-promotion audit for D07 should now be read as a **four-plus-one** support subtraction: **platform/reproducibility support (PRS)**, **reward-origin support (ROS)**, **verification-shell support (VSS)**, **transfer-protocol support (TPS)**, and **multi-task curriculum shell support (MCS)**. The logic is cumulative but still simple: if a row can already be explained by a cleaner platform, a better reward-credit path, a richer benchmark shell, a smoother sim-real protocol, or a stronger curriculum shell, then those explanations must be exhausted before the paper is allowed to credit the manipulator itself as the high-frequency stabilizer.

Operationally, the submission-facing readout can extend the MEGA accounting tuple to

\[
\Lambda^{+}_{row} = (PRS,\; ROS,\; VSS,\; TPS,\; MCS,\; BDEE,\; CET,\; PCST,\; PGFR,\; g^{+},\; g^{\dagger},\; blocker),
\]

where `MCS` records whether demonstration-guided curriculum effects still provide a sufficient explanation after matched guidance audits. Under this rule, a row that mainly improves because it is easier to train across related manipulation tasks should freeze at **curriculum-shell support**. Only the residual that survives this expanded subtraction and still improves the moving-base guidance tuple deserves to occupy the strongest MEGA language.

### 3.32 Current Evidence-Consistent Next Step after Re-reading Isaac Sim Survey and MT-Libero + DGPO

After absorbing the two newest June anchors—**NVIDIA Isaac Sim: Enabling Scalable, GPU-Accelerated Simulation for Robotics** [REF: 2606.03551] and **GPU-Parallel Multi-Task Reinforcement Learning with Demonstration Guided Policy Optimization** [REF: 2606.03335]—the most evidence-consistent next step for D07 is not to broaden the method story, but to narrow the first trustworthy bundle even further around **moving-base guidance residual after support-family subtraction**. The Isaac Sim survey strengthens the infrastructure-side justification for choosing Isaac Sim / Isaac Lab as the experimental substrate, while MT-Libero + DGPO strengthens the optimization-shell argument for demonstration-guided multi-task training. Both are useful; neither should be allowed to silently occupy the paper's core contribution.

### 3.33 Off-Policy Throughput Support as a Training-Shell Ceiling

The current D07 draft is already support-heavy enough that a future off-policy acceleration row could easily be over-interpreted if its ceiling is not written in advance. In particular, if D07 later introduces a FlashSAC-style or replay-efficient off-policy branch, its first and weakest explanation should not be controller-side guidance, but **training-shell throughput improvement**. Such a branch can plausibly reduce wall-clock time to threshold, improve replay reuse, stabilize critic updates, and reduce seed variance, all without yet proving that the manipulator has become a better high-frequency stabilizer over bounded UAV drift.

We therefore introduce **off-policy throughput support (OTS)** as a dedicated ceiling. By default, OTS is allowed to explain `time-to-threshold` gains, replay-efficiency gains, critic-stability gains, and seed-variance reduction under matched compute. Under the MEGA reading rule, however, OTS is not allowed to claim moving-base guidance improvement unless the same row still flattens `drift_amp \mapsto BDEE`, improves `CET/PCST`, survives to `g^{\dagger}=W3`, and lowers payload-bearing `PGFR` after throughput-side advantages are matched out. This ceiling matters because a faster or more stable learner is scientifically useful, but under D07 it should still first be written as **training-shell support** rather than mistaken for evidence that the arm-side residual compensation mechanism has fundamentally improved.

### 3.34 Row-Native Support Frontiers for the First Four-Row Bundle

The four-row minimal bundle in D07 is only useful if each row is granted an explicit **row-native support frontier** before stronger title escalation is allowed. `B2` should first be allowed to explain **warm-start acceleration**, i.e., stronger `WSR / PRS / W1` alignment and earlier convergence under matched compute. `B3` should first be allowed to explain **retention-bridge behavior**, i.e., better W2/W3 holding and limited `CET` improvement caused by BC-to-Q gating rather than true arm-side disturbance compensation. `B4` should first be allowed to explain **projection / safety-shell behavior**, i.e., higher unsafe-action rejection, better contact-preserving correction, and local `PCST` improvement before any stronger MEGA reading is entertained.

This row-native discipline keeps D07 honest in a way generic family subtraction alone cannot. A row should only be promoted beyond its native frontier if the residual gain continues to improve the guidance tuple outside the mechanism’s most natural metric footprint. Concretely, if `B2` mostly wins in early-window acceleration, it should freeze at warm-start support; if `B3` mainly preserves safe behavior into W2/W3 without clearly flattening the drift-decoupling curve, it should freeze at retention bridge; if `B4` mainly rejects unsafe commands or smooths local contact correction without surviving free-moving-base disturbance, it should freeze at projection / safety-shell support. Only residual gains that continue past these native frontiers deserve to speak in the stronger MEGA language.

### 3.35 Fixed-Flight versus Free-Moving-Base Survival before the Strongest MEGA Wording

A central risk in D07 is that a row may look convincing only because the flight base is artificially easy. Under fixed or heavily stabilized flight conditions, local compensation, projection, or planner-side routes may appear to preserve moving-base guidance even when their gains collapse once true base-arm coupling is restored. For this reason, the paper should introduce an explicit **boundary-survival split** between `FFB` (fixed-flight boundary survival) and `FMB` (free-moving-base survival). The former asks whether the row remains useful under an artificially stabilized or bounded-base setting; the latter asks whether the residual still survives once coupled disturbance and arm-induced back-reaction are restored.

Under the MEGA reading rule, passing `FFB` is necessary but never sufficient. If a row only survives at `FFB` but fails at `FMB`, then the honest interpretation is still planner-side support, projection-side support, or local-arm support under simplified base conditions. Only rows whose residual guidance gain survives both `FFB` and `FMB`, while continuing to flatten `drift_amp \mapsto BDEE`, maintain `g^{\dagger}=W3`, and reduce `PGFR`, may continue upward toward the strongest wording that the manipulator itself acts as a **high-frequency stabilizer over low-frequency UAV drift**. This distinction is especially important for D07 because the owner-side core hypothesis is explicitly about preserving precision after base motion is no longer negligible, not merely under sanitized hover conditions.

### 3.36 Boundary-Focused Deployment-Risk Coverage as the Final Pre-Claim Gate

Even after support-family subtraction and `FFB/FMB` survival, D07 still needs one final pre-claim gate: **boundary-focused deployment-risk coverage**. The local ROBOGATE evidence makes clear that a row can look persuasive on mean metrics while hiding a very narrow usable transition zone. For aerial manipulation, this risk is amplified because moving-base contact performance can collapse abruptly as drift amplitude, payload shift, contact-timing offset, state-estimation degradation, or actuation latency crosses a narrow threshold. A method that looks strong in easy interiors but fails sharply near the boundary should not yet occupy the strongest MEGA headline.

We therefore require a two-stage boundary audit before the strongest claim is allowed. First, each candidate row receives a coarse disturbance sweep over the main D07 axes. Second, the apparent success-transition region is refined with boundary-focused probing to estimate how wide or brittle the usable region actually is. Under the MEGA reading rule, a row that only wins on average but reveals a narrow boundary-transition width, an obvious collapse axis, or poor payload/latency robustness should freeze below the strongest headline even if its aggregate guidance tuple is attractive. Only residual gains that survive support subtraction, pass both `FFB` and `FMB`, and remain acceptable under boundary-focused risk coverage are allowed to support the claim that the arm is the decisive high-frequency stabilizer over bounded UAV drift.

### 3.37 Current Evidence-Consistent Next Step after Re-reading the Local D07 Support Families

After re-reading the current local D07 anchor set, the most evidence-consistent next step is not to widen the method stack again, but to tighten the execution contract around the first trustworthy four-row bundle. The draft already has enough support families—`PRS / ROS / VSS / TPS / MCS / WMPS / TFOS / KAS / MTS`, and now `OTS` plus row-native support frontiers—that adding more candidate modules would mostly dilute attribution. What is still missing is a clean proof that any surviving gain is not just faster optimization, better retention, safer projection, easier transfer, or cleaner verification, but a genuine arm-side residual that remains alive when the base is allowed to move and the disturbance boundary is probed aggressively.

This means the next D07 writing priority should stay firmly guidance-first and subtraction-heavy. Every future row should first declare its row-native ceiling, then report whether the residual survives `FFB`, then whether it survives `FMB`, and finally whether it remains usable under ROBOGATE-style boundary-focused risk coverage. Only if the residual still flattens `drift_amp \mapsto BDEE`, improves `CET/PCST`, keeps `g^{\dagger}=W3`, and lowers payload-bearing `PGFR` after all these gates does the paper earn the right to keep the strongest MEGA wording. Until then, the honest and valuable outcome may still be acceleration, retention bridge, projection support, or transfer-side support—but not yet the full high-frequency stabilizer claim.

### 3.33 Cross-Embodiment Support Families should be Matched out before Promoting the MEGA Claim

A further constraint emerges when D07 is read together with nearby cross-embodiment transfer work. **Any2Any** [REF: 2605.23733] shows that action-space alignment can substantially reduce embodiment-translation friction, while **X-DiffVLA** [REF: 2605.25044] shows that morphology-tree organization can improve action-head reuse and embodiment-family adaptation. These are valuable results, but under the MEGA title their first honest role is still support-side: they can explain why a policy transfers more smoothly across manipulators or embodiment families, not why an aerial manipulator on a drifting base has become a stronger high-frequency end-effector stabilizer.

We therefore explicitly freeze them as two additional support families. Any2Any-style methods are treated as **kinematic-alignment support (KAS)** and are granted first-pass rights to explain lower action-space mismatch, easier embodiment translation, and faster target-side adaptation. X-DiffVLA-style methods are treated as **morphology-tree support (MTS)** and are granted first-pass rights to explain morphology reuse, shared action-head organization, and lower embodiment-family adaptation cost. Under the D07 reading rule, neither KAS nor MTS should be promoted into the MEGA headline unless the same row still improves `BDEE`, `CET`, `PCST`, and `PGFR` after embodiment-translation and morphology-reuse advantages have been matched out.

This distinction matters because aerial manipulation can otherwise inherit an attractive but incorrect narrative: if a row transfers more smoothly from one arm configuration or robot family to another, it is rhetorically tempting to upgrade that result into evidence that the manipulator itself guides better under moving-base disturbance. D07 should explicitly resist this move. Cleaner embodiment reuse is valuable, but the honest ceiling remains **cross-embodiment support** unless residual gains continue to flatten `drift_amp \mapsto BDEE`, survive into `g^{\dagger}=W3`, and reduce payload-bearing failure after KAS/MTS subtraction.

### 3.34 Seven-Family Support Audit and High-Frequency Stabilizer Residual

With KAS and MTS made explicit, the pre-promotion audit for D07 should now be read as a **seven-family support subtraction**: **platform/reproducibility support (PRS)**, **reward-origin support (ROS)**, **verification-shell support (VSS)**, **transfer-protocol support (TPS)**, **multi-task curriculum shell support (MCS)**, **kinematic-alignment support (KAS)**, and **morphology-tree support (MTS)**. The goal is not to make MEGA impossible to justify, but to ensure that the strongest wording is reserved for the narrow residual that truly belongs to the moving-base aerial-arm interface.

Operationally, we extend the submission-facing accounting tuple to

\[
\Lambda^{++}_{row} = (PRS,\; ROS,\; VSS,\; TPS,\; MCS,\; KAS,\; MTS,\; BDEE,\; CET,\; PCST,\; PGFR,\; g^{+},\; g^{\dagger},\; blocker),
\]

where `KAS` records whether embodiment-translation and action-space alignment remain sufficient explanations, and `MTS` records whether morphology-family reuse and shared action-head structure remain sufficient explanations. Under this rule, a row that mainly improves because it is easier to port across embodiments or easier to organize across morphology families should freeze below the MEGA claim, even if its aggregate results look deployment-promising. Only rows whose residual survives all seven support families and still improve the moving-base guidance tuple deserve to occupy the **mechanical-arm high-frequency stabilizer** narrative.

### 3.35 Current Evidence-Consistent Next Step after Re-reading TD-MPC2, DIAL-MPC, Any2Any, and X-DiffVLA

After re-reading the strongest recent local D07 anchors across planner-side and transfer-side families—**TD-MPC2** [REF: 2310.16828], **DIAL-MPC** [REF: 2409.15610], **Any2Any** [REF: 2605.23733], and **X-DiffVLA** [REF: 2605.25044]—the most evidence-consistent next step is still not to widen the method stack, but to ask one narrower residual question: **after planner-side, optimizer-side, curriculum-side, protocol-side, and cross-embodiment support explanations are all matched out, does the arm still act as the decisive high-frequency stabilizer over bounded UAV drift?**

This directly sharpens D07's next submission-facing priority. Stronger world-model planning or training-free full-order optimization should be granted first-pass rights to explain smoother local proposals and better short-horizon disturbance handling. KAS and MTS should be granted first-pass rights to explain lower adaptation cost, embodiment translation, and morphology reuse. Only if a row still keeps a flatter `drift_amp \mapsto BDEE` curve, pushes `g^{\dagger}` to `W3`, and lowers `PGFR` after those explanations are removed may the paper continue to promote the **MEGA** headline. In other words, D07 now has an even clearer burden of proof: it must show not merely that the system is easier to train, easier to transfer, or easier to optimize, but that the manipulator itself remains the precision-preserving residual layer once those easier stories have already been consumed.

### 3.36 Family-to-Metric Rights before Promoting the High-Frequency Stabilizer Claim

The current D07 bundle is now rich enough that support families should be granted explicit **metric-first rights** before any stronger title promotion is attempted. In our reading rule, each family is first allowed to explain the guidance metric it most naturally improves. A row may only be promoted beyond that family's ceiling if it still retains a residual gain on the rest of the MEGA tuple after matched subtraction.

Concretely, **warm-start / acceleration families** are first allowed to explain earlier `W1` stabilization, lower time-to-threshold, and better `WSR/PRS`; **retention families** such as BC-to-Q are first allowed to explain `W2/W3` survivability and limited `CET` improvement; **projection / safety-shell families** are first allowed to explain `unsafe-action rejection`, `contact-preserving correction`, and local `PCST` gains; **platform / protocol / curriculum families** are first allowed to explain variance reduction, transfer readiness, and adaptation-cost reduction. None of these first-order wins are yet sufficient for the MEGA headline. Only residual gains that continue to flatten `drift_amp \mapsto BDEE`, push `g^{\dagger}` to `W3`, and reduce payload-bearing `PGFR` after these family-native metrics are matched deserve to be interpreted as evidence that the arm itself is acting as the decisive high-frequency stabilizer.

### 3.34 Fixed-Flight versus Free-Moving-Base Survival as the Last Boundary Test

A final boundary is required before D07 can honestly use its strongest wording. Some rows may look highly persuasive under an artificially stabilized or fixed-flight base, yet collapse once base-arm coupling is restored. This means D07 must explicitly separate **fixed-flight boundary survival (FFB)** from **free-moving-base survival (FMB)** instead of reporting only one aggregate disturbance result.

Under the MEGA reading rule, a row that survives only under `FFB` should default to planner-side, projection-side, or local-arm support, because its gain may still depend on an unrealistically quiet base. Only rows whose residual survives both `FFB` and `FMB`, while still keeping a flatter `drift_amp \mapsto BDEE` curve and stronger `CET/PCST/PGFR`, are eligible for the strongest interpretation that the manipulator is functioning as a high-frequency stabilizer over bounded UAV drift. This final boundary test is important because it prevents D07 from confusing **compensation over an easy base** with **compensation that remains valid under the coupled moving-base conditions the paper actually claims to solve**.


This means the immediate write-up priority remains unchanged in spirit but sharper in execution: the first D07 result bundle must be read as a competition between support-family explanations and the arm-side high-frequency stabilizer residual. In practical terms, the paper should continue to privilege rows that can still flatten `drift_amp \mapsto BDEE`, improve `CET/PCST`, and lower `PGFR` after PRS/ROS/VSS/TPS/MCS subtraction, while treating platform choice and curriculum quality as explicitly logged but title-limited advantages. That keeps MEGA honest: it is allowed to stand on a strong simulator and a strong curriculum shell, but it is not allowed to mistake either one for the arm-side guidance mechanism itself.

### 3.33 Fixed-Base-to-Moving-Base Transfer as the Most Actionable D07 Middle Route

Re-reading **TD-MPC2** [REF: 2310.16828] and **DIAL-MPC** [REF: 2409.15610] together with the existing D07 archive suggests that the most actionable next method refinement is not another planner family, but a stricter **fixed-base-to-moving-base transfer route**. TD-MPC2 tells us that a strong latent-dynamics planner can already absorb a substantial part of continuous-control difficulty, while DIAL-MPC tells us that short-horizon full-order refinement can improve tracking even without policy pretraining. For D07, this implies that directly claiming a moving-base high-frequency stabilizer from scratch is too entangled: planner strength and optimizer strength can already explain a large fraction of the gain before the aerial-base coupling question is honestly isolated.

We therefore insert an intermediate route into the MEGA method reading rule: first learn or validate manipulation competence in a **fixed-base arm regime**, then promote the same controller family into a **bounded moving-base regime** where UAV drift is gradually injected as an external disturbance ladder. Under this design, the core question becomes sharper: after fixed-base competence is established, does the same arm policy family retain precise end-effector guidance once low-frequency base drift is introduced, or does performance collapse in a way that must be rescued by planner-side or optimizer-side support? This route is especially useful for D07 because it turns "mechanical arm as high-frequency stabilizer" into a measurable delta over an already competent manipulator baseline, instead of an all-at-once whole-system slogan.

### 3.34 Bounded-Drift Curriculum and Explicit Promotion Boundary

Concretely, we define a bounded-drift curriculum with four promotion stages: **F0 fixed base**, **F1 low-amplitude scripted drift**, **F2 stochastic bounded drift with contact**, and **F3 payload-bearing bounded drift**. The controller is only allowed to promote from one stage to the next if it preserves end-effector guidance quality without borrowing its entire gain from WMPS, TFOS, TPS, or MCS support families. In other words, D07 should no longer ask whether a row is simply "good at aerial manipulation"; it should ask whether guidance quality degrades gracefully as the task moves from fixed-base manipulation to genuinely moving-base disturbance.

This curriculum creates an explicit promotion boundary for the paper's title logic. A row that succeeds in **F0→F1** mainly proves manipulator competence plus mild disturbance tolerance. A row that survives **F2** begins to support local moving-base compensation. A row that remains stable in **F3** while still improving `BDEE/CET/PCST/PGFR` after support-family subtraction becomes credible evidence for the MEGA high-frequency stabilizer claim. By writing this progression directly into the method section, D07 gains a cleaner bridge between the strong local planner baselines and the owner's desired narrative of "机械臂作为高频稳定器".

### 3.35 Evaluation Contract against Fixed-Flight and Free-Flying Strong Baselines

The local D07 archive also contains a useful cautionary anchor outside the exact aerial-manipulator setting: on-orbit free-flying multi-arm control [REF: 2603.23182] shows that once a floating base, arm coordination, and disturbance-aware planning are coupled together, aggregate task success can hide whether the dominant gain comes from better global path planning or better local manipulation recovery. For MEGA, this is a helpful warning signal. A row that improves mainly because the planner produces cleaner approach trajectories or reduces global base-motion burden should not be over-read as evidence that the manipulator itself has become the decisive high-frequency stabilizer.

We therefore add one more evaluation contract for D07: every MEGA-facing result should be compared not only against scratch / warm-start / gating / projection families, but also against a **fixed-flight simplification boundary** and a **free-flying coupled-motion boundary**. The fixed-flight boundary asks whether the claimed gain survives when the base is artificially stabilized and only manipulator correction remains; the free-flying boundary asks whether the same gain still survives when the base is allowed to move and arm actions can feed back into global platform dynamics. If the advantage disappears at the fixed-flight boundary, the row is likely over-dependent on planner-side or base-side stabilization. If it disappears at the free-flying boundary, the row is likely still a local manipulator controller rather than a true moving-base guidance mechanism.

Operationally, this means D07 should attach two additional tags to the first trustworthy bundle: `FFB` (fixed-flight boundary survival) and `FMB` (free-moving-base survival). A result that keeps flatter `drift_amp \mapsto BDEE` only under fixed-flight but collapses once base-arm coupling is restored must be frozen below the MEGA headline, typically as projection-side or planner-side support. By contrast, residual gains that survive both boundaries and still improve `CET/PCST/PGFR` give much cleaner evidence that the manipulator is genuinely acting as a high-frequency compensator over bounded but non-vanishing base motion.

### 3.36 Immediate Missing Gate after the Current Local Re-read

After re-reading the local D07 anchors and the current owner notes, the most under-specified gap is no longer support-family enumeration, but **whether the claimed residual actually survives once artificial base simplifications are removed**. A row may look persuasive if evaluated only under a fixed or strongly stabilized base, because the remaining problem collapses toward local manipulator correction. Under the MEGA hypothesis, however, the core scientific burden is harsher: the arm-side mechanism should still preserve guidance quality when base-arm coupling is restored and the platform continues to drift within a bounded but non-vanishing envelope.

This means the next D07 writing and experiment synchronization should explicitly elevate `FFB` and `FMB` into first-class gates. Support-family subtraction remains necessary, but not sufficient. Even a row that survives `PRS / ROS / VSS / TPS / MCS / WMPS / TFOS / KAS / MTS` matching should still be frozen below the strongest MEGA wording if it only works in fixed-flight conditions and collapses once free-moving-base coupling returns. The most evidence-consistent next step is therefore to make **boundary survival after support subtraction** the immediate criterion for promotion: only rows that keep flatter `drift_amp \mapsto BDEE`, hold `g^{\dagger}=W3`, and reduce `PGFR` under both `FFB` and `FMB` should be allowed to speak as mechanical-arm high-frequency stabilizers.

### 3.37 Off-Policy Throughput as a Missing Optimization-Side Challenger

One optimization-side challenger is still missing from the current D07 draft: **FlashSAC** [REF: 2604.04539]. Its value for D07 is not that it already solves moving-base aerial manipulation, but that it offers a strong alternative explanation for early wins on high-dimensional robotic control: a row may look much better simply because **off-policy replay, critic stabilization, and throughput scaling** make learning faster and more stable. For aerial-arm control, this is especially relevant because a branch may reach useful recovery behaviors earlier, or preserve more rare disturbed-contact fragments, without the underlying arm-side residual guidance mechanism having fundamentally improved.

We therefore freeze FlashSAC-style methods as an **off-policy throughput support (OTS)** family. By default, OTS is granted first-pass rights to explain lower time-to-threshold, higher replay efficiency, better critic stability, and reduced seed variance under matched task definitions. Under the MEGA reading rule, however, OTS should not inherit controller-side credit. Unless an off-policy row still flattens `drift_amp \mapsto BDEE`, improves `CET/PCST`, survives to `g^{\dagger}=W3`, and lowers payload-bearing `PGFR` after optimization-throughput advantages are matched out, the honest ceiling remains **training-efficiency support** rather than moving-base end-effector guidance.

### 3.38 The Immediate Next Step is to Reserve an OTS Ceiling without Expanding the Bundle Yet

The practical consequence is intentionally modest. D07 does **not** need to expand the first executable bundle immediately to include a FlashSAC branch; that would dilute the current four-row verdict objective. But the paper draft should already reserve a clear interpretation slot for off-policy challengers, so that if a future branch wins on speed or stability, the result is not over-promoted by default. In other words, the right near-term move is to add an explicit OTS ceiling to the writing and logging contract now, while keeping the active experimental bundle frozen.

This keeps the MEGA title honest in advance. If an off-policy variant later arrives and improves only optimization throughput, D07 can still report a strong and useful result—but it must be written as **off-policy throughput support** unless a residual moving-base guidance gain survives after OTS subtraction. That is the most evidence-consistent addition the current local re-read supports.

## 3.16 World-Model and Training-Free Planning Families as Strong but Bounded Control Baselines

Recent local anchors indicate that D07 must compare itself not only against warm-start, retention, projection, and hybrid-inner-loop families, but also against **strong planner-side control families** that may already absorb a large portion of disturbance handling. TD-MPC2 [REF: 2310.16828] represents a **world-model planning support (WMPS)** route: a latent-dynamics planner with scalable cross-task and cross-embodiment capability, where part of the robustness gain may come from stronger prediction and planning rather than from a genuinely better arm-side high-frequency compensator. DIAL-MPC [REF: 2409.15610] represents a complementary **training-free optimizer support (TFOS)** route: diffusion-style annealed sampling can improve full-order control refinement without relying on policy learning at all, meaning that some late-window gains may be explained by a better online optimizer rather than a better learned guidance interface.

For MEGA, these two families play an important but deliberately bounded role. We do not treat them as direct substitutes for moving-base end-effector guidance, because neither route by itself proves that the manipulator is acting as the dominant high-frequency stabilizer over residual UAV drift. Instead, they define a harder subtraction baseline: if a D07 row wins only because planner-side prediction is stronger or online refinement is more powerful, then the honest interpretation is still **planner-side disturbance handling support**. Only the residual gain that remains after matched comparison against WMPS/TFOS-style explanations is allowed to support the stronger claim that arm-side local compensation still matters even when latent planning or training-free full-order optimization are already strong.

### 3.17 Planner-Side versus Arm-Side Disturbance Attribution

The introduction of WMPS and TFOS forces a sharper attribution split inside D07. Let `G_total` denote the observed improvement in moving-base manipulation quality. We conceptually decompose it into a planner-side term `G_plan`, which captures gains from stronger latent prediction, longer-horizon proposal quality, or full-order online refinement, and an arm-side residual term `G_arm`, which captures gains from fast local compensation that directly suppress end-effector error under ongoing base drift. The key MEGA question is not whether `G_total > 0`, but whether `G_arm` remains meaningfully positive after the strongest available planner-side explanation has already been accounted for.

This distinction is especially important because planner-side families can look deceptively similar to guidance-bearing improvement in aggregate metrics. A better world-model planner may reduce approach error and improve early contact timing, while a stronger training-free optimizer may stabilize short-horizon correction under disturbance; however, either effect can inflate whole-task success without demonstrating that the manipulator itself continues to flatten the `drift_amp -> BDEE` curve once the base keeps moving. We therefore treat WMPS and TFOS as mandatory subtraction families in later experiments: only when a row still improves late-window guidance, post-contact stability, and payload-bearing survival beyond these planner-side routes should it be promoted toward the high-frequency stabilizer narrative.

### 3.18 Cross-Embodiment Routes as Support-Side Transfer Structures

A second hard confound comes from **cross-embodiment transfer families** that may reduce adaptation cost, unify action interfaces, or improve morphology reuse without yet proving stronger moving-base guidance. Any2Any [REF: 2605.23733] is treated here as a **kinematic-alignment support (KAS)** family: it is most naturally read as improving embodiment translation, action-interface alignment, and target-side adaptation efficiency across robots with different kinematic structures. X-DiffVLA [REF: 2605.25044] is treated as a **morphology-tree support (MTS)** family: it suggests that embodiment hierarchy and morphology-aware action sharing can improve reuse and transfer without requiring each platform to relearn everything from scratch.

For D07, these routes are valuable because aerial-arm research may eventually need to transfer policies or guidance priors across different arm geometries, payload setups, or vehicle-manipulator pairings. But under the MEGA title, their role must remain conservative. By default, KAS/MTS-style gains are allowed to explain **embodiment translation gain**, **morphology reuse gain**, **shared action-head gain**, and **adaptation-cost reduction**. They are not yet allowed to explain why a manipulator on a drifting aerial base functions as a decisive high-frequency stabilizer. Only if their gains survive matched subtraction and continue to flatten `drift_amp -> BDEE`, improve `CET/PCST`, and reduce payload-bearing `PGFR` under free-moving-base evaluation should they be allowed to move beyond transfer-structure support.

### 3.19 Current Evidence-Consistent Next Step

Given the current local evidence base, the next honest step for D07 is not to widen the method stack again, but to force the first real bundle to answer a narrower question: **after subtracting planner-side strength (WMPS/TFOS) and transfer-structure strength (KAS/MTS), is there still a residual arm-side gain that survives into W3 and payload-bearing disturbance?** This means the first deployment-facing bundle should log not only aggregate success, but also which explanation family most naturally owns each improvement before any title upgrade is attempted.

Concretely, D07 now treats warm-start, retention, reward shaping, projection, verification shell, copilot hygiene, world-model planning, training-free optimization, kinematic alignment, and morphology-tree reuse as separate support-side ceilings. The MEGA narrative is allowed to advance only if the remaining row-wise residual still passes matched-compute comparison, boundary-survival checks, and guidance-first metrics under free-moving-base disturbance. In other words, the paper should no longer ask merely whether the stack performs well, but whether the **mechanical arm remains the dominant explanation for flattened guidance error after every weaker support family has been given first right of explanation**.

## 4. Experiments

To make the above routing executable, the next real training bundle must log the minimal fields required to reconstruct `\mathcal{G}_{row}` and its promotion blockers. In addition to general success and phase statistics, each row should export **base-drift-conditioned end-effector pose error**, **time-to-first-stable-contact**, **time-from-contact-to-stabilized-hold**, **payload-bearing guidance failure episodes**, the earliest and latest guidance-positive windows, and the highest disturbance source boundary reached. Without these fields, D07 cannot honestly decide whether a module improved moving-base guidance or merely improved a support-side subsystem.

This logging rule also closes the gap between `experiments.md` and `PAPER.md`. The four-row bundle is now not just a procedural execution plan but a title-routing instrument: the logs determine which route is allowed to speak in the paper. If future rounds add semantic-state overlays, shared-autonomy assistance, reward-model variants, or whole-body extensions, those modules must still first pass through the same guidance-centric logging gate before they are allowed to affect the MEGA narrative.

### 4.33 Fixed-Base-to-Moving-Base Promotion Table

To make the new middle route executable, the first submission-facing D07 table should explicitly include a **fixed-base-to-moving-base promotion axis** in addition to the usual guidance tuple. Each row must report whether it succeeds at **F0 fixed base**, **F1 low-amplitude drift**, **F2 bounded stochastic drift with contact**, and **F3 payload-bearing bounded drift**, together with the corresponding `BDEE`, `CET`, `PCST`, and `PGFR` values. This turns the paper's main question into a visible ladder: not "which method has the highest aggregate success," but **which controller family preserves guidance quality as the base is progressively allowed to move**.

Under this table, support families get natural early stopping ceilings. WMPS and TFOS are first allowed to explain why a row promotes more smoothly from F0 to F1 or F2 through stronger planning or refinement. TPS and MCS are first allowed to explain why fixed-base competence is learned faster or transfers more smoothly into bounded-drift regimes. Only if a row still preserves flatter `drift_amp \mapsto BDEE` and stable `CET/PCST/PGFR` all the way to F3 after these explanations are matched out may the paper continue to route it upward toward the MEGA headline.

### 4.34 Minimal Go/No-Go Rule for the High-Frequency Stabilizer Claim

The most important consequence of the new promotion ladder is a simpler first-round go/no-go rule. **Go**: if one row establishes fixed-base competence, survives bounded moving-base disturbance through F2 or F3, and still retains residual guidance gains after matched subtraction against planner-side, optimizer-side, transfer-side, and curriculum-side support. **No-go**: if the apparent gain disappears once support families are matched, or if promotion stalls at F0/F1 and never reaches contact-heavy moving-base slices. In that case, the paper must be honestly narrowed to fixed-base acceleration, transfer protocol, or stronger planning support instead of claiming a moving-base high-frequency stabilizer.

This rule is deliberately harsh because D07 is already mature enough to be hurt more by story inflation than by modest scope. A negative result is still useful if it proves that the current gain is planner-dominated, transfer-dominated, or fixed-base only. The real mistake would be to skip the fixed-base-to-moving-base promotion audit and over-read a cleaner training shell as evidence that the arm has already become the decisive high-frequency compensation layer over bounded UAV drift.

### 4.13 Guidance-Adjacent Transfer Support from Find-the-Fruit-Style Decomposition

Find the Fruit [REF: 2505.16547] is useful to D07 not because it already solves moving-base aerial-arm guidance, but because it demonstrates a decomposition pattern that can make zero-shot transfer look stronger than the underlying controller interface really is. Its staged stack—interaction planning, obstacle-aware approach, and compliant low-level execution—shows that a row may gain robustness by **decomposition quality** rather than by a better guidance mechanism. For MEGA, this means such evidence should be treated as **guidance-adjacent transfer support** unless it directly improves `BDEE / CET / PCST / PGFR` under moving-base disturbance.

We therefore freeze Find-the-Fruit-style evidence into a narrow interpretation: it can justify stronger environment decomposition, uncertainty exposure, or structured transfer protocols, but it cannot by itself occupy the title route of moving-base end-effector guidance. If a future D07 row appears much better simply because scene decomposition or obstacle clearing became cleaner, the honest reading is still transfer-side support unless the row also proves better late-window guidance retention under base drift and payload-bearing perturbation.

### 4.6 Minimal Verification Questions

Before any deployability claim is made, the first round must answer three concrete questions. **Q1:** does warm-start initialization still help after matched compute and seed averaging, or is the advantage only an early optimization transient? **Q2:** do relation-aware local priors improve final-approach and grasp stability under D2/D3 disturbance, instead of merely smoothing trajectories in simulation? **Q3:** does the hybrid controller retain its advantage after sim-to-real bridge cost, risk coverage checks, and maintenance overhead are all accounted for?

These questions define the default routing of the paper after the first experimental round. If only Q1 is positive, D07 should be written as a **data-gated acceleration recipe**. If Q2 is positive and Q3 remains inconclusive, the draft should narrow to **relation-aware local compensation under coupled disturbance**, optionally with perception bridging as a supporting module. Only when Q3 is also positive, together with acceptable `SARR`, `STG`, and `NMC`, may D07 be promoted to a **deployable hybrid aerial-arm control** narrative.

### 4.7 Human-in-the-Loop Data Quality Ablation

Because D07 may rely on teleoperation or shared-autonomy demonstrations for warm start, we include a compact ablation on demonstration quality rather than assuming all demonstrations are equally useful. We compare **raw teleoperation warm start**, **offline-refined teleoperation warm start**, and **shared-autonomy-assisted warm start**, where the last condition uses a sequence-level copilot to denoise or complete unstable action chunks before policy initialization. The core question is whether better human-guided data primarily improves convergence speed, disturbance robustness, or online recoverability.

This ablation is interpreted conservatively. If shared-autonomy assistance only improves warm-start efficiency, its effect is counted as a stronger data-shaping recipe rather than a controller innovation. If it materially improves D2/D3 contact robustness or reduces catastrophic recovery failures when kept online, then it becomes evidence that D07 may need a bounded copilot layer at deployment time. This experiment is important because it prevents D07 from conflating three distinct claims: having better demonstrations, having a better autonomous controller, and having a better human-machine shared control interface.

### 4.8 Intervention-Hygiene versus Controller-Interface Attribution

Recent local evidence from **DiSCo** [REF: 2603.22787] and **Hand-in-the-Loop** [REF: 2605.15157] suggests that human correction quality itself can become a dominant source of apparent improvement. We therefore split human-in-the-loop benefit into two explicitly different components: **intervention-hygiene gain**, which captures smoother, more learnable, and lower-jitter corrective trajectories, and **controller-interface gain**, which captures genuine improvement in disturbance-window execution even after correction quality is matched. Concretely, each human-assisted row is summarized by `H=(IHG, CIG, OCR, TJR, W2R, W3R)`, where `OCR` denotes online correction rate, `TJR` denotes takeover jitter ratio, and `W2R/W3R` measure whether contact-stage and post-contact recovery remain improved once raw correction noise is normalized.

This split is necessary because a method may look stronger simply because users hand over cleaner trajectories, not because the autonomous aerial-arm interface has fundamentally improved. If a row mainly reduces `TJR` and improves early convergence while matched-noise W2/W3 robustness remains unchanged, D07 must route that result to **data shaping / bounded assistance hygiene** rather than to deployable hybrid control. Only when `CIG` remains positive after correction smoothness is matched is the method allowed to support a stronger controller-facing claim. In this way, D07 forces DiSCo-style sequence copilot gains to declare whether they are helping the learner, the operator, or the deployed controller.

### 4.9 Reward-, Retention-, and Projection-Origin Accountability

The current D07 stack contains at least three non-equivalent routes for improving late-window behavior: **reward-origin recovery shaping** motivated by ARM [REF: 2604.03037], **retention-origin BC-to-Q stability** motivated by Q2RL [REF: 2605.05172], and **projection-origin safety support** motivated by hierarchical RL plus analytic QP projection [REF: 2605.03363]. We therefore require every positive W2/W3 result to declare which origin family explains it first. Each row is logged as `R_or=(AER, RCR, PRJ, RET, w_or^+, s_or^\dagger)`, where `AER` measures advantage-style reward credit concentration, `RCR` measures recovery-consistency retention after disturbance, `PRJ` measures safety projection reliance, `RET` measures behavior-retention support, `w_or^+` is the first improved window, and `s_or^\dagger` is the highest disturbance-source boundary survived.

This accounting rule blocks a common overclaiming path in aerial manipulation: a late-window improvement caused by denser reward credit, anti-forgetting retention, or analytic safety projection gets rhetorically upgraded into a stronger autonomous control interface. If a row wins only because reward credit is better shaped, the honest story is still reward-origin support; if it wins only because BC behavior is preserved online, it is a retention bridge; if it wins only because unsafe task-space proposals are projected back into a feasible set, it is a safety-shell result. Only rows that remain positive after these weaker origin explanations are explicitly matched out may be promoted toward deployable hybrid aerial-arm control.

### 4.10 Guidance-Centric Logging Contract for the Next D07 Bundle

To make the current MEGA title test executable, the next real D07 bundle must log a minimal set of guidance-centric fields rather than only generic success or reward curves. For every row `r`, we require the compact tuple
\[
\mathcal{G}_{row}(r)=(\Delta BDEE,\; \Delta CET,\; \Delta PCST,\; \Delta PGFR,\; g^{+},\; g^{\dagger})
\]
together with the existing dominant-credit and disturbance-boundary tags. Here `\Delta BDEE` is the moving-base end-effector error reduction under drift, `\Delta CET` is the contact-establishment speedup, `\Delta PCST` is the post-contact stabilization improvement, and `\Delta PGFR` is the reduction in payload-bearing guidance failure. The tuple must be paired with `\kappa_dom` (dominant credit family), `s^{\dagger}` (highest disturbance source survived), and a binary `guidance_bearing` flag indicating whether the row remains title-eligible after matched weaker-family checks.

This contract is intentionally narrow. Q2RL-style gating, Hand-in-the-Loop intervention blending [REF: 2605.15157], Find-the-Fruit-style environment decomposition [REF: 2505.16547], and Isaac-Lab-native infrastructure discipline [REF: 2511.04831] may all improve the stack in useful ways, but none is allowed to influence the MEGA title unless the same row also moves `\mathcal{G}_{row}` in a late window. In practice, this means the log schema must expose **base-drift-conditioned end-effector pose error**, **time-to-first-stable-contact**, **time-from-contact-to-stabilized-hold**, **payload-bearing guidance failure episodes**, and the earliest/latest windows in which the gains remain stable.

### 4.11 Support-Route Mapping for Local Anchors under Matched Guidance Audits

The current local D07 anchor set implies a concrete guidance-first interpretation map for the next bundle. **Isaac Lab** [REF: 2511.04831] is frozen as an **infrastructure and protocol substrate**: cleaner resets, disturbance slicers, and unified logging may improve reproducibility, but they are not themselves moving-base guidance evidence. **Hand-in-the-Loop** [REF: 2605.15157] is frozen as an **intervention-hygiene and data-shaping route** whose primary expected benefit is smoother takeover trajectories and cleaner corrective data; unless that benefit lowers `BDEE`, shortens `CET`, or improves `PCST / PGFR` under disturbance, it remains guidance-adjacent. **Find the Fruit** [REF: 2505.16547] is frozen as an **environment-decomposition and structured sim-to-real route**: stronger occlusion handling, compliant preparation, and uncertainty exposure are scientifically valuable, but they are still support-side unless they survive as better moving-base guidance under cluttered or contact-preparation-heavy aerial-arm slices.

This matched mapping matters because D07 now contains enough positive support routes that title inflation can happen even without a stronger controller interface. A row that mainly benefits from infrastructure cleanliness, smoother human correction, or cleaner environment decomposition is still useful, but its honest ceiling is **reproducibility support**, **bounded-assistance support**, or **decomposition support** rather than MEGA-level moving-base guidance. The first result table and paragraph-level interpretation should therefore route these anchors through the same `\mathcal{G}_{row}` audit before allowing any title-bearing language.

### 4.14 Guidance-Bearing Method Freeze after Re-reading Hand-in-the-Loop and Find-the-Fruit

After re-reading the local anchors **Hand-in-the-Loop** [REF: 2605.15157] and **Find the Fruit** [REF: 2505.16547], the D07 method section should become even stricter about what is allowed to speak in the MEGA title. Hand-in-the-Loop-style intervention blending is best interpreted as an **intervention-hygiene and correction-trajectory smoothing route**: it can reduce takeover discontinuity, make behavior-cloning initialization cleaner, and potentially improve early approach stability, but these gains are still guidance-adjacent unless they also reduce `BDEE`, shorten disturbed `CET`, or survive into `PCST/PGFR` under moving-base payload-bearing slices. Likewise, Find-the-Fruit-style decomposition is best interpreted as an **environment-decomposition and uncertainty-exposure route**: stronger preparation, compliant contact staging, and broader randomization can make policies look more robust, yet they still do not qualify as MEGA evidence unless they improve late-window moving-base guidance itself.

We therefore freeze a narrower method interpretation for the next D07 write-up. The title-bearing core is restricted to three coupled claims only: **base-motion-aware end-effector guidance**, **contact-stage agility**, and **post-contact stabilization under payload-bearing disturbance**. Warm start, BC-to-Q gating, intervention smoothing, semantic relation overlays, and decomposition-aware training may remain inside the stack, but by default they are attached to the paper as support routes whose role is to explain *why the core guidance mechanism may become easier to learn or easier to verify*, not to substitute for that mechanism. This freeze is useful because it prevents D07 from mistaking cleaner data, cleaner decomposition, or cleaner simulator protocols for genuine moving-base guidance innovation.

### 4.15 Guidance-First Main Table Bridge from experiments.md to Submission Routing

To make the above freeze operational, the paper should directly mirror the guidance-first logging schema already frozen in `experiments.md`. For each row in the first trustworthy bundle, the submission-facing main table should minimally include `Method`, `ΔBDEE`, `ΔCET`, `ΔPCST`, `ΔPGFR`, `g^{+}`, `g^{dagger}`, `s^{dagger}`, `SupportFamilyExplanation`, `GuidanceSubtractedCeiling`, and `PromotionBlocker`. The first four columns answer whether the row improved moving-base guidance quality; the next three answer *how late* and *under how strong a disturbance source* the gain survived; the last three answer what the row is still honestly allowed to claim after weaker-family explanations are subtracted.

This bridge matters because D07 already has enough support-side positives that a generic result paragraph would almost certainly overclaim. With the table fields above, each row must explicitly declare whether it is still just `acceleration`, `retention bridge`, `projection-only safety-shell support`, `reward-shaped recovery`, `copilot/decomposition support`, or a true `deployable hybrid` route. In other words, the main table no longer merely reports performance; it performs the first round of title arbitration. That is exactly the level of writing discipline the MEGA framing needs before any real training bundle arrives.

### 4.16 Guidance-First Family-Matched Submission Schema under the Current D07 Anchor Set

The current local D07 anchor set already provides four strong support-family explanations that can dominate first-round gains even when the moving-base end-effector interface itself has not fundamentally improved. **Find the Fruit** [REF: 2505.16547] can explain stronger uncertainty exposure, occlusion handling, and task decomposition quality; **Q2RL** [REF: 2605.05172] can explain BC retention and safer offline-to-online carryover; **Reactive Dexterous Grasping** [REF: 2605.03363] can explain analytic task-space projection and safer controller-consumable action execution; and **DiSCo** [REF: 2603.22787] can explain intervention hygiene and sequence-level correction quality. D07 therefore treats these four routes as the minimum family-matched explanation set that must be defeated before a row is allowed to speak in MEGA title language.

Reviewer-facing readout is frozen into the following order: first inspect `ΔBDEE` and `ΔCET` to test whether the row only improves early guidance acquisition; then inspect `ΔPCST` to test whether the row actually stabilizes post-contact behavior; finally inspect `ΔPGFR`, `g^{\dagger}`, and `s^{\dagger}` to determine whether the gain survives into payload-bearing moving-base disturbance. Only after these fields are read may the row be assigned a `SupportFamilyExplanation`, a `GuidanceSubtractedCeiling`, and a `PromotionBlocker`. This ordering matters because support-family gains can look impressive in aggregate success while still stopping at early or mid-stage guidance. Under the current D07 policy, a row that does not survive this readout sequence cannot be promoted by prose alone.

### 4.17 Family-Specific Guidance Ceilings for Decomposition, Retention, Projection, and Copilot Support

The four local anchor families are each assigned a default ceiling before any row is allowed to compete for a deployable-hybrid interpretation. **Find-the-Fruit-style decomposition support** may explain stronger task preparation, occlusion robustness, or indirect improvements in `ΔBDEE` and `ΔCET`, but unless its gain clearly propagates to `ΔPGFR` under `g^{\dagger}=W3` and `s^{\dagger}=s_p`, it remains frozen as **decomposition support** rather than guidance-bearing control evidence. **Q2RL-style retention support** may extend `g^{\dagger}` or preserve W2 carryover by reducing catastrophic forgetting, but if `ΔPGFR` remains weak, the honest ceiling is still **retention bridge**. **Reactive-projection support** may improve `ΔPCST` or unsafe-action rejection through controller-side safety enforcement, but if this gain does not survive payload-bearing disturbance, it remains **safety-shell support**. **DiSCo-style copilot support** may smooth intervention trajectories and improve early alignment or contact preparation, yet if it does not continue into W3 with a positive payload-bearing guidance signal, it remains **intervention-hygiene support**.

These ceilings are intentionally conservative because D07 now treats support-family improvement as valuable but title-limited evidence. A row is not allowed to inherit MEGA-level interpretation merely because it wins in one or two local metrics. Instead, it must first cross the ceiling of the weakest still-valid family explanation. This makes the main table more honest: a late-window gain is not automatically a hybrid-control gain, and a sequence-level correction gain is not automatically a stronger moving-base guidance interface.

### 4.18 Current Evidence-Consistent Promotion Discipline after Re-reading Find the Fruit, Q2RL, Reactive Projection, and DiSCo

After re-reading the current local D07 anchors, the most evidence-consistent promotion discipline is now straightforward: D07 may only promote a row from support-family status to **deployable hybrid** if four conditions hold simultaneously—`ΔPCST` is positive, `ΔPGFR` is positive, `g^{\dagger}=W3`, and `s^{\dagger}=s_p`. If any one of these conditions fails, the title path must immediately collapse to the weakest still-valid explanation among **acceleration**, **retention bridge**, **safety-shell support**, **reward-shaped recovery**, or **decomposition support**. This rule is intentionally harsher than a conventional aggregate-score reading because the current evidence base already shows how easily aerial-arm results can look deployment-ready for the wrong reason.

### 4.19 Infrastructure-Level Verification Support should be Frozen below Guidance Claims

**MobileManiBench** [REF: 2602.05233] clarifies another route that can materially improve D07 without yet strengthening the moving-base guidance interface itself: **verification infrastructure quality**. By automating multi-view RGB-D/state/instruction generation and standardizing verification scenarios in Isaac Sim, it can sharply increase task coverage, failure-mode coverage, and cross-method comparability. For D07, this is extremely useful because the current first-round bundle risks over-interpreting small gains when the validation shell is too narrow or too hand-crafted. A stronger benchmark substrate can reveal whether warm-start, retention gating, analytic projection, or hybrid control actually differ in disturbance-heavy slices.

However, MobileManiBench-style gains still live primarily on the route `better verification coverage → earlier exposure of brittle rows → more reliable method ranking`, not on the route `better moving-base end-effector guidance under disturbance`. We therefore freeze this family as **verification-support / infrastructure-support** by default. It is allowed to explain stronger failure discovery, more reproducible comparisons, and better coverage of whole-body versus local-compensation routes, but it is not allowed to promote a row into MEGA-level narrative unless the same row also improves `ΔBDEE`, `ΔCET`, `ΔPCST`, or `ΔPGFR` under matched guidance audits.

### 4.20 Verification-Coverage Audit before Promoting Hybrid or Whole-Body Narratives

To prevent validation-shell improvements from being confused with controller gains, we add a minimal verification-coverage audit for the next D07 bundle. Each row should now carry a compact tuple
\[
V_{row}=(C_{task},\; C_{fail},\; C_{view},\; T_{build},\; \kappa_{verify})
\]
where `C_{task}` measures task coverage, `C_{fail}` measures failure-mode coverage, `C_{view}` measures observation/viewpoint coverage, `T_{build}` measures benchmark construction overhead, and `\kappa_{verify}` records whether the row's apparent gain might still be explained by a better validation shell rather than a better controller interface. This tuple is not title-bearing by itself; it serves as a subtraction audit.

Operationally, a row that looks stronger only after moving from a hand-crafted validation subset to a richer MobileManiBench-style verification shell should first be interpreted as **verification-sensitivity exposed**, not as a stronger deployable controller. Only residual gains that remain after matched verification coverage—and still improve the guidance tuple `\mathcal{G}_{row}` through late windows and payload-bearing disturbance—may continue toward **deployable hybrid** or **whole-body** narratives. This keeps D07 honest: better validation is essential, but it is evidence hygiene, not automatic control innovation.


The deeper methodological point is that D07 is no longer asking only whether a module helps, but whether it helps *late enough*, under *hard enough disturbance*, and after *enough weaker explanations have been removed*. Under this standard, a row that mainly preserves BC competence, smooths human correction, or enforces safer action projection may still be a very useful component of the final system, but it is not yet a MEGA result. The paper therefore treats these routes as support-side assets whose value must first be subtracted before any remaining residual is allowed to occupy the title-level claim.

### 4.19 Mobile-Manipulation Benchmark Infrastructure as a Verification-Support Family

The newly re-read **MobileManiBench** [REF: 2602.05233] adds an additional D07 support family that should be written into the experiments section before any stronger guidance claim is made. Its main contribution is not a better aerial-arm controller, but a cleaner **verification substrate**: automatic task generation, multi-view RGB-D and segmentation export, standardized robot-state logging, and a lower-cost route to constructing diversified validation episodes inside Isaac Sim. For D07, this means benchmark infrastructure itself can improve the *visibility* of guidance failures—especially warm-start overfitting, retention-only carryover, or projection-only safety improvements—without yet improving the moving-base end-effector interface.

We therefore freeze MobileManiBench into a narrow interpretation: it is a **verification-support family** whose default allowed gains are better task coverage, cleaner disturbance-slice construction, and stronger failure-mode surfacing under the same four-row bundle. In reviewer-facing language, this family may justify why a D07 table can more reliably expose whether a row only wins in `ΔBDEE/ΔCET` or actually survives into `ΔPCST/ΔPGFR`, but it cannot by itself occupy the MEGA title route. If a future row looks better simply because the validation shell is broader or better instrumented, the honest explanation remains **benchmark/verification support** until guidance-bearing metrics themselves move.

### 4.20 Verification-First Logging Contract after MobileManiBench Re-read

MobileManiBench also sharpens what the next D07 bundle should log before any real title routing is attempted. Beyond the existing guidance tuple `\mathcal{G}_{row}`, the verification layer should explicitly export **task-coverage diversity**, **failure-mode bucket counts**, and **disturbance-slice completeness** for each row, so that stronger-looking rows can be checked against the possibility that they merely benefited from easier or narrower validation shells. Concretely, the next bundle should pair `ΔBDEE / ΔCET / ΔPCST / ΔPGFR / g^{+} / g^{\dagger} / s^{\dagger}` with at least one verification tuple
\[
\mathcal{V}_{row}=(C_{task},\; C_{fail},\; C_{slice}),
\]
where `C_{task}` measures coverage over generated task variants, `C_{fail}` measures surfaced failure-mode diversity, and `C_{slice}` measures whether `D0-D3` and payload-bearing slices are all actually represented in the validation shell.

This addition is useful because D07 now has two distinct ways to look stronger without truly improving moving-base guidance: by accumulating support-family modules, or by improving the validation substrate itself. The first must be subtracted through `SupportFamilyExplanation`; the second must be subtracted through `\mathcal{V}_{row}`. Only rows that remain strong after both subtractions are allowed to speak in MEGA-level language. In that sense, MobileManiBench does not widen the controller claim—it hardens the verification discipline needed to keep that claim honest.

### 4.21 Verification-Subtracted Guidance Routing after Re-reading Isaac Lab and Sim-Real Co-Training

After re-reading **Isaac Lab** [REF: 2511.04831], **RL-Based Sim-Real Co-Training for VLA** [REF: 2602.12628], and **MobileManiBench** [REF: 2602.05233], D07 now needs one tighter promotion boundary in the experiments section: a row is not allowed to claim moving-base guidance progress unless its guidance tuple remains positive **after subtracting verification- and protocol-side advantages**. Isaac Lab can improve reset discipline, disturbance injection consistency, and logging reproducibility; sim-real co-training can improve transfer stability through better data anchoring and curriculum continuity; MobileManiBench can improve task diversity and failure visibility. All three are scientifically valuable, but under the MEGA title they are still support-side unless the same row preserves `ΔBDEE / ΔCET / ΔPCST / ΔPGFR` after those protocol advantages are explicitly accounted for.

We therefore define a verification-subtracted routing view
\[
\mathcal{G}^{\ominus V}_{row} = \big(\Delta BDEE,\; \Delta CET,\; \Delta PCST,\; \Delta PGFR\big) - \psi\big(\mathcal{V}_{row},\; \kappa_{infra},\; \kappa_{cotr}\big),
\]
where `\mathcal{V}_{row}` is the verification tuple introduced above, `\kappa_{infra}` denotes infrastructure-side explanation strength from Isaac-Lab-native protocol hygiene, and `\kappa_{cotr}` denotes sim-real co-training-side explanation strength from data anchoring and training continuity. The purpose of this view is not to estimate a precise numeric residual, but to force every positive row to answer a narrower reviewer-facing question: **if cleaner infrastructure, broader validation, and better sim-real anchoring are treated as the first explanation, what guidance-bearing gain is still left?** If the residual is strongest only in early `ΔBDEE / ΔCET`, the row remains acceleration / protocol support; if it survives through `ΔPCST`, `ΔPGFR`, `g^{\dagger}=W3`, and `s^{\dagger}=s_p`, only then may it compete for a deployable-hybrid route.

### 4.22 Current Evidence-Consistent Next Step after the Verification-Support Re-read

This re-read further confirms that D07 does not currently lack controller families; it lacks a first trustworthy table that can jointly report **guidance gain**, **support-family explanation**, and **verification subtraction** on the same four-row substrate. The next evidence-consistent step is therefore to keep new-paper ingestion at zero and require the next real bundle to emit, in addition to `\mathcal{G}_{row}`, at least one compact verification-routing block: `VerificationSupportFamily`, `TaskCoverageDiversity`, `FailureModeCoverage`, `DisturbanceSliceCompleteness`, and `VerificationSubtractedCeiling`. Until that block is present, rows improved by cleaner resets, broader generated validation, or better sim-real anchoring should remain frozen as protocol- or verification-strengthened support routes rather than title-bearing moving-base guidance evidence.


### 4.23 Environment-Decomposition and Retention Routes under the Drift-Decoupling Hypothesis

The latest local anchors further clarify how **Find the Fruit** [REF: 2505.16547] and **Q2RL** [REF: 2605.05172] should be read once D07 is centered on the high-frequency-stabilizer hypothesis. Find-the-Fruit-style decomposition can improve uncertainty exposure, obstacle-aware preparation, and occlusion-resilient approach quality; Q2RL-style BC-to-Q gating can preserve competent warm-start behavior and reduce catastrophic forgetting during online adaptation. Both are valuable, but under the MEGA framing they are still upstream support routes unless they measurably improve the **drift-amplitude decoupling curve**, i.e., unless end-effector error remains bounded as base-drift amplitude increases and the gain survives into contact and post-contact windows.

We therefore assign them explicit ceilings under the decoupling view. Decomposition support may explain stronger early `\Delta BDEE` or `\Delta CET` by making the task easier to stage, but unless it also reduces `\Delta PCST`, lowers `\Delta PGFR`, and flattens the drift-to-error slope, it remains an **environment-decomposition support** explanation rather than a moving-base guidance result. Likewise, retention support may extend `g^{\dagger}` from `W1` toward `W2`, but unless the same row preserves bounded end-effector error at larger drift amplitudes and survives payload-bearing disturbance, it remains an **anti-forgetting bridge** rather than a deployable high-frequency compensation route.

### 4.24 Drift-Amplitude-Decoupling as the First Submission-Facing Main Table

Following the owner's 2026-05-26 direction, the first submission-facing D07 table should no longer be organized primarily around generic family wins. Instead, the table should be centered on a **drift-amplitude-decoupling readout**: for each row, we report the mean and variance of end-effector error at increasing drift amplitudes together with the earliest and latest windows in which the row still preserves guidance quality. In compact form, each row should expose
\[
\mathcal{D}_{row} = (\alpha_{drift},\; BDEE(\alpha),\; \partial BDEE / \partial \alpha,\; g^{+},\; g^{\dagger},\; s^{\dagger}),
\]
where `\alpha_{drift}` denotes the disturbance amplitude ladder and `\partial BDEE / \partial \alpha` is the empirical slope of guidance degradation under stronger base motion.

This table is the most direct test of the MEGA physical claim. If a row mainly improves optimization, decomposition, retention, or projection, but the slope `\partial BDEE / \partial \alpha` remains steep, then the row has not actually decoupled end-effector precision from base drift; it has only improved a weaker upstream factor. Conversely, if the slope remains shallow and positive gains survive through `W2/W3` and toward `s_p`, then the row has much stronger evidence for the statement that the manipulator is acting as a high-frequency stabilizer over a lower-frequency moving base.

### 4.25 Failure-Bucket Readout for the High-Frequency Stabilizer Hypothesis

The decoupling table alone is still insufficient, because a flatter curve can fail for very different reasons. We therefore pair every row with a three-bucket failure readout aligned with the owner's latest physical framing: **arm agility deficit**, **local-state-quality deficit**, and **arm-induced back-reaction**. The first bucket asks whether the manipulator simply cannot move fast enough or far enough to compensate the measured drift envelope. The second asks whether the arm has enough authority but receives a poor local target/contact state, causing compensation in the wrong direction or at the wrong time. The third asks whether aggressive arm motion itself amplifies UAV disturbance enough to erase the apparent benefit.

This bucketization keeps D07 from reporting an opaque “hybrid worked / did not work” conclusion. A row with good retention and transfer support may still fail because its dominant residual error sits in `F_arm`; a row with strong projection safety may still fail because `F_state` dominates late contact windows; and a row with apparently strong local guidance may still fail because `F_react` grows under payload-bearing motion. Under the MEGA title, such outcomes should be written as physically informative negative results rather than generic training failures.

### 4.26 Contact-Representation and Critic-Guidance Families should be Subtracted before Promoting the High-Frequency Stabilizer Claim

The latest local D07 scan adds two nearby but still support-side routes that can make aerial-arm rows look stronger without yet proving the MEGA core claim. **Physics-Grounded Contact Representation** [REF: 2605.28812] shows that a better contact state can sharply improve sim-to-real dexterous manipulation by replacing coarse binary touch with a physics-meaningful center-of-pressure style representation. **CGPO** [REF: 2605.30056] shows that critic-guided diffusion RL can improve sample efficiency and preserve multimodality by injecting value information into the denoising trajectory. For D07, both are highly relevant: the first can improve `F_state` by making contact onset and post-contact state estimation cleaner, while the second can improve optimization speed or local recovery quality by making the policy search more sample-efficient and less mode-collapsed.

However, neither route is guidance-bearing by default. A contact-representation upgrade may reduce post-contact jitter or help W2/W3 stabilization simply because the local state becomes more truthful, not because the manipulator has become a stronger high-frequency stabilizer over low-frequency base drift. Likewise, critic-guided diffusion RL may improve convergence, reduce catastrophic exploration, or sharpen recovery actions without yet flattening the `drift_amp → BDEE` curve. We therefore freeze them as **contact-state support** and **critic-guided optimization support** families unless the same row still improves `\partial BDEE / \partial \alpha`, survives to `g^{\dagger}=W3`, and lowers `\Delta PGFR` after those weaker explanations are matched out.

### 4.27 Current Evidence-Consistent Freeze after Re-reading CoP Contact Representation and CGPO

Under the current evidence base, the first trustworthy D07 table should now explicitly allow two new support-family tags: `contact-state support` and `critic-guided optimization support`. Reviewer-facing readout should follow the same order as before, but with one sharper subtraction step: if a row improves mainly because contact state is cleaner, it should first be frozen as **local-state-quality improvement**; if it improves mainly because diffusion RL explores or recovers better under critic guidance, it should first be frozen as **optimization / recovery support**. Only residual gains that still survive after these subtractions—and still flatten the drift-decoupling slope while preserving late-window payload-bearing guidance—may compete for the stronger MEGA claim.

This addition is useful because it further tightens the owner’s physical framing. The paper is no longer asking only whether some learning stack or contact representation helps, but whether those helps are enough to establish the stronger statement that the **arm itself is acting as a high-frequency stabilizer** over the UAV’s low-frequency drift. If the answer is “the row mainly got a cleaner contact state” or “the row mainly trained more efficiently with critic guidance,” the honest route remains support-side, not title-bearing.

## References


## References

[1] Isaac Lab: A GPU-Accelerated Simulation Framework for Multi-Modal Robot Learning. arXiv:2511.04831, 2025.
[2] AGILE. arXiv:2603.20147, 2026.
[3] WHOLE-MoMa. arXiv:2604.12509, 2026.
[4] Global End-Effector Pose Control of an Underactuated Aerial Manipulator via Reinforcement Learning. arXiv:2512.21085, 2025.
[5] A Comparison of Reinforcement Learning and Optimal Control for Aerial Manipulators. arXiv:2604.12628, 2026.
[6] RL-Based Sim-Real Co-Training for VLA. arXiv:2602.12628, 2026.
[7] Squint: Fast Visual RL for Sim-to-Real Manipulation. arXiv:2602.21203, 2026.
[8] ViserDex: Visual Sim-to-Real for Robust Dexterous In-hand Reorientation. arXiv:2604.11138, 2026.
[9] RoTri-Diff: A Spatial Robot-Object Triadic Interaction-Guided Diffusion Model for Bimanual Manipulation. arXiv:2603.07165, 2026.
[10] Synthesis of Model Predictive Control and Reinforcement Learning: Survey and Classification. arXiv:2502.02133, 2025.
[11] RLBench: The Robot Learning Benchmark & Learning Environment. arXiv:1909.12271, 2019.
[12] Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning. arXiv:2603.24083, 2026.
[13] Q2RL. arXiv:2605.05172, 2026.
[14] Reactive Dexterous Grasping. arXiv:2605.03363, 2026.
[15] ARM: Advantage-Driven Reward Model for Robotic Manipulation. arXiv:2604.03037, 2026.
[16] Real-to-Sim-to-Real Shared Autonomy for Precision Manipulation. arXiv:2603.17016, 2026.
[17] DiSCo. arXiv:2603.22787, 2026.
[18] Find the Fruit: Zero-Shot Sim2Real RL for Occlusion-Aware Plant Manipulation. arXiv:2505.16547, 2025.
[19] Hand-in-the-Loop: Improving Dexterous VLA via Seamless Interventional Correction. arXiv:2605.15157, 2026.
[20] Robo-Dopamine: General Process Reward Modeling for High-Precision Robotic Manipulation. arXiv:2512.23703, 2025.




### 4.28 Contact-State and Critic-Guidance Families should be Frozen to the Drift-Ladder before MEGA Promotion

The latest local D07 re-read adds two nearby but still support-side families that can easily create attractive partial wins in the first four-row bundle. **Physics-Grounded Contact Representation** [REF: 2605.28812] strengthens the local-state side of aerial manipulation by replacing coarse contact signals with a more physically meaningful representation; for D07, its most natural value is not a new controller interface, but a cleaner explanation for why `F_state` might shrink during disturbed contact and post-contact phases. **CGPO** [REF: 2605.30056] strengthens the optimization side by injecting critic guidance into diffusion-policy RL; for D07, its most natural value is not immediate MEGA evidence, but a cleaner explanation for why sample efficiency, recovery smoothness, or exploration quality may improve without yet changing the core moving-base compensation interface.

We therefore freeze these two routes to the drift-guidance ladder before any stronger title claim is allowed. Contact-representation upgrades are only allowed to first explain improvements in `ΔCET` and `ΔPCST`, especially when the dominant failure bucket shifts from `F_state` toward a more bounded local-state regime. Critic-guidance routes are only allowed to first explain optimization-side or recovery-side support, and at most early guidance improvements such as smaller `ΔBDEE` variance or smoother W2 recovery. Neither family is allowed to promote a row toward MEGA unless the same row still flattens the `drift_amp → BDEE` slope, survives to `g^† = W3`, lowers `ΔPGFR`, and remains strong after subtracting the weaker explanations `contact-state support` or `critic-guided optimization support`.

### 4.29 Current Evidence-Consistent Next Step after Re-reading CoP Contact Representation and CGPO

Under the current evidence base, D07 does not need more nearby literature to justify another method branch. What it needs is one cleaner submission-facing accounting rule: if a row looks stronger after adding better contact representation or critic-guided diffusion optimization, the first table must explicitly report whether that strength is still best explained by **local-state-quality improvement** or **optimization / recovery support**, rather than by a stronger moving-base guidance interface. The next trustworthy bundle should therefore allow two additional support-family tags—`contact-state support` and `critic-guided optimization support`—inside `SupportFamilyExplanation`, and should only allow promotion beyond them when the same row still improves `ΔBDEE / ΔCET / ΔPCST / ΔPGFR`, survives the drift ladder through `W3`, and reduces payload-bearing guidance failure after matched subtraction.

This addition further tightens the owner's physical framing of D07. The central question is no longer merely whether cleaner contact state or better RL optimization helps, but whether those helps are enough to establish the stronger statement that the **arm itself acts as a high-frequency stabilizer over low-frequency UAV drift**. If the answer is still “the row mainly got a truer local contact state” or “the row mainly trained more efficiently with critic guidance,” then the honest route remains support-side rather than title-bearing.

## References

TMRL-style gains are especially easy to overread in D07 because wider exploration can make the finetuning curve look healthier long before the controller actually survives stronger disturbance windows. We therefore freeze a non-promotion rule: if a row improves exploration coverage or early recovery diversity but its strongest stable gain is still concentrated in W1, or still collapses before payload-bearing `s_p`, then the dominant interpretation must remain **exploration-side warm-start support** rather than retention-side robustness or deployable interface improvement. In particular, `\Omega_{expl}^{D07}` is not allowed to absorb Q2RL-style BC retention credit, Reactive-Dexterous-Grasping-style projection credit, or DSAM-style hybrid interface credit unless the same row also defeats the matched weaker family on the same W2/W3 and disturbance boundary.

This rule matters because D07 now has two superficially similar but conceptually different warm-start stories: **Q2RL preserves safe prior behavior during online improvement**, while **TMRL expands the exploration radius available during finetuning**. Both are useful, but they answer different questions. The first is about *not forgetting what already works*; the second is about *finding better behaviors outside the original manifold*. Unless TMRL-style exploration also survives late-window contact and post-contact disturbance with matched-family defeat, it must remain a support-side explanation attached to acceleration or exploration coverage, not a new controller-family headline.

### 4.72 Zero-Shot Reactive-Catching Route Freeze for DRIS-Style Dynamics Set Training

A second newly visible support line comes from zero-shot reactive catching with domain-randomized instance sets (DRIS) [REF: 2605.09789], which strengthens the argument that D07 should distinguish **dynamics-set robustness** from controller-interface robustness. DRIS does not primarily change the execution interface; instead, it exposes the policy to multiple randomized dynamics instances simultaneously so that action selection becomes robust to uncertainty sets rather than single sampled episodes. For aerial-arm control, the closest interpretation is not “new controller family,” but **training-time uncertainty set broadening** for highly reactive manipulation under modeling mismatch, especially in tasks where contact timing and rapid compensation matter.

We therefore define a dynamics-set route tag
\[
\Omega_{dris}^{D07}=(\delta_{set}, \delta_{react}, \delta_{zero}, w^+_{dris}, s^\dagger_{dris}),
\]
where `\delta_{set}` measures robustness gain attributable to multi-instance uncertainty exposure, `\delta_{react}` measures whether that gain survives in fast reactive windows rather than only in nominal pre-contact motion, `\delta_{zero}` measures whether the benefit remains under zero-shot transfer without extra real-world finetuning, and `(w^+_{dris}, s^\dagger_{dris})` record the earliest positive window and highest disturbance boundary where the gain remains visible. By default, gains routed through `\Omega_{dris}^{D07}` are classified as **uncertainty-set training support** and must not be promoted to deployable hybrid-control or retention-interface claims unless they also survive W2/W3 contact windows, payload-bearing disturbance, and matched-family defeat on the same substrate.

This freeze is important because DRIS-like zero-shot success can look deceptively close to D07’s deployment story while actually answering a narrower question: whether broader training-time uncertainty coverage produces a more reactive policy under modeling error. That is valuable, especially for disturbance-heavy aerial-arm control, but it is still not the same as proving BC retention, analytic projection, or coupled-base hybrid interface superiority. If the main win stays at the level of uncertainty-set exposure plus reactive zero-shot compensation, then D07 should write it as a **dynamics-set robustness bridge** rather than silently upgrading it into a full controller-route promotion.

### 4.73 Sequence-Copilot Retention Boundary under Disturbance-Localized Credit

The local D07 evidence base now also makes it necessary to freeze a cleaner boundary between **sequence-copilot support** and **retention or deployable-interface credit**. DiSCo [REF: 2603.22787] shows that diffusion sequence copilots can preserve high-level human intent while repairing temporally noisy action chunks, and this is directly relevant to aerial-arm demonstration collection or bounded online assistance. However, the main benefit of such a copilot may still lie in **trajectory denoising and intent-consistent data shaping**, not in proving that the final controller family itself has gained stronger retention or hybrid robustness.

We therefore introduce a sequence-copilot route tag
\[
\Omega_{cop}^{D07}=(\delta_{seq}, \delta_{intent}, \delta_{retain}, w^+_{cop}, s^\dagger_{cop}),
\]
where `\delta_{seq}` measures whether sequence-level action repair improves temporally coherent execution over noisy demonstrations or bounded assistance, `\delta_{intent}` measures whether user or demonstrator intent is preserved while low-level instability is reduced, `\delta_{retain}` measures whether any apparent gain actually survives as BC-to-Q-style retention across W2/W3 rather than only as cleaner initialization, and `(w^+_{cop}, s^\dagger_{cop})` record the earliest positive window and highest disturbance boundary where the copilot-linked gain remains visible. By default, gains routed through `\Omega_{cop}^{D07}` are classified as **sequence-level data-shaping or bounded-assistance support** and cannot be promoted to retention robustness, analytic projection, or deployable hybrid-control claims unless the same row also survives late-window contact, payload-bearing disturbance, and matched-family defeat on the same substrate.

This boundary matters because DiSCo-like sequence improvement can visually resemble “better online control” even when the real gain source is simply that the system started from cleaner, more intent-faithful trajectories. For D07, that is still useful—especially if aerial teleoperation suffers from delay, jitter, or underactuated compensation noise—but it answers a narrower question than Q2RL-style retention or Reactive-Dexterous-Grasping-style safety projection. If the strongest effect remains sequence repair with intent preservation, the paper should honestly write it as a **shared-autonomy data-shaping bridge** or **bounded copilot support layer**, not as proof that the core controller family has crossed into a stronger deployment-facing route.

### 4.74 Disturbance-Localized Credit Ledger for the First Real D07 Table

The current D07 draft already separates reward, retention, projection, exploration, dynamics-set robustness, and copilot support at the prose level, but the first real table still needs one tighter ledger that can be filled directly from logs without rhetorical drift. We therefore define a disturbance-localized credit ledger
\[
\Xi_{D07}=(\ell_{seg}, \ell_{ret}, \ell_{proj}, \ell_{rew}, \ell_{expl}, \ell_{dris}, \ell_{cop};\, W^\star, s^\star)
\]
where each `\ell_*` is a normalized credit share assigned to one explanation family, `W^\star \in \{W1,W2,W3\}` is the latest window whose positive gain remains after seed averaging, and `s^\star \in \{s_v,s_b,s_c,s_p\}` is the highest disturbance source that still survives under the same matched substrate. The accounting rule is simple but strict: the row title, paragraph lead, and abstract-facing summary may only name the single largest surviving credit share, while all other positive effects must remain subordinate diagnostics. In particular, a row is not allowed to write "hybrid" or "deployable" if the largest surviving credit still belongs to reward shaping, BC retention, projection-side safety, exploration-side warm-start support, dynamics-set uncertainty exposure, or sequence-copilot data shaping.

This ledger matters because D07 is now rich enough that multiple support routes can co-occur in the same late-window plot. A row may improve W2/W3 because it retained BC behavior longer, rejected unsafe actions more aggressively, explored outside the original manifold, or simply saw a broader randomized dynamics set during training. Those are all scientifically meaningful, but they are not interchangeable. By forcing every first-table row to emit `\Xi_{D07}`, D07 gains a final log-to-paper bridge: once the dominant credit share, latest surviving window, and highest disturbance source are known, the strongest honest claim becomes mechanically constrained rather than narratively negotiated.

### 4.75 Minimal Logging Fields for Filling the Credit Ledger

To make `\Xi_{D07}` directly executable, every row in the first real D07 bundle must export one compact logging block beyond the existing route and sim2sim tuples. For acceleration-side credit, log `time_to_thr`, `matched_compute_sr`, and `early_seed_var`; for retention-side credit, log `bc_anchor_sr`, `forgetting_gap`, and `late_window_keep_ratio`; for projection-side credit, log `unsafe_reject_delta`, `contact_preserve_delta`, and `post_contact_safe_hold`; for reward-side credit, log `recovery_credit_ratio`, `late_recovery_share`, and `advantage_explained_gain`; for exploration-side credit, log `novel_state_coverage`, `safe_explore_ratio`, and `late_window_explore_gain`; for dynamics-set credit, log `uncertainty_set_gain`, `zero_shot_gap`, and `reactive_window_gain`; for copilot-side credit, log `sequence_repair_gain`, `intent_preserve_score`, and `online_assist_dependence`. Each logging block must be indexed by the same disturbance-phase tags `(d_t, p_t)` so that credit can be localized to W1/W2/W3 and `s_v/s_b/s_c/s_p` rather than inferred from whole-episode averages.

The purpose of this section is not to bloat the experiment protocol, but to keep the first trustworthy table fillable with the smallest set of evidence that still respects the claim-routing discipline. If these fields are missing, the corresponding credit family is considered unverified and may not dominate the row narrative. This gives D07 a clean completion condition for the next real run: once the four-row bundle exports the route tuple, sim2sim tuple, and this minimal credit-ledger block, the paper can finally route itself from logs instead of intuition.

### 4.76 Unified Title-Routing Rule after Credit-Ledger Fill

After `\Xi_{D07}` is filled, D07 should route the title through a single deterministic rule rather than by holistic impression. If `\ell_{seg}` is dominant and the positive frontier stops before stable W2/W3 contact survival, the title freezes at **acceleration**. If `\ell_{ret}` is dominant and the row mainly preserves BC-consistent behavior across online updates, the title freezes at **retention bridge**. If `\ell_{proj}` is dominant and the strongest visible benefit is safer execution without payload-bearing W3 continuity, the title freezes at **safety-shell support**. If `\ell_{rew}` is dominant, the route freezes at **reward-shaped recovery**. Only when the dominant surviving share is no longer explainable by these weaker families, and the row still survives to late-window `s_p`, may the draft escalate toward a deployable hybrid title.

This routing rule matters because D07 is now much closer to a real first bundle than to a literature bottleneck. The next failure mode is no longer “not enough ideas,” but “too many partially positive routes at once.” By freezing title routing after the ledger is filled, the paper makes one last promise to itself: even if several support families are mildly positive, the title goes to the strongest honest surviving explanation, not to the most exciting composite story.

### 4.77 Minimal Log-to-Paragraph Contract for the First Trustworthy Bundle

To close the loop from raw execution to paper prose, every first-bundle row should generate a four-field paragraph seed: `(winner_row, \kappa_{dom}, W^\star, s^\star)`, where `winner_row` is the row identity within B1--B4, `\kappa_{dom}` is the dominant credit family induced by `\Xi_{D07}`, and `W^\star, s^\star` are the latest surviving window and disturbance source. The first sentence of the corresponding results paragraph must state the matched-compute or matched-substrate condition; the second sentence must state `winner_row` and `\kappa_{dom}`; the third sentence must state `W^\star` and `s^\star`; the fourth sentence must freeze the strongest honest title route. No extra sentence is allowed to silently promote the claim beyond what the four fields support.

This contract is intentionally narrow, but it is exactly what D07 now needs. The draft already has enough method richness, enough support routes, and enough local literature anchors. What it needs next is a way to stop once the first trustworthy evidence appears. With this log-to-paragraph contract, the first real bundle can immediately produce submission-shaped prose without reopening the argument about what kind of paper D07 wants to be.

### 4.78 Executable Log-Key Contract for the Four-Row Minimal Bundle

The current D07 workflow has already frozen the first executable bundle to four rows, but the paper still needs one last bridge from `experiments.md` into a log schema that can be parsed without interpretation drift. We therefore define a minimal executable key set
\[
\Lambda^{+}_{D07}=(winner\_row, w^{+}, s^{\dagger}, \tau_{title}, \tau_{credit}, family\_ctrl, compute\_match, payload\_slice, reset\_reason, route\_tag),
\]
where `winner_row` records which row among `B1/B2/B3/B4` survives family-control filtering, `w^{+}` records the earliest stable positive window, `s^{\dagger}` records the highest surviving disturbance source, `\tau_{title}` freezes the strongest honest title route, `\tau_{credit}` stores the dominant credit family inferred from `\Xi_{D07}`, `family_ctrl` stores the nearest defeated weaker family, `compute_match` records whether the row was evaluated under matched-compute or matched-substrate conditions, `payload_slice` indicates whether payload-bearing promotion was explicitly opened, `reset_reason` summarizes the dominant reset/failure cause, and `route_tag` records whether the row is currently read as acceleration, retention, projection, reward, base-locked compensation, or deployable hybrid support.

This contract is intentionally operational rather than conceptual. It does not introduce a new method component; instead, it forces the first trustworthy D07 bundle to emit exactly the fields that `experiments.md` already expects the runner to fill. The value of writing `\Lambda^{+}_{D07}` into the paper is that it prevents the draft from drifting away from the executable plan: if a future row cannot produce these keys, then it is not yet eligible to anchor the main table, paragraph seed, or title-routing decision.

### 4.79 Family-Matched Comparator and Promotion-Blocker Fields

To ensure that `\Lambda^{+}_{D07}` cannot be filled in a superficially positive but reviewer-unusable way, we add four reviewer-facing auxiliary fields that must accompany every first-table row: **Family-Matched Comparator**, **Closure Level**, **Promotion Ceiling**, and **Promotion Blocker**. The Family-Matched Comparator states which weaker explanatory family the current row is actually trying to defeat, e.g., planner-only acceleration for `B2`, retention-only support for `B3`, or projection-only support for `B4`. Closure Level states whether the row has only reached `W1`, has closed W2 contact establishment, or has closed late-window W3 stabilization under the current disturbance ladder. Promotion Ceiling states the strongest claim the row is currently allowed to inherit even if the raw score looks strong. Promotion Blocker states the unresolved reason the row may not yet be upgraded, such as missing payload-bearing survival, unresolved reward dominance, unmatched compute, or failure to beat the nearest weaker family on the same substrate.

These fields matter because D07 is already beyond the stage where more numbers automatically help. The real risk is that a row with one attractive late-window gain could still be missing the exact comparator or blocker information needed to interpret it honestly. By requiring these four fields, the paper aligns itself with the existing executable discipline in `experiments.md`: every row now has to say not only what improved, but also whom it was compared against, how far the evidence really closes the route, how high the claim is currently allowed to go, and what unresolved issue still prevents further promotion.

### 4.80 Current Evidence-Consistent Freeze after ARM Re-read

A local re-read of ARM [REF: 2604.03037] reinforces that D07 must keep reward-origin improvements on a separate reporting rail even when they visibly help W2/W3 behavior. ARM shows that long-horizon manipulation can look dramatically better once recovery-compatible reward credit is modeled correctly, especially in tasks with backtracking, correction, and re-contact. For D07, this means a row that suddenly looks more stable in the late windows may still be primarily a *reward-accountable recovery* result rather than a stronger controller-interface result. In the current evidence regime, ARM therefore remains a strong reason to demand `Promotion Blocker` visibility: unless the same row also defeats the matched retention- or projection-family explanation and reaches payload-bearing `s_p`, reward-origin gains must remain frozen below deployable hybrid control.

This freeze is useful because it keeps D07 from misreading the exact kind of partial success it is most likely to see first. Aerial-arm experiments are very likely to show some late-window recovery improvement before they show true coupled-disturbance interface superiority. Writing the ARM-based freeze directly into the experiments section ensures that the first real bundle can still be published honestly even if its best result turns out to be reward-accountable recovery rather than deployable hybrid control.

### 4.81 Moving-Base Guidance-Centered Promotion Rule

Following the updated MEGA title and owner guidance, the D07 evaluation must now explicitly prioritize **moving-base end-effector guidance quality** rather than letting generic retention, projection, or reward-side gains dominate the paper headline. We therefore introduce four guidance-facing metrics alongside `SR / SEG / STG / SARR / NMC`: **Base-Drift End-Effector Error (BDEE)**, **Contact Establishment Time (CET)**, **Post-Contact Stabilization Time (PCST)**, and **Payload-Bearing Guidance Failure Rate (PGFR)**. `BDEE` measures how far the end effector deviates from the intended contact trajectory under base drift; `CET` measures the time from disturbed final approach to stable contact formation; `PCST` measures how long the controller needs to settle after contact or payload acquisition; and `PGFR` measures how often the guidance stack loses stable end-effector-to-target alignment after payload-induced disturbance appears.

These metrics operationalize the owner requirement that D07 should center **base-motion-aware end-effector guidance / contact-stage agility / post-contact stabilization**. Any route whose gain appears only in optimization speed, reward shaping, BC retention, safety projection, or teleoperation-data hygiene, but does not improve `BDEE`, `CET`, `PCST`, or `PGFR`, must remain a **support route** rather than a title-bearing contribution. Under the MEGA framing, the paper’s main question is no longer merely “which stack performs best,” but “which stack most honestly improves moving-base guidance quality late enough and under hard enough disturbance to deserve the title.”

### 4.82 Guidance-Centered Title Routing for MEGA

To keep the MEGA narrative honest, we define a guidance-centered routing rule `\mathcal{G}_{MEGA}` on top of the existing family-credit ledger. A row is only allowed to influence the paper title when its dominant surviving gain also appears in at least one late-stage guidance metric: lower `BDEE` under moving-base tracking, lower `CET` during disturbed contact establishment, lower `PCST` during post-contact recovery, or lower `PGFR` under payload-bearing disturbance. Improvements confined to `SEG`, reward-origin recovery, retention stability, projection safety, sequence-copilot smoothing, or semantic-state disambiguation remain scientifically useful, but they are frozen as **support-side evidence** unless they measurably improve moving-base guidance.

This routing rule directly implements the owner note that the title no longer describes a generic aerial-arm stack, but specifically **Moving-base End-effector Guidance and Agility**. As a result, D07 may not promote a row to the main narrative merely because it trains faster, looks cleaner in simulation, or provides a tidier interface decomposition. The dominant test is narrower and more falsifiable: does the method make the end effector easier to guide on a moving base, faster to establish stable contact, and more agile in surviving post-contact disturbance?

### 4.83 Guidance-Coupled Re-Read of the Four-Row Bundle

Under this new title discipline, the first-round D07 bundle must be re-read through a guidance lens rather than only a family-control lens. **B1 scratch PPO** remains the weakest baseline and mainly tests whether any moving-base guidance improvement exists beyond nominal Isaac-Lab learning. **B2 BC warm-start PPO** is now kept only to test whether optimization acceleration also reduces `BDEE` or `CET`; if it improves convergence speed without moving these guidance metrics, it remains an acceleration-only support row. **B3 BC-to-Q gated PPO** must now prove that retention support preserves guidance continuity through `W2/W3`, rather than merely reusing safe behavior in `W1`. **B4 task-space RL + analytic safety projection** is allowed to count as a true hybrid candidate only if projection-led safety also improves `PCST` or lowers `PGFR` under payload-bearing disturbance.

This rewrite matters because the older four-row interpretation was still slightly too abstract: retention, projection, reward, and hybrid routes could all remain alive in the table without proving that they actually improved moving-base end-effector guidance. Under the MEGA title, that is no longer sufficient. Every surviving row must now declare not only its dominant credit family and disturbance boundary, but also **which guidance metric moved first and farthest**. In effect, the first bundle is no longer merely `family-control honest`; it must now be **guidance-coupled honest**.

### 4.84 Guidance-Localized Readout Contract

To make MEGA routing executable, we append a compact guidance tuple to every first-round row:
\[
\mathcal{G}_{row}=(\Delta BDEE,\; \Delta CET,\; \Delta PCST,\; \Delta PGFR,\; g^{+},\; g^{\dagger})
\]
where `g^{+}` denotes the earliest guidance metric that becomes stably positive and `g^{\dagger}` denotes the strongest late-stage guidance benefit that survives after matched-compute, matched-substrate, family-credit, and disturbance-boundary checks. A row may still report strong reward, retention, projection, or exploration-side support, but if `\mathcal{G}_{row}` remains effectively null, the row is frozen as support-side evidence and cannot occupy the title route.

This contract bridges the owner requirement with the existing D07 logging discipline. The paper already distinguishes reward-side, retention-side, projection-side, exploration-side, and hybrid-side credit; `\mathcal{G}_{row}` now asks whether any of those credits actually cash out as **moving-base end-effector guidance and agility**. That means the first real table can no longer stop at “which route won,” but must continue to “which route improved guidance, contact agility, and post-contact survival enough to deserve the MEGA narrative.”

### 4.85 Guidance-Bearing versus Guidance-Adjacent Freeze after Local Re-read

The current D07 anchor set is now strong enough to support a stricter MEGA-facing freeze rule. **Reactive Dexterous Grasping** and **DSAM-style hybrid stabilization** are provisionally treated as guidance-bearing families because they can directly alter how the end-effector is steered, stabilized, and kept contact-ready under moving-base disturbance. By contrast, **Q2RL**, **DiSCo**, **KG-M3PO**, and **Find the Fruit** are frozen as guidance-adjacent by default: they may improve retention, demonstration hygiene, semantic relation clarity, or environment decomposition quality, but none of those gains is allowed to occupy the MEGA title route unless it measurably improves moving-base guidance metrics.

This freeze rule is intentionally harsher than the earlier route-family bookkeeping. Under the MEGA title, it is no longer enough to say that a module helps the system somewhere in the pipeline. The paper must now ask whether the module actually reduces disturbed end-effector guidance error, speeds contact establishment, stabilizes post-contact motion, or lowers payload-bearing guidance failure. If not, the module remains support-side even if its local gain is large.

### 4.86 Next-Step Guidance-Centric Logging Gap

The next writing and experiment gap for D07 is therefore no longer “find more baselines,” but “make the bundle rows log MEGA-compatible evidence directly.” In particular, future rows should report at least `BDEE`, `CET`, `PCST`, `PGFR`, the earliest positive guidance window, the latest surviving guidance window, and the highest disturbance boundary crossed. Without these fields, D07 can still discuss support-side improvement, but it cannot honestly upgrade any row into a moving-base end-effector guidance claim.

This also gives the next heartbeat a concrete landing point: align `experiments.md` and the first trustworthy table around guidance-centric logs first, then decide whether BC-to-Q, projection, copilot, semantic-state, or decomposition routes remain merely guidance-adjacent or actually cross into guidance-bearing evidence.

### 4.87 Guidance-Metric-Aligned Main Table Contract after MEGA Re-read

To make the MEGA title executable at the first trustworthy result table, every D07 row must now expose a compact guidance-first schema rather than only family-level credit tuples. We therefore require the main table to report at least: `Method | ΔBDEE | ΔCET | ΔPCST | ΔPGFR | g^+ | g^\dagger | s^\dagger | Dominant Family | Weakest Honest Route | Promotion Blocker`. The first six columns answer the owner-mandated question directly—did the row actually improve **moving-base end-effector guidance**, **contact-stage agility**, or **payload-bearing post-contact stability**—while the last five columns preserve the existing D07 discipline that any positive row must still declare its highest disturbance boundary, dominant explanatory family, current honest route, and why stronger promotion is still blocked.

This table contract is intentionally stricter than the older route-only or window-only summaries. A row with strong `R_ret`, `R_proj`, or `R_rew` but near-zero `ΔBDEE / ΔCET / ΔPCST / ΔPGFR` is still useful, but it is now frozen as support-side evidence by table design rather than by later prose repair. In other words, the main table itself must already separate **guidance-bearing gain** from **guidance-adjacent support**, so that the MEGA narrative cannot be accidentally inflated by a row that is explanation-rich but guidance-poor.

### 4.88 Guidance-Metric-to-Family Routing under the Current D07 Anchor Set

The current local anchor set also suggests a more specific mapping from guidance metrics to honest family routes. Improvements concentrated in `ΔBDEE` with little late-window carryover should first be read as **base-motion-aware alignment support**, which may still come from perception bridge, semantic-state disambiguation, or cleaner warm starts. Improvements concentrated in `ΔCET` but not `ΔPCST / ΔPGFR` should first be read as **contact-entry agility support**, which may still be explained by BC retention, sequence copilot smoothing, or local geometric priors rather than a stronger deployment interface. Only rows that continue from `ΔCET` into positive `ΔPCST` and reduced `ΔPGFR`, while surviving to `s_p`, are allowed to compete for the stronger **deployable moving-base guidance** route.

This routing layer matters because D07 now has several nearby families that can all create plausible early positives without yet earning the title. Q2RL-style BC-to-Q gating may reduce `CET` by stabilizing early contact approach, Reactive Dexterous Grasping may reduce `PCST` through projection-mediated safe execution, DiSCo may clean demonstration trajectories enough to help `BDEE` or `CET`, and KG-M3PO-style relation state may sharpen alignment under drift. But unless these gains survive the full metric ladder `BDEE → CET → PCST / PGFR` together with late disturbance survival, the correct narrative remains **guidance-adjacent support with a named dominant family**, not MEGA-level control superiority.

### 4.89 Guidance-Oriented Method Routing after the Current D07 Anchor Re-read

Under the MEGA title, the method section also needs a sharper internal routing rule: every module should first answer which part of **moving-base end-effector guidance** it is expected to improve before it is allowed to speak in a stronger controller narrative. We therefore align the current D07 stack to three guidance-facing method threads: **base-motion-aware end-effector guidance**, **contact-stage agility**, and **post-contact stabilization under payload-bearing disturbance**. Warm-start, BC-to-Q gating, sequence copilot refinement, and semantic relation state are all support-side by default because they may improve optimization hygiene, alignment quality, or bounded correction quality without yet proving stronger late-window guidance. By contrast, analytic safety projection and DSAM-style hybrid stabilization remain the most plausible routes for directly changing `ΔPCST / ΔPGFR`, while relation-aware local geometric priors are the most plausible route for improving `ΔCET` without overclaiming whole-body autonomy.

This method-routing rule is useful because it prevents D07 from continuing to describe the stack as a generic aerial-arm pipeline after the title has already narrowed to MEGA. In practice, a module that only helps `ΔBDEE` should be written as **guidance preparation support**; a module that helps `ΔCET` but not `ΔPCST / ΔPGFR` should be written as **contact-entry agility support**; and only a route that maintains positive gains from alignment through contact and into payload-bearing post-contact survival can be elevated to a **deployable moving-base guidance mechanism**. That keeps Method aligned with the owner’s requirement that base-motion-aware guidance, contact-stage agility, and post-contact stabilization are the actual three pillars of the paper, rather than after-the-fact interpretations of a broad control stack.

### 4.90 Guidance-Ladder Experiment Freezing Rule for the Next D07 Bundle

The next D07 bundle should therefore be frozen around a guidance ladder rather than around generic route diversity. Concretely, every candidate row must now declare its earliest positive guidance metric (`BDEE` or `CET`), its latest surviving guidance metric (`PCST` or `PGFR` if any), and whether the gain crosses the full ladder `BDEE → CET → PCST / PGFR` under the same disturbance slice. Rows that stall at `BDEE` remain alignment-support rows; rows that stall at `CET` remain contact-agility-support rows; rows that survive into `PCST / PGFR` but fail to cross `s_p` remain local stabilization evidence; only rows that survive the full ladder and cross `s_p` are eligible for a deployable MEGA narrative.

This freezing rule gives the next heartbeat a concrete writing-and-experiment landing point without reopening the full method zoo. Instead of asking whether Q2RL, DiSCo, KG-M3PO, Find the Fruit, or projection layers are broadly useful, D07 now asks a narrower and more paper-productive question: **how far up the guidance ladder does each row really travel, and where does it honestly stop?** That question is exactly what the MEGA title needs, because it turns the first result table into a direct audit of moving-base guidance quality rather than a loose collection of support-side wins.

### 4.91 Guidance-First Main Table Columns after Re-reading Hand-in-the-Loop and Find-the-Fruit

After re-reading the current D07 anchor set, the first submission-facing main table should expose two additional columns that make support-side wins visibly non-title-bearing: **Support-Family Explanation** and **Guidance-Subtracted Ceiling**. The recommended compact schema is therefore `Method | ΔBDEE | ΔCET | ΔPCST | ΔPGFR | g^+ | g^† | Highest Disturbance Source | Support-Family Explanation | Guidance-Subtracted Ceiling | Promotion Blocker`. Here the `Support-Family Explanation` column should explicitly tag whether a row's strongest weaker explanation is **intervention hygiene**, **environment decomposition**, **retention bridge**, **analytic projection**, **reward density**, or **infrastructure hygiene**. The `Guidance-Subtracted Ceiling` column then states what claim remains after subtracting the observed guidance gain, e.g., `data-shaping support`, `decomposition support`, `retention support`, or `research-only hybrid`.

This small addition is valuable because Hand-in-the-Loop [REF: 2605.15157] and Find the Fruit [REF: 2505.16547] both make D07 more vulnerable to support-side overpromotion. A row can look much stronger simply because takeover jitter is lower, corrective data are smoother, or occlusion-aware task decomposition is cleaner, while the moving-base end-effector guidance mechanism itself has not materially advanced. By making the support-family explanation and the guidance-subtracted ceiling visible in the first main table, D07 forces reviewer-facing honesty at a glance: readers can immediately see not only what improved, but what the row is still *not yet allowed* to claim under the MEGA title.

### 4.92 Current Evidence-Consistent Next-Step Priority

Given the current local evidence, the most valuable next step is not adding another controller family, but translating the MEGA guidance ladder into directly loggable experiment fields inside `experiments.md`. Concretely, the next heartbeat should prioritize mapping `ΔBDEE / ΔCET / ΔPCST / ΔPGFR / g^+ / g^† / Highest Disturbance Source / Support-Family Explanation / Guidance-Subtracted Ceiling` into the execution plan so that the first real D07 bundle can produce a submission-ready table without post hoc reinterpretation. This keeps the research loop honest: experiment logs, main table columns, and title-routing logic all speak the same language.

Under this evidence-consistent freeze, no new paper ingestion is required this round. The current anchor coverage from **Q2RL / Reactive Dexterous Grasping / DiSCo / Hand-in-the-Loop / Find the Fruit / Isaac Lab** is already sufficient for the immediate writing task. What D07 lacks is not more literature names, but a cleaner bridge from guidance-centric metrics to title-bearing claims. Until that bridge is hard-coded in `experiments.md`, any additional method expansion would likely increase narrative noise more than scientific certainty.

### 4.93 Guidance-Bearing Ceiling after Re-reading HandITL, DiSCo, PRISM, and Find-the-Fruit

After re-reading the current local support-route anchors, the MEGA draft needs one stricter reviewer-facing rule: **human-correction quality, sequence-copilot smoothing, instruction-guided personalization, and environment decomposition are not allowed to speak in the title's voice unless they improve moving-base end-effector guidance itself.** Hand-in-the-Loop [REF: 2605.15157] clearly improves takeover hygiene, lowers gesture discontinuity, and turns cleaner interventions into better refinement data, but its primary gain family remains **intervention-hygiene support** unless it measurably reduces base-drift end-effector error or payload-bearing guidance failure in a moving-base aerial-arm setting. DiSCo [REF: 2603.22787] similarly remains a **sequence-copilot support route** by default: it can denoise temporally coherent action chunks and preserve human intent under noisy control, but unless those gains survive as `ΔBDEE`, `ΔCET`, `ΔPCST`, or `ΔPGFR` improvements under moving-base disturbance, it still does not earn a guidance-bearing interpretation.

PRISM [REF: 2603.05574] sharpens another nearby boundary. Its main value lies in **instruction-guided refinement** of imitation skills through language-conditioned reward shaping and human correction, which is highly relevant for fast adaptation and preference-aware manipulation. However, under the MEGA title this family is still guidance-adjacent unless it changes moving-base end-effector guidance quality rather than only improving task adaptation speed or personalization convenience. Find-the-Fruit [REF: 2505.16547] stays in a different support bucket: it is best interpreted as an **environment-decomposition support route**, where stronger uncertainty exposure, occlusion handling, and staged interaction structure can produce impressive sim-to-real results without yet proving a stronger disturbed guidance interface. Together, these four anchors force a conservative ceiling: support-side rows may explain data hygiene, instruction-conditioned refinement, sequence-level correction, or decomposition robustness, but they cannot occupy the MEGA title unless they first move the guidance tuple itself.

### 4.94 Guidance-First Submission Paragraph Contract

To keep the submission narrative aligned with the title, every future result paragraph in D07 should follow a **guidance-first contract** rather than a support-family-first contract. Concretely, the paragraph must first report the moving-base guidance tuple
\[
\mathcal{G}_{row}=(\Delta BDEE,\; \Delta CET,\; \Delta PCST,\; \Delta PGFR,\; g^{+},\; g^{\dagger},\; s^{\dagger}),
\]
where `ΔBDEE` is the change in base-drift end-effector error, `ΔCET` is the change in contact-establishment time, `ΔPCST` is the change in post-contact stabilization time, `ΔPGFR` is the change in payload-bearing guidance failure rate, `g^{+}` is the earliest positive guidance window, `g^{\dagger}` is the latest surviving guidance window, and `s^{\dagger}` is the highest disturbance source boundary reached. Only after these numbers are stated is the paragraph allowed to explain *why* the gain appeared, e.g., through intervention hygiene, sequence copilot support, BC-to-Q retention, analytic projection, or environment decomposition.

This ordering is not cosmetic; it is the main anti-overclaim device for MEGA. A row that first sounds impressive because it uses HandITL-style blending, DiSCo-style action inpainting, PRISM-style instruction refinement, or Find-the-Fruit-style staged decomposition must still be demoted immediately if the guidance tuple does not move in the late disturbed windows. In reviewer-facing writing, such a row should therefore be described as **support-family evidence with a guidance ceiling**, not as direct evidence for moving-base end-effector guidance and agility. This contract ensures that support-side modules remain valuable but title-bounded, and it gives D07 a clean path from first real bundle logs to a submission-ready, guidance-honest Results section.

### 4.95 Guidance-Led Experiment Logging Schema for the First Trustworthy MEGA Bundle

To make the current writing discipline executable, the first trustworthy D07 bundle should log a fixed guidance-led schema rather than rely on ad hoc post hoc interpretation. For every row, the experiment log should minimally expose `BDEE_mean / BDEE_p95`, `CET_mean`, `PCST_mean`, `PGFR`, `g^+`, `g^\dagger`, `s^\dagger`, together with `DominantFamily`, `SupportFamilyExplanation`, `GuidanceSubtractedCeiling`, and `PromotionBlocker`. This schema turns the current MEGA routing from prose into an actual data contract: a row is first judged by whether it improves moving-base guidance quality, then by which weaker family still explains the gain, and only then by whether the title route can be upgraded.

This logging schema also clarifies how the current local anchors should be read operationally. **Reactive Dexterous Grasping** and **DSAM-style hybrid stabilization** are expected to compete mainly on `PCST` and `PGFR`, because their strongest plausible value is in safer contact execution and post-contact stabilization under disturbance. **Q2RL-style BC-to-Q gating** is expected to show up first in `CET` continuity or in reduced failure spikes before contact, but unless it continues into `PCST / PGFR` and survives to `s_p`, it remains a retention-support route. **DiSCo / HandITL / PRISM** should primarily be audited through whether cleaner demonstrations or bounded corrections materially move `BDEE` or `CET`; otherwise, they stay frozen as intervention- or refinement-side support. Writing this directly into the experiments section prevents the next bundle from mixing guidance-bearing and guidance-adjacent evidence in the same table row.

### 4.96 Minimal Submission-Ready Result Paragraph Template under the Guidance Ladder

Once the first trustworthy bundle is available, each main result paragraph should follow a minimal three-sentence template. **Sentence 1** reports the guidance gain and boundary, e.g., *"Row B4 reduces `BDEE` by X%, shortens `CET` by Y%, and remains positive through `PCST` up to disturbance boundary `s_p`, indicating a late-window moving-base guidance benefit."* **Sentence 2** reports the dominant family and weaker explanation ceiling, e.g., *"The dominant explanatory family is analytic projection with hybrid stabilization, while matched retention and intervention-hygiene explanations no longer account for the observed late-window gain."* **Sentence 3** states the honest route, e.g., *"We therefore route this row to deployable moving-base guidance"* or *"we conservatively keep it at contact-agility support"*.

This template is intentionally rigid because D07 now has too many nearby support families that can produce superficially strong numbers. By forcing every paragraph to move in the order **guidance gain → explanatory family → honest route**, the paper stays aligned with the MEGA title and the owner's requirement that modules without true moving-base guidance improvement cannot occupy the main narrative. It also gives the next heartbeat a concrete downstream target: align `experiments.md` and future logs so that these three sentences can be filled automatically from the first real bundle.

### 4.97 Guidance-First Logging Bridge from experiments.md to the Submission-Facing Main Table

The current D07 draft already has a guidance-centered title contract, but it still lacks one final bridge between the executable experiment plan and the exact columns required by the first submission-facing main table. We therefore explicitly align the four-row bundle in `experiments.md` with a fixed MEGA guidance schema. For every row, the paper now requires the following columns: `Method`, `ΔBDEE`, `ΔCET`, `ΔPCST`, `ΔPGFR`, `g^{+}`, `g^{\dagger}`, `s^{\dagger}`, `SupportFamilyExplanation`, `GuidanceSubtractedCeiling`, and `PromotionBlocker`. This bridge matters because D07 should no longer let a route look stronger simply because it reports good retention, smoother correction, or safer projection in the abstract; it must show which part of moving-base guidance improved, how late that gain survived, and what still prevents promotion to the MEGA title route.

Under this bridge, the first table becomes mechanically tied to the four-row experiment logic rather than interpreted post hoc. **B2** is only allowed to claim acceleration unless `ΔBDEE` or `ΔCET` improves beyond matched-compute speedup; **B3** is only allowed to claim retention bridge unless `g^{\dagger}` reaches `W2/W3` and `ΔCET` or `ΔPGFR` also improves; **B4** is only allowed to claim deployable hybrid potential if `ΔPCST` and `ΔPGFR` remain positive through `s_p` after matched weaker-family defeat. Conversely, support-side rows such as copilot smoothing, semantic overlays, or decomposition-aware transfer are frozen as guidance-adjacent unless they move the same guidance tuple. This section therefore closes the loop from `experiments.md` to `PAPER.md`: the next real bundle can directly export the exact fields needed for an honest submission-ready table, and the paper can decide whether it has found a title-bearing mechanism or only a stronger support stack without reopening the interpretation by hand.


### 4.98 Guidance-Ceiling-Aware Experiment Bridge after the Current experiments.md Freeze

The current D07 workflow has now frozen a guidance-first execution contract in `experiments.md`, but `PAPER.md` still needs one last paragraph that explicitly binds those executable fields to the submission-facing interpretation discipline. We therefore define a compact bridge tuple
\[
\Phi^{MEGA}_{row}=(\Delta BDEE,\; \Delta CET,\; \Delta PCST,\; \Delta PGFR,\; g^{+},\; g^{\dagger},\; s^{\dagger},\; \kappa_{sup},\; 	au_{ceil},\; eta_{block})
\]
for every row in the four-line bundle, where `\kappa_{sup}` is the surviving support-family explanation, `	au_{ceil}` is the guidance-subtracted claim ceiling, and `eta_{block}` is the direct promotion blocker. This tuple is intentionally aligned with the newly frozen `experiments.md` logging keys rather than adding another conceptual layer. Its purpose is narrower: once a row improves some part of moving-base guidance, the paper must still declare **how far that improvement is currently allowed to speak** after weaker support-side explanations are subtracted.

This bridge is especially important for the current D07 anchor set because the most likely first positive rows are still mixed rows. A BC warm-start may improve `\Delta BDEE` or `\Delta CET` while remaining acceleration-dominated; BC-to-Q gating may extend `g^{\dagger}` into `W2` while still remaining a retention bridge; analytic projection may improve `\Delta PCST` while still failing the payload-bearing `s_p` requirement; and support-side overlays may sharpen guidance statistics without changing the dominant route. By writing `\Phi^{MEGA}_{row}` directly into the experiments section, D07 commits to an evidence-first rule: **even guidance improvement itself does not automatically imply title promotion unless the row also clears its support-family ceiling and blocker.**

### 4.99 Current Evidence-Consistent Freeze after the Guidance-First Log Bridge

Given the current local evidence and the newly aligned `experiments.md` schema, the most valuable next step is no longer adding another related-work family or broadening the first bundle. The real bottleneck is now whether the first trustworthy D07 table can export `\Phi^{MEGA}_{row}` cleanly enough that the title route becomes mechanically decidable. Under this freeze, the immediate scientific priority is to verify that `B1-B4` can each emit `\Delta BDEE / \Delta CET / \Delta PCST / \Delta PGFR / g^{+} / g^{\dagger} / s^{\dagger}` together with `SupportFamilyExplanation / GuidanceSubtractedCeiling / PromotionBlocker` on the same matched substrate. Until that happens, new semantic overlays, extra reward variants, or wider whole-body comparisons are more likely to add interpretive noise than real evidence.

This means the current D07 paper is already mature enough in concept but still intentionally immature in claim. The honest next-round objective is not to prove MEGA in words, but to force the first executable bundle to reveal whether the dominant route is still **acceleration**, **retention bridge**, **safety-shell support**, **reward-shaped recovery**, or a genuine **deployable moving-base guidance hybrid**. If the table cannot yet defeat the support-family ceiling, then the draft should stay frozen at the weaker route. If it can, then the title upgrade will be earned by the guidance-first log bridge rather than by narrative optimism.

### 4.100 Guidance-Metric-to-Anchor Attribution after Re-reading DiSCo, Reactive Projection, and Find-the-Fruit

After re-reading the current D07 anchor set, the next missing experiment-side clarification is not another family name but a stricter attribution rule for **which guidance metric each anchor is actually allowed to explain first**. DiSCo [REF: 2603.22787] is still best interpreted as a **sequence-copilot / intervention-hygiene support route**: if a row improves mainly through smoother action chunks, cleaner corrective demonstrations, or lower short-horizon jitter, its first plausible effect should appear in `\Delta BDEE` or early `\Delta CET`, not in late-window `\Delta PCST` or payload-bearing `\Delta PGFR`. Reactive Dexterous Grasping [REF: 2605.03363], by contrast, is still the clearest **analytic-projection / contact-stability** anchor in the current local queue, so its first honest explanatory right lies in `\Delta PCST` and, only if the gain survives harder disturbance slices, in reduced `\Delta PGFR`. Find-the-Fruit [REF: 2505.16547] remains an **environment-decomposition support route** whose strongest default explanatory right is improved uncertainty exposure and task preparation, meaning it may help `\Delta BDEE` or `\Delta CET` indirectly without yet earning a late-window moving-base guidance interpretation.

This attribution rule matters because D07 now risks a subtler overclaim than before: not just letting support-side routes occupy the title, but letting them occupy the *wrong metric* inside the first trustworthy table. Under the MEGA ladder, a row that improves `\Delta CET` through cleaner sequence smoothing should not be narrated as if it had proved post-contact guidance survival; a row that reduces `\Delta PCST` via analytic projection should not be retold as if it had solved demonstration quality or decomposition robustness. We therefore require the next D07 bundle to state, for each positive row, not only `SupportFamilyExplanation` and `GuidanceSubtractedCeiling`, but also the **first guidance metric the dominant family is allowed to explain honestly**. This keeps `BDEE → CET → PCST → PGFR` from becoming a decorative ladder and turns it into a family-matched attribution protocol.

### 4.101 Family-Matched Promotion Boundary for the Next D07 Bundle

With the above anchor-to-metric attribution in place, the next D07 bundle should also follow a family-matched promotion boundary. Rows dominated by **copilot / intervention hygiene** are only allowed to compete first on `BDEE` and, at most, early `CET`; rows dominated by **retention bridge** are only allowed to claim that they preserve or extend stable behavior into `CET` or `g^{\dagger}`; rows dominated by **analytic projection** are only allowed to compete first on `PCST` and then `PGFR`; rows dominated by **environment decomposition** are only allowed to claim broader uncertainty exposure or cleaner task preparation unless the same row later survives the full guidance ladder under moving-base disturbance. In other words, every positive row must now defeat the weakest honest family *at the metric where that family is naturally strongest* before it is allowed to promote into a stronger MEGA route.

This boundary is especially useful for the four-row frozen bundle in `experiments.md`. **B2** should first be judged against acceleration-family ceilings at `BDEE/CET`; **B3** should first be judged against retention-family ceilings at `CET/g^{\dagger}`; **B4** should first be judged against projection-family ceilings at `PCST/PGFR`. If a row cannot win even at the metric where its dominant family is naturally strongest, then any broader title promotion is automatically blocked. By writing this directly into the paper, D07 becomes more submission-ready in a reviewer-facing sense: the table no longer just says what improved, but also whether the improvement first survived the *correct matched family challenge* before asking for a stronger moving-base guidance claim.

### 4.102 Guidance-Bearing Method Narrowing after Re-reading Reactive Projection and Hand-in-the-Loop

After re-reading the current D07 anchor set, the next most useful PAPER.md refinement is to narrow the method description itself around the MEGA title. The method should no longer be read as a generic aerial-arm stack with several optional modules, but as a **moving-base end-effector guidance system** organized around three title-bearing paths: **base-motion-aware end-effector guidance** for reducing drift-induced endpoint error, **contact-stage agility** for shortening and stabilizing contact establishment, and **post-contact stabilization** for preserving manipulation-ready guidance under payload-bearing disturbance. Reactive Dexterous Grasping [REF: 2605.03363] most directly informs the third path by showing that task-space intent plus analytic projection can stabilize contact and post-contact motion. Hand-in-the-Loop [REF: 2605.15157], by contrast, sharpens the opposite boundary: smoother intervention and cleaner correction data may improve training quality or early guidance behavior, yet still remain an intervention-hygiene support route unless they measurably improve moving-base guidance quality itself.

This narrowing implies a stricter wording rule for D07. Warm start, BC-to-Q gating, copilot assistance, semantic relation state, and decomposition-aware training may all remain in the method section, but they must be introduced as **support routes around the guidance core** unless they explicitly improve `\Delta BDEE`, `\Delta CET`, `\Delta PCST`, or `\Delta PGFR` in the late windows and stronger disturbance slices. In other words, the D07 method is no longer “RL plus several useful modules”; it is a guidance-first architecture in which support modules are justified only insofar as they strengthen moving-base end-effector guidance, contact-stage agility, or payload-bearing post-contact stability.

### 4.16 Guidance-Bearing Method Freeze after Re-reading Hand-in-the-Loop and Find-the-Fruit

Re-reading the current D07 local anchors sharpens a constraint that is easy to blur in a MEGA-shaped paper: some methods improve the *conditions around guidance* without yet improving **moving-base end-effector guidance itself**. Hand-in-the-Loop [REF: 2605.15157] is the clearest current example. Its strongest evidence is that seamless intervention blending suppresses takeover jitter, reduces grasp failure, and yields cleaner corrective data for later policy refinement. Under D07's guidance-first contract, this is valuable but still support-side by default: it can legitimately explain better demonstration hygiene, smoother bounded intervention, and perhaps earlier-window reductions in `ΔBDEE` or `ΔCET`, but unless it materially improves `ΔPCST`, `ΔPGFR`, and late-window survival under coupled disturbance, it remains an **intervention-hygiene / correction-smoothing support route** rather than a MEGA title-bearing route.

Find the Fruit [REF: 2505.16547] produces a parallel but distinct warning. Its zero-shot sim-to-real success comes from structured environment decomposition—interaction planning, compliant low-level control, and broad morphology/occlusion randomization—not from a demonstrated improvement in moving-base end-effector guidance under base-induced disturbance. For D07, this means Find-the-Fruit-style gains are best interpreted as **decomposition / uncertainty-exposure support**: they may legitimately explain stronger task preparation, broader uncertainty coverage, and cleaner early-stage target exposure, and they may indirectly help `ΔBDEE` or `ΔCET`; however, unless they also improve `ΔPCST`, `ΔPGFR`, and `g^†` in payload-bearing disturbance slices, they cannot be promoted into a guidance-bearing explanation. This freeze is methodologically important because it prevents D07 from borrowing sim-to-real robustness narratives or high-quality human-correction narratives to overclaim moving-base end-effector guidance.

### 4.17 Guidance-First Main Table Bridge from experiments.md to Submission Routing

To keep the paper aligned with the executable D07 workflow, we directly mirror the `experiments.md` guidance-first logging contract into the submission-facing main table. Each row is summarized as
\[
\mathcal{T}_{MEGA}=\big(\Delta BDEE,\; \Delta CET,\; \Delta PCST,\; \Delta PGFR,\; g^{+},\; g^{\dagger},\; s^{\dagger},\; \kappa_{support},\; \tau_{ceil},\; \beta_{block}\big),
\]
where `\kappa_{support}` is the weakest still-sufficient support-family explanation, `\tau_{ceil}` is the current guidance-subtracted ceiling, and `\beta_{block}` records the direct promotion blocker. Concretely, D07's first trustworthy table should expose the following columns: `Method / ΔBDEE / ΔCET / ΔPCST / ΔPGFR / g^+ / g^† / s^† / SupportFamilyExplanation / GuidanceSubtractedCeiling / PromotionBlocker`.

This bridge matters because it makes submission routing a deterministic consequence of logged guidance evidence rather than a prose decision made afterward. A row that improves only `ΔBDEE` or early `ΔCET` while remaining explainable by intervention hygiene, retention, projection, or decomposition support must stay frozen at the corresponding support ceiling. A row that reaches late-window `ΔPCST` and `ΔPGFR` improvements and survives to `s_p` may move upward, but only after the matched weaker-family explanation is defeated. By hard-linking `experiments.md` fields to the paper's main table in this way, D07's next real bundle can be read in the required order—**guidance first, support-family second, title routing last**—which keeps the MEGA narrative honest.

### 4.103 Guidance-Bearing versus Support-Route Metric Attribution from the Current Local Anchor Set

Because the MEGA title centers **moving-base end-effector guidance**, D07 should explicitly state which metric each current anchor family is allowed to explain *first*. We therefore add a metric-first attribution rule before any route promotion is considered. **BC-to-Q retention routes** such as Q2RL are only allowed to first explain more stable warm-start behavior under base drift and modest improvement in `ΔBDEE` continuity over `W1/W2`; if they do not also improve `ΔPCST` or `ΔPGFR`, they remain an **anti-forgetting support route**. **Analytic projection routes** such as Reactive Dexterous Grasping are first allowed to explain faster contact establishment and safer local post-contact recovery—namely `ΔCET` and early `ΔPCST`; only if they keep that gain under stronger disturbance sources and payload-bearing slices may they support a stronger guidance-bearing interpretation. **Hand-in-the-Loop / DiSCo-style copilot routes** are first allowed to explain intervention hygiene and smoother corrective transitions, which may indirectly reduce `ΔCET` variance, but unless they directly improve guidance quality under moving-base disturbance they remain **bounded-assistance support**. **Find-the-Fruit-style decomposition routes** are first allowed to explain uncertainty exposure and better preparation under occlusion or clutter, but not by themselves stronger `ΔBDEE`, `ΔPCST`, or `ΔPGFR`.

This rule matters because it prevents D07 from letting support-side gains occupy the same evidential slot as title-bearing guidance gains. A row is only allowed to move from support-route language toward MEGA language when its dominant family first wins the weakest metric it should naturally own, and then continues to improve later guidance metrics under stronger disturbance boundaries. In practice, that means **every first-pass result paragraph must say which metric the row earned honestly first**, before claiming that it improved moving-base end-effector guidance more broadly.

### 4.105 Guidance-Metric-First Family Freeze from the Current Local Anchor Set

A final clarification emerging from the current local D07 anchor set is that each nearby family should be granted only a **metric-first explanatory right**, not an open-ended promotion path. Isaac Lab [REF: 2511.04831] can legitimately explain better reset hygiene, disturbance injection consistency, and multi-modal training throughput, but by itself it should first be credited as an **infrastructure-hygiene support family** rather than a guidance-bearing method. In the MEGA framing, that means Isaac-Lab-enabled rows are only allowed to claim that cleaner infrastructure made `\Delta BDEE` or `\Delta CET` easier to measure or optimize; unless the same row later survives `\Delta PCST` and `\Delta PGFR` under harder disturbance slices, infrastructure gain cannot be retold as moving-base guidance gain. Likewise, ViserDex-style visual bridging [REF: 2604.11138] should first be allowed to explain improvements in **drift-robust end-effector alignment**—namely lower `\Delta BDEE` and possibly earlier positive `g^{+}`—because its strongest default contribution is perception-gap reduction rather than late-window disturbance survival.

By contrast, the DSAM-style aerial-manipulator route [REF: 2512.21085] remains one of the few current anchors that is naturally allowed to speak first in the late guidance metrics. Since its core evidence is RL outer-loop control combined with INDI/PID inner-loop stabilization on a real aerial manipulator, its first honest explanatory rights lie in `\Delta PCST` and reduced `\Delta PGFR` under payload-bearing or contact-compounded disturbance. This yields a stricter family freeze for the next D07 bundle: **Isaac Lab** rows must first defeat infrastructure-only explanations, **ViserDex** rows must first defeat perception-bridge-only explanations at `BDEE`, and **DSAM / hybrid stabilization** rows must first defeat local-projection-only explanations at `PCST / PGFR`. Only after clearing those metric-matched weaker families may a row be promoted from support-side evidence toward a true MEGA guidance-bearing route.

### 4.106 Current Evidence-Consistent Next Step after the Isaac-Lab / ViserDex / DSAM Re-read

This re-read further confirms that D07 does not currently lack method families; it lacks one more layer of **guidance-metric-specific comparator discipline** in the first trustworthy table. The immediate next step is therefore to ensure that `experiments.md` can log, for each row, not only `\Delta BDEE / \Delta CET / \Delta PCST / \Delta PGFR / g^{+} / g^{\dagger} / s^{\dagger}`, but also the matched weaker family that must be defeated at that metric: `infrastructure hygiene`, `perception bridge`, `retention bridge`, `analytic projection`, or `hybrid stabilization`. Without this explicit comparator field, an Isaac-Lab-enabled row may be over-read as controller progress, a ViserDex-like row may be over-read as guidance agility, and a DSAM-like row may be over-read as whole-stack superiority before it has actually defeated the nearest weaker explanation.

Under this freeze, the evidence-consistent recommendation for the next D07 round is simple: keep the literature set fixed, keep high-value new-paper ingestion at zero unless a clearly stronger moving-base guidance anchor appears, and continue pushing the paper toward a **submission-facing guidance table whose first decision is metric-matched family defeat rather than raw score ranking**. That keeps MEGA honest: the title will be earned only when a row improves moving-base guidance late enough, under hard enough disturbance, and after subtracting the exact weaker family explanation most naturally attached to that metric.


The current local D07 anchors now support one tighter writing rule: every nearby family should first be frozen to the **weakest guidance metric it naturally explains**, and only then be allowed to compete for stronger MEGA language. DiSCo-style sequence copilots [REF: 2603.22787] primarily improve **intervention hygiene and action-sequence smoothness**, so their first honest explanatory slot is `ΔBDEE` and, at most, early `ΔCET`. If a row using copilot assistance fails to push the gain into `W2/W3`, then the correct reading is still **sequence-copilot support**, not late-window moving-base guidance. Reactive Dexterous Grasping [REF: 2605.03363] instead provides the clearest current anchor for **analytic projection and contact-stage safety**, so its first honest explanatory slot is `ΔPCST`, with `ΔPGFR` becoming available only if the same row survives stronger disturbance sources and payload-bearing slices. KG-M3PO [REF: 2603.24083] sits in a third bucket: relation-state and scene-graph support may reduce ambiguity during alignment or approach, but their natural first slot remains `ΔBDEE` or earlier `ΔCET`, not post-contact guidance preservation.

This family freeze is useful because D07 has accumulated enough support-side structure that overclaim risk now happens at the **metric level**, not only at the title level. A row may look strong simply because its dominant family is being credited for a harder metric than it actually owns. By freezing DiSCo to `ΔBDEE/early ΔCET`, Reactive Projection to `ΔPCST→ΔPGFR`, and relation-state support to `ΔBDEE/ΔCET`, the paper forces every row to win its easiest honest metric first before it can ask for a stronger route. This directly aligns PAPER.md with the new `guidance-family metric-first routing freeze` already committed in `experiments.md`.

### 4.107 Reward-Generalization and Test-Time Steering Families under the Guidance-First Audit

The recent local anchors on **invariant reward learning** [REF: 2605.22123] and **deployment-time residual steering under distribution shift** [REF: 2604.01142] expose another risk for D07: rows may improve under camera shifts, object variations, or contact-parameter drift without yet improving the moving-base guidance story promised by the MEGA title. We therefore add two more support families to the guidance-first audit. Invariant-reward routes are allowed to explain more stable `\Delta CET` and `\Delta PCST` under viewpoint, position, or object changes, because better reward invariance can preserve task progress signals when raw pixels move. However, unless those gains also extend `g^{\dagger}` into `W3` and reduce `\Delta PGFR` under payload-bearing disturbance, they remain **reward-generalization / reward-shaped recovery support** rather than moving-base guidance evidence.

Residual-steering or test-time correction routes are interpreted even more narrowly. Methods such as bounded extremum-seeking correction [REF: 2604.01142] can improve post-contact stabilization and reduce drift after deployment-time distribution shift, but they first earn credit only as **post-contact correction support**. To be promoted beyond that ceiling, they must not only improve `\Delta PCST` and possibly `\Delta PGFR`, but also defeat the matched weaker explanations from analytic projection and hybrid inner-loop stabilization. In other words, D07 now distinguishes three separate explanations for late-window recovery under shift—better reward credit, better local corrective steering, and better deployable hybrid interface—and forbids them from being merged until the guidance-first audit explicitly removes the weaker families.

### 4.109 Four-Row Guidance-Metric-Matched Comparator Contract for the Next Trustworthy Bundle

The current D07 evidence is now strong enough that the next step should no longer add new nearby families, but should instead bind the existing **four-row bundle** to a stricter guidance-metric-matched comparator contract. Each row in `B1-B4` must therefore declare not only its `\Delta BDEE / \Delta CET / \Delta PCST / \Delta PGFR / g^{+} / g^{\dagger} / s^{\dagger}` tuple, but also the **single weakest family explanation it must defeat at its first honest metric**. Concretely, `B2` is matched first against an **acceleration-only** explanation at `\Delta BDEE/\Delta CET`; `B3` is matched first against a **retention-only** explanation at `\Delta CET/g^{\dagger}`; and `B4` is matched first against a **projection-only** explanation at `\Delta PCST`, only then being allowed to compete for `\Delta PGFR` under `s_p`. This forces the MEGA table to answer a harder but cleaner question: *did a row actually improve moving-base guidance at the metric it naturally owns, after subtracting the nearest weaker family, before asking for a stronger title route?*

This contract is important because D07 has accumulated enough support-side structure that raw score comparison is no longer trustworthy. A `B3` row that reaches `W2` but still only preserves BC-consistent behavior should stay frozen as a retention bridge. A `B4` row that lowers unsafe actions and even improves early contact, but cannot hold `\Delta PCST` or `\Delta PGFR` under payload-bearing slices, should remain a projection-only or post-contact correction support route. Under this freeze, the next trustworthy D07 bundle is not the one with the highest average success, but the one that can most cleanly say: **this row first won this guidance metric, against this weaker family, and then survived long enough to earn the next stronger MEGA language**.

### 4.110 Current Evidence-Consistent Next Step after Re-reading Q2RL, DiSCo, and the Four-Row Bundle

After revisiting the recent local anchors **Q2RL**, **DiSCo**, and the current `experiments.md` four-row freeze, the evidence-consistent next step is to keep literature expansion at zero and push D07 one notch closer to a submission-facing table. The immediate writing and experimental priority is to mirror the paper-side comparator rule directly in logging: every future row should export `FirstOwnedGuidanceMetric`, `MatchedWeakerFamily`, and `FamilyDefeatStatus` in addition to the existing guidance tuple. Without those fields, D07 still risks over-reading `B2/B3` as guidance progress when they may only be acceleration or retention support, and over-reading `B4` as deployable hybrid control when it may only be projection-side stabilization.

Under the owner's MEGA instruction, that means the next round should not hunt for another sim-to-real or RL paper unless it is a clearly stronger **moving-base guidance-bearing** anchor than the current set. Instead, D07 should continue freezing the paper around one submission-ready question: **which row first wins its owned guidance metric, defeats its nearest weaker family, and then survives into later windows and harder disturbance sources?** Only a row that clears that ladder deserves stronger MEGA title weight; everything else should remain explicitly routed to acceleration, retention bridge, copilot support, reward-shaped recovery, or safety-shell support.






### 4.111 MEGA Guidance-First Main Table Contract after Re-reading Find-the-Fruit / Q2RL / Reactive Projection / Copilot Families

Under the current D07 anchor set, the first submission-ready main table should be guidance-first rather than controller-family-first. In particular, rows should no longer be promoted primarily by whether they used BC warm start, BC-to-Q gating, analytic safety projection, or a copilot layer; they should be promoted by whether they materially improve the **MEGA guidance tuple** \((\Delta BDEE, \Delta CET, \Delta PCST, \Delta PGFR, g^{+}, g^{\dagger})\) under moving-base disturbance. This matters because the recent local anchors now make it easy to confuse guidance-adjacent support with guidance-bearing evidence: Find-the-Fruit can improve uncertainty exposure and interaction preparation, Q2RL can preserve useful competence during online adaptation, Reactive Dexterous Grasping can improve local safety projection, and copilot / intervention-hygiene families can smooth correction trajectories. All four are valuable, but none should occupy a MEGA headline unless the gain survives into late-window moving-base guidance.

We therefore require each D07 row to expose a guidance-first block before any title promotion: `\Delta BDEE`, `\Delta CET`, `\Delta PCST`, `\Delta PGFR`, `g^{+}`, `g^{\dagger}`, `s^{\dagger}`, `SupportFamilyExplanation`, `GuidanceSubtractedCeiling`, and `PromotionBlocker`. These fields force the paper to answer a sharper question than “which controller is better”: after subtracting acceleration-only, retention-only, projection-only, reward-shaped, copilot, or decomposition-side explanations, **did the row still improve moving-base end-effector guidance deeply enough to survive W3 and payload-bearing disturbance?** If not, the row must freeze at the weakest still-honest support route.

### 4.112 Guidance-Family Routing Freeze before Deployable-Hybrid Promotion

The reviewer-facing reading order should now be explicitly metric-first: first check `\Delta BDEE` and `\Delta CET` for early guidance acquisition, then `\Delta PCST` for post-contact stabilization, and finally `\Delta PGFR` together with `g^{\dagger}` and `s^{\dagger}` for payload-bearing guidance survival. Only after this metric ladder is established may we ask which family explanation still remains cheapest. Under this rule, **copilot / intervention-hygiene** routes may explain early `\Delta BDEE` or `\Delta CET` improvements, **Q2RL-style retention** routes may explain stronger `g^{\dagger}` or safer W2 carryover, **analytic projection** routes may explain local `\Delta PCST` gains, and **Find-the-Fruit-style decomposition** routes may explain stronger task preparation or uncertainty exposure. But unless the residual after those matched explanations still raises `\Delta PGFR` and survives to `W3` under `s_p`, the row cannot be promoted to `deployable hybrid`.

This freeze is intentionally strict because D07 is now mature enough that multiple guidance-adjacent routes can produce attractive partial wins. Without a guidance-family routing freeze, the paper could too easily over-read smoother correction, safer projection, or better occlusion preparation as if they had already solved moving-base guidance under payload-bearing disturbance. Our main-results contract should instead force every row to stop at the weakest honest title route: `acceleration`, `retention bridge`, `safety-shell support`, `reward-shaped recovery`, or `deployable hybrid`.

### 4.113 Current Evidence-Consistent Freeze after Guidance-First Promotion Alignment

Given the current local D07 anchor set, the most honest freeze remains conservative. Rows whose gains are exhausted by faster matched-compute convergence without stable `\Delta PCST` or `\Delta PGFR` remain **acceleration**. Rows whose gains are exhausted by BC-to-Q carryover and improved `g^{\dagger}` but do not materially improve payload-bearing guidance remain **retention bridge**. Rows whose gains are exhausted by analytic rejection or projection-side contact stabilization while leaving `\Delta PGFR` weak remain **safety-shell support**. Rows whose gains are mainly attributable to denser reward credit or OOD reward generalization remain **reward-shaped recovery**. Only rows that survive these weaker explanations, still improve `\Delta PCST` and `\Delta PGFR`, and remain alive through `W3` at disturbance source `s_p` may support a **deployable hybrid** claim.

This means D07 should currently resist a familiar temptation: if a row improves contact establishment and looks safer under projection, it may still be fully explainable by retention, safety-shell, or reward-origin families rather than by a stronger moving-base guidance interface. Under the MEGA title, the headline belongs only to the residual that still moves the guidance tuple after all cheaper support routes are honestly subtracted.

### 4.107 Guidance-Family Metric-First Routing Freeze for Reward-Generalization and Test-Time Steering

The recent local anchors on **invariant reward learning** [REF: 2605.22123] and neighboring deployment-time correction routes make one more support-family boundary necessary. Reward-generalization families are best interpreted as **guidance-adjacent support** whose natural first claim is not better controller architecture, but more stable reward credit across viewpoint, object, or scene variation. Under the MEGA title, such routes should first be required to improve `ΔPCST` or `ΔPGFR`, because their most plausible guidance-facing benefit is more reliable post-contact recovery and lower payload-bearing failure through better task-level credit assignment, not sharper pre-contact alignment by itself. Likewise, deployment-time residual steering or online correction layers should first be required to improve `ΔPCST` or `ΔPGFR` under matched disturbance slices before they are allowed to compete with true guidance-bearing hybrid routes.

This freeze matters because reward-generalization and test-time steering can easily produce rows that “look smarter” without actually changing moving-base end-effector guidance quality. A row that simply rewards the right recovery behavior more consistently, or adds a lightweight test-time correction layer, may still be extremely useful for D07, but its first honest route is **reward-generalization support** or **post-contact correction support** rather than a stronger MEGA controller story. By forcing these families to win on `ΔPCST / ΔPGFR` first, the paper keeps them attached to the guidance ladder without letting them bypass it.

### 4.108 Current Evidence-Consistent Freeze after Re-reading Invariant Rewards and Post-Contact Correction Support

After re-reading the local invariant-reward anchor, the current D07 evidence suggests that reward-side and correction-side routes should remain explicitly subordinate to the core guidance narrative until proven otherwise. The strongest honest interpretation today is that these routes may become valuable **post-contact support families**: invariant rewards can make recovery credit more transferable across OOD settings, and residual steering can make late-stage corrections more stable under disturbance. But unless those gains clearly reduce `ΔPCST` or `ΔPGFR` on the same moving-base substrate—and survive family-matched comparison against simpler reward- or correction-side explanations—they should remain frozen below deployable moving-base guidance.

This freeze is useful because it matches the likely first positive outcome pattern in D07. Long before a route proves full moving-base guidance superiority, it may already show nicer late-stage recovery curves, cleaner OOD reward reuse, or smoother disturbance correction. The paper should welcome those gains, but name them honestly: **reward-generalization support** or **post-contact correction support** first, and only later—if the guidance ladder is truly crossed—something stronger.



### 4.21 Current Evidence-Consistent Next Step after the MobileManiBench Re-read

Under the current local anchor set, the most evidence-consistent next step for D07 is still not to expand the method matrix, but to make the **guidance tuple plus verification tuple** directly writable by the first real four-row bundle. The current anchor division is now relatively stable: **Isaac Lab** explains infrastructure hygiene and disturbance reproducibility, **RL-Based Sim-Real Co-Training** explains protocol continuity and real-data anchoring support, and **MobileManiBench** explains verification-shell completeness. None of these routes should be allowed to speak more loudly than the moving-base guidance metrics themselves.

Therefore, the immediate experimental priority is to ensure that the training scripts can export `\mathcal{G}_{row}` and `V_{row}` jointly, together with row-level support-family explanations and promotion blockers. Only after this logging bridge is complete should D07 reopen hybrid expansion, semantic overlays, or richer whole-body variants. This ordering keeps the paper aligned with the owner's MEGA title constraint: the draft must first prove that a route actually improves **moving-base end-effector guidance and agility**, not merely that it is easier to train, easier to verify, or easier to explain.

### 4.109 Verification-Support and Protocol-Support Families under the Guidance-First Audit

The latest local D07 re-read also makes a second subtraction boundary impossible to ignore: some rows may look stronger because their **validation substrate** or **sim-real protocol discipline** is better, not because the moving-base guidance interface itself improved. Under the current anchor set, **Isaac Lab** [REF: 2511.04831] is the clearest `infrastructure-hygiene support` family, **RL-Based Sim-Real Co-Training** [REF: 2602.12628] is the clearest `protocol support` family, and **MobileManiBench** [REF: 2602.05233] is the clearest `verification-support / benchmark-shell` family. All three are highly valuable to D07, but their default explanatory rights remain below title-bearing MEGA guidance claims.

We therefore require the next trustworthy D07 bundle to carry a second verification tuple alongside the guidance tuple:
\[
V_{row}=(C_{task},\; C_{fail},\; C_{slice},\; \kappa_{verify},\; \kappa_{protocol}),
\]
where `C_task` measures task-coverage diversity, `C_fail` measures exposed failure-mode diversity, `C_slice` measures disturbance/payload slice completeness, `\kappa_{verify}` records the dominant verification-support family, and `\kappa_{protocol}` records whether the row still benefits mainly from stronger sim-real anchoring or curriculum continuity. A row whose apparent gain is mostly explained by `V_row` must first be frozen as **infrastructure-hygiene support**, **protocol support**, or **verification-support**, even if that row makes failures easier to surface or comparisons easier to trust.

This rule is intentionally conservative because MEGA is not a paper about benchmark quality in the abstract. A stronger substrate can make D07 more scientific and more reproducible, but exposing brittle rows more clearly is still different from actually improving **base-motion-aware end-effector guidance**, **contact-stage agility**, or **payload-bearing post-contact survival**. Only residual gains that remain after matched verification coverage and matched protocol support—and that still improve `\Delta BDEE`, `\Delta CET`, `\Delta PCST`, or `\Delta PGFR` through late windows—may continue toward a stronger title route.

### 4.110 Current Evidence-Consistent Next Step after the Isaac Lab / RL-Based Sim-Real Co-Training / MobileManiBench Re-read

Given the current local anchor set, the most evidence-consistent next step for D07 is not to enlarge the method zoo, but to make the first real bundle **jointly guidance-first and verification-aware**. In practice, this means the next bundle must export both `\mathcal{G}_{row}` and `V_{row}` for the four-row core comparison, so that D07 can answer two questions at once: **which row improves moving-base guidance latest and hardest**, and **how much of that apparent gain is still explainable by stronger infrastructure, protocol discipline, or verification shell**.

This freeze also clarifies title-routing discipline. If a row mainly benefits from Isaac-Lab-native reset hygiene, stronger sim-real anchoring continuity, or MobileManiBench-style validation breadth, it should be written as **infrastructure/protocol/verification support** even when those supports are strategically important. Only rows that still preserve late-window positive `\Delta PCST`/`\Delta PGFR`, survive to `W3` and `s_p`, and defeat these weaker support-family explanations may be promoted toward **deployable hybrid moving-base guidance**. That is currently the cleanest honest bridge from the D07 local evidence base to a submission-ready MEGA main table.

### 4.111 Transfer-Protocol Support should be Frozen below the High-Frequency Stabilizer Claim

RL-Based Sim-Real Co-Training for VLA [REF: 2602.12628] and adjacent Isaac-Lab transfer recipes strengthen an increasingly important route for D07: a policy may look much more deployment-ready simply because its initialization, adaptation schedule, or real-data anchoring protocol is better organized. In aerial-arm manipulation, this route is highly valuable because real moving-base data are expensive, disturbance-rich, and potentially unsafe to collect at scale. A stronger transfer protocol can therefore reduce sim-to-real friction, shorten adaptation time, and preserve more useful pre-contact competence before one ever proves that the arm itself is compensating low-frequency UAV drift in a fundamentally stronger way.

We therefore treat sim-real co-training and related initialization/adaptation schedules as a distinct **transfer-protocol support (TPS)** family. Their gains are allowed to explain lower adaptation cost, stronger initialization retention under real-data anchoring, and cleaner deployment readiness in early windows, but they are not yet allowed to occupy the MEGA title claim. Under our guidance-first reading, any apparent gain that is still fully explainable by better transfer organization must freeze below the **mechanical-arm high-frequency stabilizer** narrative unless it also keeps `drift_amp \mapsto BDEE` flatter, pushes `g^{\dagger}` to `W3`, and lowers `PGFR` under payload-bearing disturbance after matched subtraction.

### 4.112 Platform-, Reward-, Verification-, and Transfer-Subtracted Residual as the Core MEGA Evidence

To keep D07 honest after the recent anchor expansion, we define a stronger residual accounting view for the MEGA claim. Let `PRS` denote platform/reproducibility support, `ROS` denote reward-origin support, `VSS` denote verification-shell support, and `TPS` denote transfer-protocol support. Then a row is only allowed to support the central D07 narrative if its moving-base guidance gain remains positive after these four weaker explanations have been explicitly discounted. In other words, the reviewer-facing question is no longer merely whether a row improves `BDEE`, `CET`, `PCST`, or `PGFR`, but whether it still improves them after we ask: was the row simply run on a cleaner platform shell, helped by denser reward credit, protected by a broader verification shell, or organized by a stronger transfer recipe?

This leads to a stricter interpretation of the high-frequency stabilizer hypothesis. The residual that matters for MEGA is the part of the guidance gain that cannot be eaten by platform hygiene, reward shaping, verification coverage, or transfer scheduling. Only if that residual still flattens the `drift_amp \mapsto BDEE` curve, survives to `g^{\dagger}=W3`, and improves payload-bearing `PGFR` should the paper promote the result to **mechanical-arm high-frequency stabilizer evidence**. Otherwise, the result must freeze at the weakest support-family explanation that still honestly fits the data.

