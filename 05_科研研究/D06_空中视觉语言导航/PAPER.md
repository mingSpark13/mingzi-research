# Aerial Vision-Language Navigation with 3D Semantic Frontier Mapping and Dynamics-Aware Exploration

> 方向：D06 空中视觉语言导航 | 目标会议：ICRA 2027 | 状态：🟡 成形
> 最后更新：2026-06-12

---

## Abstract

Aerial Vision-Language Navigation (VLN) requires unmanned aerial vehicles to follow natural-language instructions in open 3D environments, yet existing VLN methods remain poorly matched to aerial deployment because they inherit 2D frontier assumptions, weak dynamics awareness, and brittle planner-controller handoff. We propose a packet-centric aerial VLN framework that couples **3D Semantic Frontier Mapping**, **dynamics-aware frontier selection**, and a **Navigate-then-Manipulate (NtM)** execution interface. The core representation is a **Semantic Waypoint Packet**, which serializes target pose, yaw prior, altitude band, stage tag, runtime budget, and guarded execution prefix into a verifier-compatible object that can be refreshed online. This framing allows us to jointly evaluate semantic planning quality, directional instruction adherence, runtime continuity, and downstream controllability rather than collapsing them into a single navigation score. Compared with prior aerial systems such as OnFly, HTNav, LookasideVLN, and LiveVLN, our method is designed to preserve packet validity through the full `planner → verifier → controller` chain while retaining the ability to hand off from search-time navigation to manipulation-time local interaction. We further formalize packet-facing metrics, including verifier pass rate, packet staleness, packet handoff failure, context retention, and progress-trace consistency, to expose failure modes that standard success-rate reporting obscures. The resulting framework positions aerial VLN as a deployable interface problem rather than only a waypoint prediction problem.

---

## 1. Introduction

### 1.1 Problem Statement

Vision-Language Navigation (VLN) enables robots to navigate to language-described targets in unknown environments, a core capability for embodied intelligence. While ground VLN has seen substantial progress—VLFM [REF: ], NaVILA [REF: ], ApexNav [REF: ], SG-Nav [REF: ]—**aerial VLN remains severely underexplored**. Existing methods assume 2D planar motion, low speed, and stable viewpoints, fundamentally incompatible with the 6-DoF motion space, high-speed flight, dramatic pitch-angle changes, and dynamics constraints of unmanned aerial vehicles (UAVs).

The gap is not merely one of scale. Aerial platforms introduce three qualitatively distinct challenges absent from ground VLN: (1) **3D spatial exploration**: targets may be above, below, or at arbitrary elevations, requiring volumetric frontier representations rather than 2D maps; (2) **dynamics-aware planning**: energy budgets, velocity limits, and attitude constraints must be incorporated into exploration cost functions to avoid infeasible paths; and (3) **navigation-to-manipulation continuity**: aerial VLN naturally extends to Navigate-then-Manipulate (NtM) tasks where the robot must transition from search to precise end-effector operations upon target localization.

Recent surveys [REF: 2604.13654, 2604.07705] confirm that 3D spatial representation and dynamics-aware planning are the two core unsolved problems in aerial VLN. This paper directly addresses both.

### 1.2 Contributions

This paper makes the following contributions:

1. **C1: Aerial VL-Frontier Map (AVLFM)**: We extend 2D vision-language frontier maps to 3D volumetric frontier representations for UAV navigation, enabling language-guided exploration over vertical structure, viewpoint change, and target-region uncertainty in full 3D space.

2. **C2: Semantic Waypoint Packet as a deployable planner-controller interface**: We formalize a reusable packet carrying target pose, yaw prior, altitude band, semantic confidence, progress state, and safety budget, and show how this interface enables verifier-compatible, refreshable, controller-consumable aerial navigation outputs instead of one-shot waypoint lists.

3. **C3: Dynamics-aware packet execution and evaluation on AirSpark**: We instantiate the packet framework on the AirSpark platform with dynamics-aware frontier ranking, online refresh, and controller-side execution constraints, and evaluate whether packet fields survive the full `planner → verifier → controller` chain under realistic aerial deployment conditions.

> **Scope note.** We intentionally do **not** treat dataset/benchmark construction as a contribution of this paper. The broader AirManip-Bench benchmark and full Navigate-then-Manipulate task suite are split into a companion benchmark paper; in this paper, NtM is the motivating downstream setting and AirSpark is the experimental platform.

---

## 2. Related Work

### 2.1 Ground Vision-Language Navigation

Ground VLN has established strong baselines for language-guided exploration. VLFM [REF: ] uses VLM semantic relevance to guide 2D frontier exploration, demonstrating that language-conditioned frontier selection significantly outperforms geometric-only exploration. NaVILA [REF: ] adopts a hierarchical VLA design with high-level VLM reasoning and low-level action execution. AgentVLN (arXiv:2603.17670) [REF: 2603.17670] proposes a VLM-as-Brain paradigm that decouples high-level semantic reasoning from perception and planning via a plug-and-play skill library, achieving SOTA on R2R-CE with only 3B parameters and enabling real-time edge deployment on quadruped robots—a modular paradigm highly relevant to aerial VLN module design. ApexNav [REF: ] introduces adaptive semantic-geometric exploration balancing exploration and exploitation. SG-Nav [REF: ] uses scene graph representations for structured navigation reasoning. DRIVE-Nav [REF: ] enhances spatial reasoning through directional inference. These methods collectively demonstrate the value of semantic frontier maps but are fundamentally limited to 2D planar motion and cannot handle aerial dynamics.

### 2.2 Aerial Navigation Systems

OnFly [REF: ] introduces a dual-agent UAV navigation system with decoupled perception and control frequencies, the closest existing work to aerial VLN. OpenFly [REF: 2502.18041] provides a comprehensive aerial VLN platform combining UE/GTA V/Google Earth/3DGS rendering sources, automatic trajectory and instruction generation, and a keyframe-aware VLA model, serving as our primary data infrastructure reference. AirNav [REF: 2601.03707] presents the first real-world urban aerial VLN benchmark with AirVLN-R1 (SFT + RLFT training recipe), directly addressing the real-data evaluation gap. APEX [REF: 2602.00551] proposes Dynamic Spatio-Semantic Mapping with RL-based action decisions and target-guided exploration, providing an interpretable modular explorer suitable as an engineering reference for our GoalSearch component. Fly0 [REF: 2602.15875] explicitly decouples semantic grounding from geometric trajectory planning in three stages, demonstrating zero-shot, low-latency navigation with geometric robustness after visual loss. FineCog-Nav [REF: 2604.16298] introduces fine-grained cognitive modularization for zero-shot UAV VLN, decomposing language processing, perception, attention, memory, imagination, reasoning, and decision into role-specific modules with the AerialVLN-Fine benchmark for sentence-level instruction-trajectory alignment. SpatialFly [REF: 2603.21046] represents a complementary geometry-first line: rather than constructing an explicit 3D frontier map, it injects geometric priors into 2D semantic tokens and aligns them with 3D structural cues, showing that representation-level geometry alignment can improve unseen-scene path alignment and motion smoothness even without full volumetric reconstruction. AeroVerse [REF: 2408.15511] complements these task-specific systems by providing a full-stack UAV-agent benchmark suite spanning simulation, pre-training, finetuning, and evaluation; for D06, its main relevance is not a packet-level method contribution but a systems-level reminder that aerial VLN claims should eventually be judged inside a broader aerial embodied stack including navigation, reasoning, and motion-decision evaluation.
KIO-planner [REF: 2605.19703] adds a useful low-level contrast point: its attention-guided single-stage motion planner with dual mapping shows that strong gains in local safety, narrow-space traversability, and controller-side latency can be obtained without solving language grounding or packet-level semantics. For D06, it should therefore be treated as a local execution-shell family that can justify stronger **geometric executability** and **near-field safety compliance**, but not by itself stronger direction-aware packet preservation or manipulation-ready handoff accountability.
FlyMirage [REF: 2605.19600] contributes from the data side rather than the packet side: by combining LLM-authored scene layouts, generative world models, 3DGS instantiation, and dynamics-feasible trajectory synthesis, it suggests that aerial VLN systems may soon benefit from scalable long-horizon data generation without manually flying or annotating every scenario. In our framing, FlyMirage can justify stronger **data coverage**, **trajectory-diversity support**, and **pretraining prior quality**, but it does not by itself establish better packet refresh honesty, controller-consumption stability, or NtM handoff preservation.
FLIGHT VLA [REF: 2606.06836] adds a newly relevant long-horizon baseline by explicitly splitting low-frequency pilot-style reasoning from high-frequency diffusion control under a unified benchmark for fine-grained aerial instruction following. Its strongest value for D06 is not a packet-level contract, but a clean demonstration that asynchronous semantic reasoning and continuous control can substantially improve **multi-stage task adherence**, **subgoal-level progress maintenance**, and **real-time control smoothness** without necessarily exposing a reusable WaypointExecutor-aligned packet object. In our framing, FLIGHT VLA therefore belongs to a **long-horizon asynchronous planning-control** family: it may justify stronger planner-side progress discipline and controller-time responsiveness under long instructions, but it does not by itself establish schema-aligned packet serialization, delayed consume-time packet preservation, or replay-level accountability inside AirSpark.

### 2.3 Aerial Manipulation

π-Make-It-Fly [REF: ] demonstrates that visual representations transfer to aerial platforms but control dynamics do not, requiring payload-aware guidance at inference time. Unified Aerial Grasping [REF: ] provides an end-to-end language-conditioned aerial grasping system with active exploration and 6-DoF grasping. AeroPlace-Flow [REF: ] addresses language-guided aerial object placement using Visual Foresight and Object Flow. These manipulation works assume known approximate target locations, lacking a systematic navigation front-end—the gap our NtM architecture addresses.

### 2.4 Direction-Aware Spatial Language Planning and Live Runtime Execution

LookasideVLN [REF: 2604.17190] highlights a previously under-modeled source of aerial VLN gain: explicit directional cues in language instructions. Instead of relying only on landmark mentions or deeper lookahead search, it introduces an Egocentric Lookaside Graph and a Spatial Landmark Knowledge Base to encode landmark-direction relations for multimodal path planning. Its strongest value for our setting is not a replacement of frontier exploration, but a lightweight planner frontend that can improve instruction adherence, waypoint proposal quality, and heading selection before the packet-verifier-controller chain. This suggests that direction-aware language parsing should be treated as an augmenting module for waypoint proposal quality, not as a substitute for 3D semantic frontier mapping or dynamics-aware execution.

OnFly [REF: 2603.10682] further strengthens the systems side of this argument from an aerial deployment perspective. Its onboard dual-agent design, hybrid keyframe/latest-frame memory, and semantic-geometric verification pipeline show that aerial VLN performance depends not only on semantic planning quality, but also on whether the runtime can maintain safe progress monitoring under onboard latency constraints. In our framing, this means that packet quality should be judged together with *verifiability* and *runtime continuity*, rather than by semantic relevance alone.

LiveVLN [REF: 2604.19536] further pushes the field toward temporally grounded, online navigation by exposing the deployment inefficiency of stop-and-go execution. It proposes a training-free runtime that splits an action sequence into an executed prefix, a guard buffer, and a revisable tail, enabling guarded handoff and real-time adaptation while preserving execution continuity. For our setting, its main value is not replacing aerial exploration itself, but motivating a **live packet refresh** mechanism that keeps frontier proposals updateable under streaming observations without forcing full replanning at every step. Together, these works motivate us to evaluate planner frontend quality not only by semantic relevance, but also by direction-conditioned adherence, verifier compatibility, and runtime continuity under packetized execution.

### 2.5 Packet-Centric Framing and the Limits of Existing Aerial VLN Pipelines

A common limitation across current aerial VLN systems is that planning quality, execution continuity, and downstream controllability are still entangled. Landmark-centric methods often produce semantically plausible targets without explicitly representing directional priors. Runtime-oriented systems improve continuity, but usually treat the planner output as a transient action list rather than a reusable interface object. Modular onboard systems add semantic-geometric verification, yet the intermediate representation passed from planner to controller is still weakly formalized. As a result, it remains difficult to diagnose whether failure comes from poor waypoint semantics, stale action handoff, verifier mismatch, or low-level control drift.

This motivates our packet-centric formulation: instead of asking only whether the planner predicts the right next move, we ask whether it emits a **semantic waypoint packet** that is direction-aware, budget-bounded, verifier-compatible, and refreshable online. Under this framing, the key unit of evaluation is not just task success, but whether the packet can survive the full `planner → verifier → controller` chain with low staleness, low heading drift, and low handoff failure. This reframes aerial VLN from pure navigation prediction toward a deployment-ready interface problem.

A further execution-facing constraint follows directly from the AirSpark owner notes: **a packet counts as a deployable interface only if its fields can be serialized into the AirSpark episode schema and then actually consumed by the WaypointExecutor**. Concretely, `goal_type`, `target_pose`, `yaw_hint`, `altitude_band`, `semantic_confidence`, `progress_score`, and `safety_budget` must admit a stable mapping into `manifest.json`, `frames.jsonl`, and `actions.jsonl`, rather than remaining paper-only abstractions. If this mapping is absent, the method may still produce a verifier-compatible planner output, but it should not be described as a true planner-controller contract.

A second design decision follows from our current research organization. We deliberately separate **benchmark construction** from **method contribution**. The broader AirManip-Bench effort is intended to study dataset design, episode generation, and full Search→Track→Approach→Contact→Recovery evaluation at the platform level. In contrast, the present paper asks a narrower method question: can a combination of **3D semantic frontier mapping** and a **Semantic Waypoint Packet** make aerial VLN outputs more executable for downstream controllers? This split keeps the current paper focused and allows benchmark claims and method claims to be evaluated independently.

### 2.6 Baseline Families and Attribution-Consistent Reading Rules

To avoid inflating the contribution of a packet-centric aerial VLN system, we organize recent baselines into four *attribution families* and constrain what each family is allowed to explain. This is not merely a writing convenience; it is a deployment-facing accounting rule. If a baseline improves one failure surface but does not alter the others, its gain should stay attached to that surface instead of being retroactively merged into a larger "better aerial planner" narrative.

The first family is **clause- and direction-preserving planner frontends**, represented by FineCog-Nav [REF: 2604.16298] and LookasideVLN [REF: 2604.17190]. These methods are most informative when the dominant question is whether language-side structure is preserved before execution: clause-level intent, egocentric directional relation, memory-backed landmark anchoring, and sentence-to-waypoint consistency. In our paper, gains from this family are therefore only allowed to explain improvements in *direction-conditioned instruction adherence*, *pre-verifier semantic mismatch reduction*, and *packet-facing clause retention*. They are not sufficient evidence for runtime survivability, controller compatibility, or cross-stage recovery on their own.

The second family is **runtime-survivability and live-execution systems**, represented by OnFly [REF: 2603.10682] and LiveVLN [REF: 2604.19536]. Their primary value lies in maintaining executable continuity under latency, monitoring, and refresh constraints. In our attribution rules, gains from this family are allowed to explain improvements in *verifier pass rate under onboard latency*, *packet staleness reduction*, *guarded execution continuity*, and *handoff survivability*. They should not be over-read as evidence that the underlying semantic planner is intrinsically better at clause interpretation or frontier ranking.

The third family is **representation-level geometry alignment**, represented by SpatialFly [REF: 2603.21046] and related geometry-guided aerial grounding methods. These methods matter when the dominant failure comes from 2D-3D mismatch, viewpoint distortion, or weak structural alignment between semantic tokens and spatial geometry. In our framework, gains from this family are only allowed to explain *representation-level grounding robustness*, *cross-view alignment stability*, and *geometry-aware path smoothness*. They are not sufficient evidence that the system has solved packet serialization, verifier compatibility, or long-horizon recovery.

The fourth family is **recoverability- and replanning-aware systems**, represented by OpenVLN [REF: 2511.06182] and other value-guided aerial replanning pipelines. Their value is that they ask whether a locally executable step preserves a future correction corridor under uncertainty. In our reading rules, gains from this family are only allowed to explain *recovery-corridor preservation*, *replanning-value retention*, and *long-horizon correction friendliness*. They should not be over-read as proof of better clause parsing, lower-latency execution, or cleaner packet handoff.

### 2.7 WildOS-Style Open-Vocabulary Outdoor Search as a Negative-Control Frontier Family

WildOS [REF: 2602.19308] is not a canonical aerial VLN system, yet it is highly informative as a **negative-control frontier family** for our setting because it cleanly separates open-vocabulary target search from packetized language-conditioned handoff. Its core pipeline combines geometry-safe exploration, semantic boundary-node scoring, traversability prediction, and particle-filter-based long-range target belief updates in complex outdoor scenes. This makes it a strong reference for *open-vocabulary outdoor search competence* without automatically granting clause-level instruction following, direction-conditioned packet serialization, or NtM handoff accountability.

For D06, the main value of WildOS is therefore diagnostic rather than substitutive. If a future D06 variant improves outdoor target finding under weak language structure, that gain may still be fully explainable by a WildOS-like frontier-search family rather than by a better aerial VLN packet interface. In our attribution discipline, this family is only allowed to explain improvements in **open-vocabulary target recall**, **boundary-node exploration efficiency**, and **long-range semantic search stability**. It should not be over-read as evidence of stronger consume-time packet preservation, direction-aware clause adherence, or manipulation-ready handoff value.

This distinction matters because outdoor success in aerial embodied tasks can look deceptively similar across two very different routes: one route finds the right region through geometry-plus-semantic search, while the other preserves a language-grounded packet through the full `planner → verifier → controller → handoff` chain. Our paper should explicitly prevent these routes from being collapsed into the same headline. WildOS therefore serves as a useful matched negative-control family for any D06 result that appears strong in outdoor search but remains under-closed at consume time.

### 2.8 Negative-Control Route Discipline for Outdoor Search Transfer

To keep D06 honest when borrowing evidence from outdoor open-vocabulary search systems, we require an explicit **route-discipline check** before any transfer claim is promoted. A WildOS-style gain may enter our paper only through the route `frontier search competence → region discovery stability → target-zone narrowing`; it is not allowed to jump directly into `direction-aware packet preservation`, `consume-time controller compatibility`, or `NtM handoff readiness`. In other words, outdoor search evidence may justify a stronger *where-to-look-next* story, but not by itself a stronger *what-packet survived until use time* story.

Operationally, we treat WildOS-like systems as matched negative controls whenever a D06 ablation improves outdoor success while remaining weakly supervised at clause level or under-logged at controller-consumption time. If the dominant gain disappears after conditioning on boundary-window packet fields, then the result should stay frozen as **search-route improvement only**. If the gain survives and additionally preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}`, it may be promoted to packet-contract language; only rows that also preserve `A^{shell}` are allowed to support manipulation-ready handoff claims. This discipline prevents us from over-crediting semantic search improvements as if they had already solved late-stage packet accountability.

### 2.9 Active-Elicitation Support improves Planner-Side Disambiguation but does not by itself Preserve Consume-Time Packet Honesty

MINT [REF: 2603.07824] exposes another route that can look deceptively strong in aerial VLN: **interactive or active elicitation before execution**. By querying missing constraints or refining task intent before committing to a plan, such systems may substantially improve planner-side ambiguity resolution, region narrowing, and waypoint proposal cleanliness. For D06, this is useful because active elicitation can reduce pre-verifier semantic mismatch and make `Q^{reason}_{route}` or `Q^{lang}_{narrow}` look much stronger. But that gain still lives primarily on the route `task disambiguation → proposal cleanup → better narrowing / better planner-side packet proposal`; it does not by itself prove that the emitted packet survives refresh episodes or remains honest at controller consumption.

We therefore treat MINT-style methods as an **active-elicitation support family**. Gains from this family are allowed to explain improvements in ambiguity resolution, query-efficient intent completion, and planner-side proposal quality, but they are not allowed to promote a row into packet-contract or NtM language unless the same row still preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}` after elicitation has already been subtracted. Only rows that additionally preserve `A^{shell}` may support manipulation-ready handoff claims. This matters because a system that asks better questions may still produce a packet that decays under streaming observations or loses shell validity before use time.


### 2.10 Search-Side Complementarity improves Narrowing Quality but does not by itself Close Packet Accountability

Recent local anchors make the search-side structure of D06 sharper. LMPath [REF: 2605.13782] improves **language-mediated region narrowing** by converting target-environment relations into aerial exploration priors before the target is directly seen. StereoNav [REF: 2605.13328] improves **geometry-grounded target anchoring** by using stereo cues and target-location priors to stabilize grounding once the target becomes partially observable. WildOS [REF: 2602.19308] strengthens **open-vocabulary outdoor search competence** through geometry-safe exploration and boundary-node semantic search. AgentVLN [REF: 2603.17670] adds a reasoning-side complement: modular VLM-as-Brain routing can improve clause decomposition, skill selection, and pre-verifier waypoint proposal quality without forcing a monolithic end-to-end controller. Taken together, these systems show that aerial navigation can become much stronger long before a packet is ever consumed by the low-level controller.

For D06, this means search-side gains should be treated as **complementary narrowing families** rather than implicit packet wins. LMPath-like gains are allowed to explain earlier `Q^{lang}_{narrow}` improvements and lower wasted coverage. StereoNav-like gains are allowed to explain stronger `Q^{geo}_{narrow}` and lower cross-view anchor drift. WildOS-like gains are allowed to explain region-discovery stability and open-vocabulary target recall. AgentVLN-like gains are allowed to explain stronger `Q^{reason}_{route}` and cleaner pre-verifier waypoint proposals. But none of these routes, alone or combined, is sufficient evidence that the packet preserves `A^{dir}`, `A^{ctx}`, `A^{guard}`, or `A^{shell}` through repeated refresh episodes and delayed controller consumption. In other words, better search and planner-side reasoning are prerequisites for D06, not yet proofs of packet accountability.

### 2.11 Route-Closure after Search-Side Complementarity

We therefore formalize a stronger route-closure discipline for search-side complementarity. A row that improves because LMPath narrows the search region better, StereoNav grounds the target more stably, WildOS recovers the correct outdoor semantic zone more reliably, or AgentVLN routes reasoning and skills more cleanly may only be promoted along the route `search-route win → refresh-stable packet support → consume-time packet preservation → manipulation-ready shell preservation`. If the evidence closes only on the first hop, the row freezes as **search-prior support** or **reasoning-route support**. If it survives refresh but loses packet fields before `w^{\dagger}_{consume}`, it freezes as **refresh-stable packet support**. Only rows preserving `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}` may be promoted to packet-contract language, and only rows that additionally preserve `A^{shell}` may support NtM handoff claims.

This rule is especially important for D06 because recent local evidence now gives us four different search-side explanations with very different semantics: language prior quality, geometry-grounded anchoring quality, open-vocabulary outdoor search quality, and modular reasoning-route quality. Without an explicit route-closure protocol, those gains are too easy to merge into a single inflated story about packet survival. Our paper should instead force them to remain separate until delayed consumption evidence actually closes the loop.

### 2.12 Search-Side Local-Execution Shells are Necessary but Not Sufficient for Packet Claims

Recent low-level UAV planning work such as KIO-planner [REF: 2605.19703] shows that a strong **local execution shell** can materially improve geometric executability, minimum obstacle clearance, and inference latency in cluttered or narrow environments. This matters for D06 because any aerial VLN packet eventually has to pass through a local controller-facing execution layer, and poor near-field safety can destroy even a semantically correct packet. However, KIO-style gains still live primarily on the route `better local depth attention / safety shielding → stronger short-horizon geometric feasibility`, not on the route `direction-aware clause preservation → refresh-stable context retention → consume-time packet survival`.

We therefore treat local execution-shell methods as a separate support family. Their gains are allowed to explain improvements in **Geometric Executability Gate** pass rate, minimum-clearance robustness, and near-field controller safety under matched latency. But they are not allowed to justify stronger `Q^{lang}_{narrow}`, `Q^{reason}_{route}`, or packet-contract promotion unless the same row also preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}`; `A^{shell}` remains the final gate for NtM handoff claims. This distinction is useful because a D06 variant may win by attaching a much better local shell to an otherwise unchanged planner, and that should be reported honestly as **execution-shell support** rather than as evidence that the packet interface itself has already become stronger.

### 2.13 Local Execution-Shell Support and Data-Scaling Support require an Explicit Experiment-Side Freeze

Recent local anchors sharpen two additional non-packet explanations that D06 must keep separate in both writing and experiments. **KIO-planner** [REF: 2605.19703] shows that a strong local execution shell can materially improve near-field geometric executability, obstacle clearance, and short-horizon safety under tight latency budgets. **FlyMirage** [REF: 2605.19600] shows that large gains may also come from upstream synthetic-data scaling, where generative world models improve data coverage, trajectory diversity, and pretraining priors before any consume-time packet field is ever tested. **LMPath** [REF: 2605.13782] and **StereoNav** [REF: 2605.13328] make the planner-side side of this story even sharper: language-mediated region priors and stereo-grounded target-location priors can both improve early narrowing and reduce wasted search effort before packet survival is ever stress-tested. All four routes are valuable for D06, but none should be allowed to silently inflate packet claims.

We therefore freeze them as distinct support families with different ceilings. KIO-style methods are allowed to explain stronger **geometric executability**, **near-field safety compliance**, and **controller-side local robustness**. FlyMirage-style methods are allowed to explain stronger **data coverage**, **trajectory-diversity support**, and **pretraining-prior quality**. LMPath-style methods are allowed to explain stronger **language-mediated search narrowing**, while StereoNav-style methods are allowed to explain stronger **geometry-grounded target anchoring** and lower cross-view drift. But none of these routes, alone or combined, is allowed to justify stronger consume-time packet preservation unless the same row still preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}`; `A^{shell}` remains the final gate for NtM handoff claims. In D06, all four families should therefore enter the evidential ladder only as subtraction routes: if a row's gain is exhausted by a better execution shell, better synthetic-data support, better language-side narrowing, or better geometry-side anchoring, it must freeze below packet-contract promotion.

### 2.14 Data-Scaling Support and Route-Closure for Synthetic Aerial Corpora

FlyMirage [REF: 2605.19600] strengthens an increasingly relevant upstream route for D06: **data scaling via generative world models**. By automatically synthesizing diverse UAV flight scenes, semantics-aligned trajectories, and dynamics-feasible long-horizon paths, it can materially improve data coverage and reduce the manual bottleneck of collecting aerial VLN supervision in the wild. For our paper, this matters because richer synthetic corpora may improve earlier `w^+`, lower planner-side search entropy, and stabilize proposal quality before deployment. In parallel, LMPath [REF: 2605.13782] and StereoNav [REF: 2605.13328] show that better planner-side priors can also shift `w^+` earlier: the former by turning language-mediated environment relations into search-region priors, the latter by using stereo-consistent target-location priors to stabilize geometric grounding after first target exposure.

However, FlyMirage-style gains still live primarily on the route `better data coverage → stronger pretraining prior → cleaner planner-side proposal`, LMPath-style gains on the route `better language prior → earlier region narrowing → lower wasted coverage`, and StereoNav-style gains on the route `better geometry prior → lower anchor drift → stabler target attachment`. None of these routes by themselves establishes stronger `A^{dir}`, `A^{ctx}`, `A^{guard}`, or `A^{shell}` at delayed controller consumption. We therefore treat synthetic-data scaling, language-prior narrowing, and geometry-prior anchoring as separate **pre-consumption support families**: their gains are allowed to explain stronger coverage, more diverse long-range priors, lower data sparsity, earlier semantic narrowing, and stronger cross-view anchoring, but they are not allowed to promote a row into packet-contract or NtM language unless the same row still preserves the consume-time packet fields after refresh episodes. This keeps D06 from confusing "trained on better data," "searched in a better region," or "anchored the target more stably" with "proved a stronger deployable packet interface."

### 2.15 Planner-Side Self-Awareness and Semantic-Geometric Decoupling as Strong but Bounded Mid-Level Baselines


AwareVLN [REF: 2605.22816] and Fly0 [REF: 2602.15875] sharpen a middle regime that sits between planner-side search priors and full packet-contract preservation. AwareVLN improves long-horizon navigation through **self-awareness and progress-structured reasoning**: it explicitly tracks agent state, task progress, and spatial relations, making the planner better at deciding *which stage it is in* and *whether the current evidence is sufficient to advance*. Fly0 instead decouples **semantic grounding** from **geometric planning**, showing that a UAV can obtain strong zero-shot navigation gains by first grounding a language-described target and then delegating motion realization to a geometry-driven planner. Both routes are highly relevant to D06 because they directly compete with our packet-centric framing at the level of mid-stage explanation: AwareVLN can explain better stage discipline and progress-consistent replanning, while Fly0 can explain cleaner semantic-to-geometric decomposition without requiring an explicit reusable packet.

For D06, these baselines should be treated as strong but bounded **mid-level support families**. AwareVLN-like gains are allowed to explain stronger planner-side stage awareness, progress-consistent recovery, and lower wrong-escalation rates at the `search → approach` boundary. Fly0-like gains are allowed to explain stronger semantic-geometric role separation, lower controller oscillation, and better zero-shot execution stability when semantic grounding is already correct. But neither route, by itself, proves that the emitted interface remains honest through repeated packet refresh and delayed controller consumption. In our route-closure discipline, AwareVLN-family gains freeze at **progress-aware planner support** unless they still preserve `A^{ctx}` and `A^{guard}` through `w^{\dagger}_{consume}`; Fly0-family gains freeze at **semantic-geometric decoupling support** unless they additionally preserve `A^{dir}`, `A^{ctx}`, and `A^{guard}` after geometric execution has already been matched. Only rows that also preserve `A^{shell}` are allowed to support full NtM handoff claims.

### 2.16 Planner-Side Active Perception, World Priors, and Semantic Completion are Strong Upstream Supports but not Consume-Time Packet Evidence

Recent local anchors make a new boundary unavoidable for D06: a row can improve substantially **before packet consumption** simply because the planner knows when to look again, inherits a stronger world prior, or fills unknown semantic regions more plausibly. **LMPath** [REF: 2605.13782], **StereoNav** [REF: 2605.13328], **WildOS** [REF: 2602.19308], and **AgentVLN** [REF: 2603.17670] already imply a planner-side active-perception route, where one more sensing action, a short verification orbit, or a tiny viewpoint shift can sharply reduce ambiguity before commitment. **SAGE** [REF: 2605.10118] adds a broader upstream route by supplying a physics-grounded semantic sandbox that improves open-world transfer priors, while **PLMD** [REF: 2605.05960] shows that language-conditioned semantic completion can sharpen goal maps even when the target region is still partially unobserved. All of these routes are highly useful, but none by itself proves that the emitted packet remains honest at delayed controller consumption.

For D06, we therefore freeze them as **planner-side active-perception support**, **world-prior transfer support**, **semantic-completion support**, and **world-action planning support** families. Active-perception gains are allowed to explain lower uncertainty before packet emission, cheaper one-more-look disambiguation, and reduced pre-commit target entropy. World-prior gains are allowed to explain stronger open-world transfer, lower early search entropy, and more robust planning in previously unseen semantics. Semantic-completion gains are allowed to explain better unknown-region filling, sharper goal maps, and lower wasted exploration in partially observed scenes. World-action gains are allowed to explain stronger short-horizon consequence modeling, lower rollout inconsistency, and better prediction-conditioned waypoint proposal quality. But none of these routes may by itself promote a row into packet-contract or NtM language unless the same row still preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}` after those upstream supports have already been subtracted; `A^{shell}` remains the final gate for handoff claims.

### 2.17 Route-Closure after Active Perception, World Priors, Semantic Completion, and World-Action Planning

We therefore extend the D06 route-closure discipline with four additional subtraction checkpoints. A row that improves because the planner takes one more informative look, because a stronger world prior already regularizes early search, because semantic completion fills useful unseen structure, or because a WorldVLN-style latent rollout better predicts the next short-horizon consequences may only be promoted along the route `upstream support win → refresh-stable packet support → consume-time packet preservation → manipulation-ready shell preservation`. If the evidence closes only on the first hop, the row freezes respectively as **active-perception support**, **world-prior transfer support**, **semantic-completion support**, or **world-action planning support**. If it survives refresh but loses packet fields before `w^{\dagger}_{consume}`, it may rise only to **refresh-stable packet support**.

Operationally, this means D06 should treat `Q^{unc}_{plan}`, `Q^{world}_{prior}`, `Q^{comp}_{sem}`, and `Q^{wam}_{plan}` exactly like earlier reasoning- and search-family diagnostics: they are legitimate explanatory wins, but only at their own ceiling. A planner that simply knows when to spend one more sensing action is not yet a stronger packet interface. A model that searches better because an upstream sandbox taught stronger semantic priors is not yet a stronger packet interface. A route that becomes cleaner because unknown semantics were completed more helpfully is not yet a stronger packet interface. A planner that predicts the next action-world consequences more coherently is not yet a stronger packet interface. Only residual evidence that survives these matched explanations and still preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through delayed consumption may support packet-contract language; only rows that additionally preserve `A^{shell}` may support NtM handoff claims.


### 2.18 World-Action Modeling for Aerial VLN is a Strong Planning Family but does not by itself Close Consume-Time Packet Accountability

WorldVLN [REF: 2605.15964] introduces the first explicit **autoregressive world-action model** for aerial VLN, converting instruction-conditioned short-horizon world evolution into executable waypoint actions and then re-encoding fresh observations back into the autoregressive context after each action segment. This is highly relevant to D06 because it occupies a route adjacent to our packet-centric framing: rather than emphasizing packet serialization first, it argues that better aerial VLN may arise from stronger latent world-transition anticipation, action-segment prediction, and rollout-aware policy improvement via Action-aware GRPO. In other words, WorldVLN can plausibly explain gains that come from **prediction-driven planning quality**, **closed-loop world-state anticipation**, and **better short-horizon waypoint consequence modeling** before delayed controller consumption is ever audited.

For D06, WorldVLN should therefore be treated as a distinct **world-action planning family** rather than as direct evidence that packet contracts have already become stronger. Its gains are allowed to explain stronger short-horizon action foresight, lower local rollout inconsistency, and better prediction-conditioned waypoint proposal quality. But unless the same row still preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}` after world-action advantages have already been subtracted, those gains must freeze as **world-action planning support** rather than packet-contract superiority. Rows that additionally preserve `A^{shell}` may support stronger NtM handoff language, but only after world-action, reasoning-route, search-prior, active-perception, world-prior, semantic-completion, local-shell, and runtime-continuity explanations have all been matched and discounted.

### 2.19 Route-Closure after World-Action Planning Gains

We therefore add one more route-closure rule specific to aerial world-action models. A row that improves because WorldVLN-style latent rollout prediction better anticipates what will happen after the next action segment may only be promoted along the route `world-action planning win → refresh-stable packet support → consume-time packet preservation → manipulation-ready shell preservation`. If the evidence closes only on the first hop, the row freezes as **world-action planning support**. If it survives refresh but loses packet fields before `w^{\dagger}_{consume}`, it may rise only to **refresh-stable packet support**. Only residual gains that survive matched world-action subtraction and still preserve `A^{dir}`, `A^{ctx}`, and `A^{guard}` through delayed consumption may support packet-contract language.

This matters because WorldVLN sits unusually close to D06's own narrative frontier: a strong autoregressive world-action model may make aerial navigation look much more coherent without yet proving that the emitted interface remains direction-honest, context-stable, guard-valid, and shell-preserving when finally consumed by the downstream controller or NtM stack. Our paper should therefore treat `Q^{wam}_{plan}` as another bounded diagnostic: it is evidence of stronger planning via latent world-transition reasoning, not yet evidence of stronger consume-time packet accountability.

### 2.19A POI-Grounded Final-Meters Reasoning improves late-stage arrival precision but does not by itself prove packet survival

POINav [REF: 2605.28237] isolates a failure regime that matters directly for D06 but is often hidden inside aggregate success metrics: **final-meters arrival**. Its POI-grounded reasoning framework shows that a navigation system may already succeed at coarse long-horizon region discovery while still failing to commit to the correct sign-, entrance-, or storefront-level endpoint in the last meters. For D06, this is highly relevant because a Semantic Waypoint Packet is only useful if its late-stage target commitment remains precise enough to survive the `approach → consume-time` boundary, especially when a downstream controller or manipulation stack expects a specific spatial anchor rather than merely the right semantic neighborhood.

We therefore treat POINav as a distinct **POI-grounded final-meters family**. Its gains are allowed to explain stronger late-stage target snapping, lower arrival offset in the final meters, and better separation between coarse-region success and POI-level commitment. However, these gains do not by themselves establish stronger `A^{dir}`, `A^{ctx}`, or `A^{guard}` preservation through delayed controller consumption. In our framing, a method may become much better at late-stage spatial convergence because its planner reasons explicitly about POIs, signage, or local anchors, yet still fail to serialize or preserve that commitment as a stable packet instance across verifier checks, executor binding, and live refresh.

### 2.19B Route-Closure after POI-Grounded Final-Meters Gains

We therefore add a dedicated route-closure rule for final-meters reasoning. A row that improves because POINav-style POI grounding sharpens the last segment of navigation may only be promoted along the route `POI-level arrival gain → refresh-stable packet support → consume-time packet preservation → manipulation-ready shell preservation`. If the evidence closes only on the first hop, the row freezes as **POI-grounded final-meters support**. If it survives refresh but loses packet fields before `w^{\dagger}_{consume}`, it may rise only to **refresh-stable packet support**. Only residual gains that survive matched POI-grounded subtraction and still preserve `A^{dir}`, `A^{ctx}`, and `A^{guard}` through delayed consumption may support packet-contract language.

This distinction is important for D06 because a strong final-meters arrival benchmark can make a method look much closer to deployment readiness than it really is. In the current paper, POI-grounded gains should therefore be credited first as evidence of stronger **late-stage target commitment** and only later, if supported by executor-facing logs, as evidence of a stronger packet interface. This keeps D06 from confusing better endpoint precision with genuine planner→verifier→executor accountability.

### 2.19C Planner–Executor Decoupling with Constraint Enforcement is a strong executor-interface baseline but not yet a packet-trace-preserving contract

PEACE [REF: 2606.00104] provides a newly relevant adjacent baseline for D06 because it explicitly separates a one-shot LLM planner from a deterministic ROS 2 / MAVLink executor and inserts an independent safety-constraint layer between them. This architecture is close to the AirSpark owner-note direction in spirit: it asks the planner to emit a structured mission object, asks the executor to consume only typed actionable fields, and audits execution through a separate enforceable interface rather than a monolithic end-to-end policy. For D06, PEACE is therefore valuable not as an aerial VLN frontier-search method, but as an **executor-interface baseline family** showing that planner–executor decoupling, typed command structures, and safety validation can already improve auditability and deployment stability without yet solving packet refresh honesty or consume-time semantic-thread preservation.

We therefore treat PEACE as a **planner–executor decoupling with constraint enforcement** family. Its gains are allowed to explain stronger typed mission decomposition, lower LLM-call frequency, better safety-policy separability, and clearer bind-time accountability between planner outputs and low-level execution APIs. However, these gains do not by themselves establish that a Semantic Waypoint Packet remains direction-honest, context-stable, and trace-preserving across `manifest.json`, `frames.jsonl`, `actions.jsonl`, and delayed `WaypointExecutor` consumption. In our framing, PEACE can justify stronger **executor-interface discipline** and **constraint-enforced bind-time robustness**, but it should still freeze below packet-contract language unless the same row preserves `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, and `c^{trace}` under online refresh and late handoff.

### 2.19D Route-Closure after Planner–Executor Decoupling Gains

We therefore add one more route-closure rule for planner–executor decoupling baselines. A row that improves because a PEACE-style architecture emits cleaner typed plans, inserts an explicit safety validator, or reduces executor ambiguity may only be promoted along the route `planner–executor interface gain → refresh-stable packet support → consume-time packet preservation → manipulation-ready shell preservation`. If the evidence closes only on the first hop, the row freezes as **executor-interface support** or **constraint-enforced bind-time support**. If it survives binding but loses semantic-thread consistency before `w^{\dagger}_{consume}`, it may rise only to **bind-stable but consume-fragile support**. Only residual gains that survive matched planner–executor subtraction and still preserve `A^{dir}`, `A^{ctx}`, `A^{guard}`, together with `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, and `c^{trace}`, may support packet-contract language.

This addition matters because D06 is increasingly converging toward an AirSpark-facing planner→verifier→WaypointExecutor interface claim. PEACE shows that better deployment behavior can already come from cleaner typed plans and external safety enforcement alone. Our paper should therefore explicitly subtract that explanation before claiming that the remaining gain truly comes from a stronger **Semantic Waypoint Packet** rather than merely from a cleaner planner–executor split.

### 2.19E AirSpark-Schema-Aligned Packet Serialization is Still Missing from Existing Planner–Executor Pipelines

PEACE [REF: 2606.00104] is especially valuable for D06 because it shows that UAV agents can already benefit from a clean `planner → executor → constraint-enforcer` split with typed mission plans and deterministic ROS 2 / MAVLink execution. However, even this stronger planner–executor discipline still stops short of the specific claim D06 now needs to make: that a **Semantic Waypoint Packet** can be serialized into an AirSpark-style episode schema and later consumed without losing the semantic thread that connected language intent, verifier approval, and executor behavior. In other words, PEACE strengthens **bind-time interface cleanliness**, but it still does not by itself establish **schema-aligned packet trace preservation** across `manifest.json`, `frames.jsonl`, and `actions.jsonl`.

For this reason, D06 should treat AirSpark-schema alignment as a separate missing capability rather than an automatic byproduct of planner–executor decoupling. A packet that is typed and safety-checked at planner output time may still fail to preserve `goal_type`, `target_pose`, `yaw_hint`, `altitude_band`, `semantic_confidence`, `progress_score`, or `safety_budget` as stable, replayable fields once the episode is logged and later replayed by `WaypointExecutor`. This gap is central to our method claim: the remaining research question is not whether a UAV planner can emit structured commands, but whether those commands remain **serializable, auditable, and consume-time faithful** inside the AirSpark execution stack.

### 2.19F PEACE-style Typed Planner–Executor Splits still need Delayed Consume-Time Trace Closure

PEACE clarifies one more important boundary for D06: a system may already achieve strong **typed mission decomposition**, **constraint-enforced bind-time safety**, and **low-frequency LLM planning** while still leaving the delayed consume-time trace under-specified. In our setting, this means a row can improve because the planner emits cleaner typed plans and the executor is easier to supervise, without yet proving that the same semantic thread survives from packet emission to actual `WaypointExecutor` consumption several steps later.

We therefore treat PEACE-like systems as strong **executor-interface supports** with a clear ceiling. Their gains are allowed to explain stronger bind-time field validity, lower planner–executor ambiguity, and cleaner safety-policy separation. But unless the same row still preserves `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, and `c^{trace}` under delayed refresh and replay, those gains must freeze below full packet-contract language. This is exactly the evidential boundary D06 now needs: typed missions and external constraints are valuable, but they are still not the same thing as a packet whose semantic thread survives the full AirSpark lifecycle.

This distinction matters because AirSpark turns the planner output into a logged episode artifact rather than only a transient ROS command. A D06-style packet must therefore survive at least three additional checks that PEACE does not directly close: `(i)` whether packet fields admit a stable schema-level serialization `C_{ser}`, `(ii)` whether the verifier-approved packet is the same object later bound by the executor `e^{bind}`, and `(iii)` whether delayed consumption still preserves semantic-thread consistency `c^{trace}` between emitted packet fields, observed frames, and low-level actions. A planner–executor split can already look safer and more deployable without satisfying any of these stronger logging-facing requirements.

### 2.19F Route-Closure after AirSpark-Schema Alignment Checks

We therefore add an explicit AirSpark-facing route-closure rule. A row that improves because of cleaner typed plans, stronger validator separation, or more stable executor binding may only be promoted along the route `bind-time interface gain → schema-aligned serialization support → consume-time packet preservation → manipulation-ready shell preservation`. If the evidence closes only on the first hop, the row freezes as **executor-interface support**. If typed plans serialize more consistently but semantic-thread continuity is not preserved through delayed execution, the row may rise only to **schema-aligned serialization support**. Only rows that still preserve `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, and `c^{trace}`, together with `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}`, may support packet-contract language; `A^{shell}` remains the final gate for NtM handoff claims.

This rule keeps D06 aligned with the owner-note constraint that the contribution must be judged at the **WaypointExecutor-consumable packet interface**, not merely at the level of cleaner planner decomposition. It also prevents us from misreporting a gain caused by better typed planning or stronger constraint enforcement as if it had already solved packet serialization honesty and consume-time trace accountability.

### 2.19G QuadAgent-Style Responsive Agent Systems Improve Planner-Side Responsiveness but still Stop Short of Executor-Consumable Packet Accountability

QuadAgent [REF: 2604.02786] adds another nearby baseline family that D06 should explicitly absorb. Its training-free responsive agent design combines vision-language reasoning, agile quadrotor control, and low-latency module orchestration, showing that aerial agents can become substantially more reactive to instruction and scene changes without requiring a full retraining loop. For D06, this is useful because such systems can explain stronger **planner-side responsiveness**, **instruction-conditioned replanning agility**, and **latency-tolerant aerial behavior** even when the intermediate interface remains only weakly typed. In other words, QuadAgent can make an aerial system look much more deployable at the behavior level while still leaving open whether the planner output survives as an auditable, schema-aligned, WaypointExecutor-consumable packet.

We therefore treat QuadAgent as a **responsive agent system** family. Its gains are allowed to explain stronger online responsiveness, lower reaction delay to scene/instruction changes, and better planner-side agility under edge constraints. However, these gains do not by themselves establish that the same semantic packet instance preserves `C_{ser}`, `e^{bind}`, `e^{consume}`, or `c^{trace}` across `manifest.json`, `frames.jsonl`, and `actions.jsonl`. In our route discipline, QuadAgent-style improvements should freeze at **responsive-agent support** unless the same row additionally proves stable packet serialization, executor binding, and consume-time semantic-thread preservation under online refresh.

### 2.19H Route-Closure after Responsive-Agent Gains

We therefore add one more route-closure rule for responsive agent baselines. A row that improves because the system reacts faster, replans more fluidly, or tolerates edge-side latency better may only be promoted along the route `responsive-agent gain → refresh-stable packet support → schema-aligned serialization support → consume-time packet preservation`. If the evidence closes only on the first hop, the row freezes as **responsive-agent support**. If the system remains responsive and refresh-stable but still lacks executor-trace consistency, it may rise only to **refresh-stable packet support** or **schema-aligned serialization support**, depending on which evidence is actually closed. Only residual gains that still preserve `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, `c^{trace}`, and `A^{dir}/A^{ctx}/A^{guard}` through delayed consumption may support packet-contract language.

### 2.19I POI-Grounded Final-Meters Planning is a Strong Late-Stage Baseline but not yet an AirSpark-Consumable Packet Contract

POINav [REF: 2605.28237] sharpens a failure regime that matters directly for D06: a system may already succeed at coarse region discovery yet still fail in the **final meters**, where the target must be committed as a specific sign, storefront, entrance, or object-facing anchor rather than a semantic neighborhood. For a packet-centric aerial VLN paper, this is highly relevant because the downstream `WaypointExecutor` does not consume a vague region-level belief; it consumes a typed target commitment with pose, yaw, altitude, and progress semantics that must remain usable at the `approach → executor-consume` boundary. POINav therefore provides a strong adjacent baseline for **late-stage target commitment quality** without automatically proving packet serialization or consume-time trace preservation.

For D06, POINav should be treated as a **POI-grounded final-meters support family**. Its gains are allowed to explain stronger last-segment target snapping, lower final-meters arrival offset, and better separation between coarse region success and POI-level commitment. However, these gains do not by themselves establish that the resulting waypoint packet can be serialized into AirSpark fields, survive verifier checks, remain semantically aligned through refresh, and then be consumed by the executor without thread drift. In our framing, POINav can justify stronger **late-stage arrival precision** and **POI-grounded endpoint selection**, but it should still freeze below packet-contract language unless the same row also preserves `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, and `c^{trace}` under delayed execution.

### 2.19J Route-Closure after POI-Grounded Final-Meters Gains

We therefore add a dedicated route-closure rule for final-meters planning. A row that improves because POINav-style POI grounding sharpens the last segment of navigation may only be promoted along the route `POI-level arrival gain → refresh-stable packet support → schema-aligned packet serialization → consume-time packet preservation → manipulation-ready shell preservation`. If the evidence closes only on the first hop, the row freezes as **POI-grounded final-meters support**. If it survives refresh but loses typed packet fields or semantic-thread consistency before delayed execution, it may rise only to **refresh-stable or bind-stable support**. Only residual gains that survive matched POI-grounded subtraction and still preserve `A^{dir}`, `A^{ctx}`, `A^{guard}`, together with `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, and `c^{trace}`, may support packet-contract language.

This distinction matters because a method can look far more deployable once it improves endpoint precision, yet still fail to produce an AirSpark-consumable packet object. In the current paper, POI-grounded gains should therefore be credited first as evidence of stronger **final-meters commitment** and only later, if executor-facing traces remain consistent, as evidence of a stronger planner→verifier→WaypointExecutor interface.

### 2.19K Reconstruction-Aware Executor Accountability is Still Missing from Current Aerial VLN Interfaces

Recent executor-facing baselines sharpen one more gap that is directly relevant to the current AirSpark-aligned D06 claim: even if a planner emits a clean typed plan, survives verifier approval, and reaches a deterministic executor, the system may still fail a stricter requirement—**post-hoc packet reconstructability**. In our setting, this means an evaluator should be able to recover the same semantic packet instance from `manifest.json`, `frames.jsonl`, and `actions.jsonl` without hidden planner-side state. Existing nearby systems such as PEACE [REF: 2606.00104] strengthen planner–executor decoupling and constraint-enforced execution, while OnFly [REF: 2603.10682] and LiveVLN [REF: 2604.19536] strengthen runtime continuity and online refresh. However, they still stop short of requiring that the emitted waypoint-level object can be **jointly reconstructed, identity-checked, and replay-audited** from cross-log evidence after execution.

For D06, this missing piece matters because a Semantic Waypoint Packet is meant to be more than a typed controller input. It is supposed to preserve a **semantic thread** from language intent through verifier approval to WaypointExecutor consumption, and that claim becomes much stronger only if the same packet can later be reconstructed from logs without privileged access to hidden planner memory. We therefore treat reconstruction-aware accountability as a distinct executor-facing requirement rather than a byproduct of bind success or consume stability. A system may already look strong at `e^{bind}` and even `e^{consume}`, yet still fail if replay from `manifest.json / frames.jsonl / actions.jsonl` cannot recover the same object/anchor/clause/thread tuple.

### 2.19L Route-Closure after Reconstruction-Aware Accountability

We therefore add one more route-closure rule for executor-facing claims. A row that improves because planner output is better typed, easier to bind, or more stable at consume time may only be promoted along the route `schema-aligned serialization → bind-stable consumption → trace-consistent execution → reconstruction-aware packet preservation → manipulation-ready shell preservation`. If the evidence closes only on the first hop, the row freezes as **schema-aligned serialization support**. If it survives bind and consume but fails cross-log replay, it may rise only to **consume-stable yet audit-fragile packet support** or **bind-stable but reconstruction-fragile support**. Only residual gains that still preserve packet identity after joint reconstruction from `manifest.json`, `frames.jsonl`, and `actions.jsonl` may support full packet-contract language.

This distinction is especially important for the current D06 paper because the owner notes explicitly require alignment with AirSpark's episode schema rather than abstract planner-only objects. In our framing, a deployable Semantic Waypoint Packet must not only be serializable and controller-consumable; it must also be **post-hoc reconstructable, trace-consistent, and replay-accountable** under the same semantic thread that justified its original release.

### 2.20 AirSpark-Schema-Aligned Waypoint Execution requires Consume-Time, Trace-Level, and Replay-Level Accountability

PEACE [REF: 2606.00104] reinforces an increasingly important adjacent baseline for D06: a planner may already gain substantial deployment robustness by emitting typed mission objects, delegating execution to a deterministic ROS 2 / MAVLink stack, and inserting an explicit safety-constraint layer between planning and actuation. This makes PEACE highly relevant as a **planner–executor decoupling with constraint enforcement** family. For our setting, its strongest value is not better frontier exploration or stronger language grounding, but cleaner bind-time semantics: typed fields, lower executor ambiguity, lower LLM-call frequency, and more auditable safety-policy separation.

For D06, however, these gains still stop short of the specific claim we now need to defend: a **Semantic Waypoint Packet** must not only be well-typed or planner-friendly, but must remain the *same semantic thread* after serialization into `manifest.json`, `frames.jsonl`, and `actions.jsonl`, after verifier approval, after executor binding, and after delayed `WaypointExecutor` consumption. We therefore freeze PEACE-like gains as **executor-interface support** unless the same row still preserves packet-facing evidence across `C_{ser}` (schema-serialization validity), `e^{auth}` (verifier authorization success), `e^{bind}` (executor bind success), `e^{consume}` (delayed consume-time semantic consistency), and `c^{trace}` (cross-file trace continuity). Better typed plans and cleaner bind-time safety are valuable, but they are not yet evidence that the packet is AirSpark-native, replay-accountable, and faithfully consumable by the executor.

### 2.21 Long-Horizon Asynchronous Aerial VLA improves progress discipline but does not by itself establish WaypointExecutor-consumable packet accountability

Recent local evidence adds a closely adjacent family that D06 must explicitly subtract before promoting stronger executor-facing claims. FLIGHT VLA [REF: 2606.06836] splits low-frequency pilot-style semantic reasoning from high-frequency diffusion control, while Goal2Pixel [REF: 2606.01621] emphasizes goal-conditioned visual grounding that sharpens progress-consistent waypoint commitment under long-horizon aerial instructions. Together, these systems strengthen a **long-horizon asynchronous planning-control** route: they can improve subgoal adherence, reduce stop-and-go oscillation, maintain smoother controller responsiveness, and keep progress traces more coherent across multi-stage flights without necessarily exposing a reusable AirSpark-schema-aligned packet object.

For D06, these gains should therefore be credited first as **progress-discipline support** and **goal-grounding support** rather than as packet-contract evidence. A method may become much better at deciding when to advance, when to refresh, and how to keep controller-time actions aligned with high-level intent, yet still fail to serialize the same semantic thread into `manifest.json`, `frames.jsonl`, and `actions.jsonl` in a way that survives delayed `WaypointExecutor` consumption and post-hoc replay. In our route-closure discipline, FLIGHT-VLA- and Goal2Pixel-style gains are allowed to explain stronger long-horizon subgoal maintenance, lower progress-trace fragmentation, and better goal-conditioned final commitment; however, they freeze below full packet-contract language unless the same row also preserves `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, and `c^{trace}` under online refresh.

### 2.22 Limitations of Existing Work

Existing aerial navigation and VLN approaches share critical gaps:
(1) No existing method provides a complete aerial VLN framework with 3D semantic frontier exploration.
(2) Ground VLN methods cannot transfer to aerial platforms due to 2D map assumptions and absence of dynamics modeling.
(3) Navigation and manipulation are treated as separate problems; no unified architecture handles the full search-to-grasp pipeline.
(4) Dynamics constraints are either ignored or handled as post-hoc filters rather than integrated into the exploration cost function.
(5) Existing aerial navigation benchmarks lack dynamics-aware metrics and 3D spatial reasoning evaluation.
(6) Directional cues and egocentric spatial relations in natural language remain under-modeled.
(7) Temporally updated online observations are still weakly integrated into planner-verifier-controller pipelines.
(8) The planner output is rarely formalized as a reusable packet-level interface.
(9) Existing responsive aerial systems improve latency but still provide limited evidence on whether packetized interfaces remain stable under downstream controller consumption and cross-stage handoff.
(10) Current aerial VLN papers also rarely align experiment logs, main tables, and result-paragraph promotion rules to the same contract.
(11) Even typed planner-executor architectures still rarely prove that semantic packet fields survive beyond bind time into delayed consume time under online refresh.
(12) Existing works almost never report whether planner outputs remain trace-consistent after serialization into a platform-native episode schema.
(13) Closely related systems still lack an explicit delayed-consumption violation taxonomy that separates directional drift, context-anchor loss, shell collapse, and guarded-prefix invalidation, making late-window failures hard to attribute cleanly.
(14) Even when methods report stronger online continuity, they seldom specify a minimal route-closure upgrade protocol for deciding when a row may be promoted from planner-time gain to consume-time packet-contract or NtM-handoff language.
(15) Existing planner–executor UAV systems with typed mission plans and explicit safety validators improve bind-time auditability, but they still do not show whether the same semantic packet remains reconstructable and trace-consistent after serialization into `manifest.json / frames.jsonl / actions.jsonl` and delayed `WaypointExecutor` consumption.
(16) More broadly, current aerial VLN papers rarely separate **schema-clean planner output** from **trace-preserving packet output**. A method may emit cleaner typed commands, lower LLM-call frequency, or stronger bind-time safety checks while still failing to preserve replay-equivalent semantic-thread continuity across refresh, logging, and final consume.
(17) Recent long-horizon asynchronous aerial systems improve subgoal adherence and real-time continuous control by splitting low-frequency semantic reasoning from high-frequency action generation, but they still rarely prove whether the intermediate object remains a **WaypointExecutor-consumable packet** rather than only an internal planner-control coupling.
(18) Correspondingly, the field still lacks a matched subtraction protocol that cleanly separates **long-horizon progress discipline** from **AirSpark-schema-aligned packet accountability**; without that split, gains from better asynchronous planning-control can be over-promoted into executor-facing packet claims.
(19) Goal-conditioned visual-grounding systems can improve final commitment and progress-trace smoothness, but current aerial VLN papers still rarely show whether those gains survive as a stable, replay-reconstructable semantic packet after delayed executor consumption.

## 3. Method

### 3.1 System Overview

We instantiate D06 on the **AirSpark** platform rather than an abstract simulator. AirSpark provides the city-scale UE5.7 aerial environment, batch episode generation, flight execution backend, and data logging stack used throughout this paper. Our method paper focuses on the **navigation-layer interface** inside that stack: language-conditioned 3D frontier reasoning produces a **Semantic Waypoint Packet**, which is then checked by a verifier and executed by the AirSpark waypoint/control backend.

At a high level, the system is organized as

```
Language Instruction
        │
   ┌────┴────┐
   │ VLM Goal │
   │  Parser  │
   └────┬────┘
        │
   ┌────┴──────────────┐
   │ 3D VL-Frontier Map │
   │ + Packet Proposal  │
   └────┬──────────────┘
        │
   ┌────┴──────────────┐
   │ Semantic Waypoint │
   │ Packet Verifier   │
   └────┬──────────────┘
        │
   ┌────┴──────────────┐
   │ AirSpark Waypoint │
   │ Executor / Ctrl   │
   └────┬──────────────┘
        │
   ┌────┴────┐
   │ Optional │
   │ NtM Hook │
   └─────────┘
```

The key point is that D06 is **not** trying to own the full Search→Track→Approach→Contact→Recovery stack in one paper. Instead, it studies a narrower but more defensible question: what packet structure should an aerial VLN planner emit so that its output remains semantically meaningful and executable when handed to a real flight execution interface? NtM remains the motivating downstream use case, but not a standalone contribution claim of this paper.

### 3.2 Aerial VL-Frontier Map (C1)

We represent the environment as a 3D occupancy voxel grid updated from UAV RGB-D observations. Frontier voxels are defined as free voxels adjacent to unknown voxels in 3D space, capturing exploration opportunities in the full volumetric workspace including vertical dimensions. Rather than directly selecting raw frontier cells, we cluster frontier voxels into candidate semantic viewpoints and attach a lightweight language-grounded proposal packet to each candidate, including target pose, yaw hint, altitude band, and semantic confidence.

For each frontier cluster, we compute a semantic relevance score from rendered observations and the goal embedding. We further augment this semantic score with a direction-conditioned prior extracted from the instruction. This module improves waypoint proposal quality without replacing 3D frontier exploration itself.

To support online execution, frontier proposals are emitted as packetized subgoals rather than one-shot plans. Each packet contains a guarded execution prefix and a revisable tail, allowing the system to refresh frontier ranking as new observations arrive while preserving short-horizon control continuity.

### 3.3 Dynamics-Aware Navigation Policy and Semantic Waypoint Packet

The frontier selection policy minimizes a composite cost over travel distance, estimated energy, collision risk, and semantic relevance. We treat dynamics awareness as an execution-critical component of the packet pipeline rather than a standalone headline contribution: the goal is not merely to rank semantically relevant frontiers, but to ensure that emitted packet fields remain valid under UAV motion constraints.

We define a **Semantic Waypoint Packet**
\[
\mathcal{W}_t = \{g_t^{type},\; p_t^{goal},\; \psi_t^{yaw},\; b_t^{alt},\; c_t^{sem},\; s_t^{prog},\; u_t^{budget},\; \rho_t^{guard}\},
\]
where `g_t^{type}` denotes the packet goal type, `p_t^{goal}` is the target pose, `\psi_t^{yaw}` is the yaw prior, `b_t^{alt}` is the altitude band, `c_t^{sem}` is semantic confidence, `s_t^{prog}` is progress state, `u_t^{budget}` is the safety/dynamics budget, and `\rho_t^{guard}` is the guarded execution prefix. This definition is chosen to align the method with the AirSpark execution stack rather than remain purely paper-internal.

Concretely, the packet fields are designed to map onto the AirSpark episode/runtime schema used by the waypoint executor and logging pipeline. The intended alignment is:
- `g_t^{type}` ↔ task/episode action intent in `manifest.json`
- `p_t^{goal}, \psi_t^{yaw}, b_t^{alt}` ↔ controller-facing waypoint command fields and per-step state traces in `actions.jsonl`
- `c_t^{sem}, s_t^{prog}` ↔ planner/verifier state recorded alongside observations in `frames.jsonl`
- `u_t^{budget}, \rho_t^{guard}` ↔ safety-envelope and guarded execution metadata used for verifier gating and rollout replay

To make this alignment executable rather than purely notational, we treat the packet as a typed intermediate object consumed by the AirSpark `WaypointExecutor`. The executor-side contract can be summarized as
\[
\Pi_{exec}(\mathcal{W}_t) = (\texttt{goal\_type},\; \texttt{target\_pose},\; \texttt{yaw\_hint},\; \texttt{altitude\_band},\; \texttt{safety\_budget},\; \texttt{progress\_tag},\; \texttt{guarded\_prefix}),
\]
where `semantic_confidence` is retained as verifier-side gating evidence rather than blindly forwarded as a control primitive. This separation matters: AirSpark does not need the planner's full semantic reasoning chain at the low-level execution API, but D06 still needs those semantics logged in a stable way so that packet failure can later be attributed to planner-side ambiguity, verifier-side rejection, or executor-side consumption drift.

We therefore distinguish three packet views throughout the method. The **planner view** keeps the full semantic packet for frontier ranking and refresh. The **verifier view** checks whether semantic confidence, context anchor, and budget remain consistent enough to authorize execution. The **executor view** consumes only the narrowed control-facing subset needed by `WaypointExecutor`. Following nearby planner–executor baselines such as PEACE and prediction-heavy UAV VLA systems such as WorldFly, this split lets us ask a sharper question than ordinary aerial VLN systems: did the gain come from better high-level planning/foresight, or from a packet object that genuinely survived schema serialization and delayed executor consumption? By making these three views explicit, D06 can ask a sharper question than ordinary aerial VLN systems: did the packet fail because the planner proposed the wrong semantic subgoal, because the verifier correctly blocked a stale packet, or because the executor consumed a superficially valid packet whose control-facing fields no longer preserved the intended semantic thread?


Execution follows a packet-verifier-controller chain. After the planner proposes a semantic waypoint packet, a verifier checks semantic validity, geometric executability, and budget compliance before the AirSpark waypoint executor commits to the guarded action prefix. If new observations invalidate the packet, only the revisable tail is refreshed while preserving the currently active semantic thread whenever possible.

### 3.4 WaypointExecutor-Aligned Packet Serialization, Consume-Time Logging, and Post-hoc Reconstruction Contract

To satisfy the AirSpark-facing owner guidance, our method must define the Semantic Waypoint Packet not only as a planner-side abstraction, but as a concrete object that can be **serialized, audited, replayed, reconstructed, and consumed** by the AirSpark execution stack. We therefore define a packet at time step `t` as

\[
\mathcal{W}_t = (g_t, p_t, \psi_t, h_t, s_t, r_t, b_t, \iota_t),
\]

where `g_t` is `goal_type`, `p_t` is `target_pose`, `\psi_t` is `yaw_hint`, `h_t` is `altitude_band`, `s_t` is `semantic_confidence`, `r_t` is `progress_score`, `b_t` is `safety_budget`, and `\iota_t` is a stable packet identifier used across AirSpark log surfaces. Unlike prior waypoint-list abstractions, `\mathcal{W}_t` is only considered valid if its fields admit a deterministic mapping into AirSpark artifacts:

- `manifest.json`: episode-level task intent, packet identifier, goal type, budget envelope, and scenario binding.
- `frames.jsonl`: frame-indexed observation evidence, semantic confidence evolution, progress-thread updates, and verifier-facing refresh history.
- `actions.jsonl`: executor-facing pose/yaw/altitude action binding, consume-time packet reference, and post-bind execution trace.

We formalize this serialization requirement as a packet-schema contract

\[
C_{ser}(\mathcal{W}_t) = \mathbb{1}[\phi_{man}(\mathcal{W}_t) \land \phi_{frm}(\mathcal{W}_t) \land \phi_{act}(\mathcal{W}_t)],
\]

where `\phi_{man}`, `\phi_{frm}`, and `\phi_{act}` indicate successful field-complete mappings into `manifest.json`, `frames.jsonl`, and `actions.jsonl`, respectively. A packet may be semantically plausible but still fail the method claim if `C_{ser}(\mathcal{W}_t)=0`, since planner-side semantics that cannot be faithfully serialized cannot become a deployable controller interface.

Beyond serialization, we require a **delayed consume-time event chain**

\[
E_t = (e_t^{emit}, e_t^{auth}, e_t^{bind}, e_t^{consume}, c_t^{trace}),
\]

where `e_t^{emit}` marks planner emission, `e_t^{auth}` verifier approval, `e_t^{bind}` executor binding, `e_t^{consume}` actual `WaypointExecutor` consumption, and `c_t^{trace}` indicates whether the same semantic packet thread remains cross-log consistent after replay and post-hoc audit. This decomposition matters because nearby baselines can explain only proper subsets of the chain: PEACE-style systems may improve `e_t^{bind}` through typed plans and safety-gated execution, while world-model systems such as AerialVLA or WorldVLN may improve planner-side proposal quality before the packet is ever consumed.

We therefore define a stricter **post-hoc reconstruction operator**

\[
R_{pkt}(\mathcal{L}_{man}, \mathcal{L}_{frm}, \mathcal{L}_{act}; \iota_t) \rightarrow \widehat{\mathcal{W}}_t,
\]

where `\mathcal{L}_{man}`, `\mathcal{L}_{frm}`, and `\mathcal{L}_{act}` are the three AirSpark log surfaces and `\widehat{\mathcal{W}}_t` is the packet reconstructed only from those logs for identifier `\iota_t`. The reconstruction is considered faithful only if

\[
\Delta_{recon}(\mathcal{W}^{consume}_t, \widehat{\mathcal{W}}_t) \le \epsilon_{pkt}
\]

for all controller-critical fields (`goal_type`, `target_pose`, `yaw_hint`, `altitude_band`, `semantic_confidence`, `progress_score`, `safety_budget`) and if the semantic-thread label implied by language intent remains unchanged across replay. This lets us separate two failure modes that prior work often conflates: **controller-consumable command compactness** (the command was easy to execute) versus **cross-log replay equivalence** (the same packet can be reconstructed and audited later). A typed, compact command object may be perfectly usable by the executor and still fail the stronger requirement that AirSpark logs are sufficient to recover the same accountable packet after the run.

Operationally, D06 only upgrades a planner output to a true WaypointExecutor-aligned packet contract when three conditions hold simultaneously: (i) `C_{ser}(\mathcal{W}_t)=1`, (ii) the delayed event chain reaches `e_t^{consume}=1`, and (iii) `c_t^{trace}=1` together with faithful reconstruction under `R_{pkt}`. If any of these fail, the row must be frozen below full packet-contract language and re-described as planner-side support, bind-time support, or consume-fragile support depending on the first broken link. This is the key engineering distinction between “a structured aerial command that sometimes works” and “a semantic packet that can be executed, audited, replayed, reconstructed, and trusted inside AirSpark.”

A further adjacent pressure comes from **multi-UAV continuity systems** such as topology-aware handover tracking [REF: 2605.15779]. These systems already show that cross-view continuity can be made highly stable through geometric overlap, topology priors, and deterministic handover logic, but they still optimize **target identity continuity across cameras or vehicles**, not **semantic packet-thread continuity across planner, verifier, and executor logs**. For D06, this matters because a row may look much more stable at the behavior level simply because view-to-view identity transfer became cleaner, while the emitted semantic packet is still not schema-aligned, replay-equivalent, or consume-time accountable inside AirSpark. We therefore treat topology-aware handover as a **cross-view continuity support** family: it may explain stronger target persistence and lower handover fragmentation, but it does not by itself raise the evidence ceiling beyond executor-facing packet accountability.

A second upstream pressure comes from **prompt-conditioned perception robustness** in difficult UAV sensing regimes. Nighttime or viewpoint-shift tracking systems such as DPTracker [REF: 2603.19628] can materially improve low-light target persistence and view-adaptive feature stability before any planning-stage packet is even emitted. For D06, this is useful because better perception can reduce pre-verifier ambiguity, refresh noise, and false packet invalidation under challenging flight conditions. But these gains still live primarily on the route `better observation quality → cleaner target hypothesis → stronger planner-side packet proposal`; they do not by themselves prove that the resulting packet remains serializable, bind-stable, consume-faithful, or reconstructable from AirSpark logs. We therefore freeze such systems as **perception-robustness support** unless the same row also closes `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, `c^{trace}`, and `R_{pkt}` under delayed executor use.

### 3.5 Dynamics-Aware Frontier Ranking

To keep D06 grounded in the AirSpark engineering stack rather than in paper-only packet notation, we further make the planner-to-executor serialization path explicit. Let
\[
\Sigma_{exec}(\mathcal{W}_t) = (m_t, f_t, a_t)
\]
denote the triplet written into `manifest.json`, `frames.jsonl`, and `actions.jsonl` for one packet episode. Here `m_t` stores episode-level intent and packet identity, `f_t` stores observation-aligned verifier state and semantic context, and `a_t` stores the executor-consumable command slice actually issued to `WaypointExecutor`. Under this view, a packet is deployable only if the same semantic thread can be followed across all three logging surfaces rather than being reconstructed retrospectively from loosely coupled traces.

We therefore require field-level serialization consistency:
\[
C_{ser}(\mathcal{W}_t)=\mathbb{I}[g_t^{type}\leftrightarrow m_t]\cdot
\mathbb{I}[(p_t^{goal},\psi_t^{yaw},b_t^{alt})\leftrightarrow a_t]\cdot
\mathbb{I}[(c_t^{sem},s_t^{prog})\leftrightarrow f_t]\cdot
\mathbb{I}[(u_t^{budget},\rho_t^{guard})\leftrightarrow (f_t,a_t)].
\]
If `C_{ser}=0`, then any downstream success may still reflect a useful planner or controller, but it cannot be honestly claimed as evidence for a **deployable packet interface**, because the packet fields were not preserved as a stable executor-facing object.

This serialization contract also sharpens the consume-time accountability boundary. We distinguish **packet emission**, **verifier authorization**, **executor binding**, and **executor consumption**. Emission means the planner created `\mathcal{W}_t`; authorization means the verifier allowed guarded execution; binding means `WaypointExecutor` accepted the command slice; consumption means the executor actually stepped through the guarded prefix while preserving the same progress tag, yaw/altitude intent, and semantic thread recorded in `frames.jsonl`. In symbols, we define a consume-time executor audit
\[
E_t=(e_t^{emit},\;e_t^{auth},\;e_t^{bind},\;e_t^{consume},\;c_t^{trace}),
\]
where `c_t^{trace}` measures cross-log consistency between planner packet identity and the action trace observed during execution.

In this round we further strengthen the contract with a **reconstructability requirement**. A packet should not count as executor-consumable only because its fields were accepted at bind time; it should also be possible for a post-hoc auditor to deterministically recover the same semantic packet from platform logs. Concretely, `manifest.json`, `frames.jsonl`, and `actions.jsonl` together should be sufficient to replay the exact packet UUID, goal semantics, guarded-prefix slice, and consume-time control intent that the executor used. This adds a second boundary beyond serialization completeness: a packet may fit the schema during execution and still fail audit if the emit-time semantics, frame-index alignment, and consume-time action slices cannot be jointly reconstructed afterward.

We therefore interpret `c_t^{trace}` more strictly as a **trace reconstructability** term rather than a loose consistency score. High `c_t^{trace}` means that planner emission, verifier approval, executor bind, and delayed consumption can all be replayed as one continuous semantic thread from logs alone; low `c_t^{trace}` means the row should freeze below packet-contract language even if task success looks strong. This distinction is useful for AirSpark because the platform turns planner outputs into persistent episode artifacts rather than transient ROS commands.

We additionally summarize this last replay-facing boundary with a dedicated reconstruction diagnostic
\[
Q^{recon}_{pkt} = \Omega_{recon}(\text{joint log recoverability},\; \text{reconstructed-packet identity consistency},\; \text{hidden planner-state dependence}),
\]
which measures whether the same packet instance can be post-hoc recovered from `manifest.json`, `frames.jsonl`, and `actions.jsonl` without privileged planner memory. In D06, `Q^{recon}_{pkt}` sits strictly above bind and consume: a row may already look strong on `e_t^{bind}` and `e_t^{consume}` while still failing reconstruction if the object/anchor/clause/thread tuple cannot be replayed as the same packet identity. We therefore treat `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, `c_t^{trace}`, and `Q^{recon}_{pkt}` as a nested executor-facing ladder; only rows that survive all six steps may support full packet-contract language.

This gives D06 a more concrete late-window failure taxonomy. A packet may fail because serialization is incomplete (`C_{ser}=0`), because the verifier blocks a stale or budget-violating packet (`e_t^{auth}=0`), because `WaypointExecutor` cannot bind the control-facing fields (`e_t^{bind}=0`), or because the executor consumes a bound packet while silently drifting away from the planner-side semantic thread (`e_t^{consume}=0` or low `c_t^{trace}`). By forcing these distinctions into the method, we stop treating AirSpark merely as a benchmark substrate and instead use it to test the central D06 thesis: **Semantic Waypoint Packet + 3D VL-Frontier Map should improve not only where the UAV goes, but whether the planner output survives as an honest executor-consumable interface.**

ImagineUAV [REF: 2606.01205] and AirDreamer [REF: 2606.03252] sharpen this boundary from the world-model side. ImagineUAV strengthens the route `instruction-conditioned world imagination → pose extraction → kinodynamic planning`, while AirDreamer strengthens the route `world-model state abstraction → RL policy generalization → sim-to-real navigation robustness`. Both can plausibly explain stronger upstream proposal quality, lower local rollout inconsistency, and better dynamics-aware path feasibility before controller consumption. In our framing, however, these are still **planner-side / world-model support families** rather than direct packet-contract evidence. Unless the same row also preserves `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, and replay-oriented `c_t^{trace}` under AirSpark logging and delayed `WaypointExecutor` use, those gains must freeze below full packet-contract language. This keeps D06 honest: stronger imagination or stronger generalist navigation is not yet the same claim as a planner output that remains serializable, bind-stable, consumable, and reconstructable as one semantic packet instance.

POINav [REF: 2605.28237] sharpens the boundary from the final-meters side. Its POI-grounded reasoning can materially improve the last segment of navigation by snapping to the correct storefront, entrance, or sign-level anchor after the system has already found the right semantic region. For AirSpark-facing D06, this is highly relevant because a late-stage packet that is only region-correct but endpoint-vague may still be useless to `WaypointExecutor` or the downstream Approach→Align boundary. We therefore treat POINav as a **POI-grounded final-meters support** family: it may explain lower final-meters offset, stronger endpoint commitment, and cleaner approach-stage arrival, but it still does not by itself establish that the same target commitment survives as a schema-aligned, verifier-authorized, executor-bound, consume-time faithful, and post-hoc reconstructable packet. Unless these executor-facing checks also close, POINav-style gains must freeze as late-stage commitment support rather than full packet-contract evidence.

### 3.5 Navigate-then-Manipulate as the downstream application boundary

We treat Navigate-then-Manipulate (NtM) as the **downstream application boundary** of D06 rather than an independent contribution of this paper. The full Search→Track→Approach→Contact→Recovery chain belongs to the broader AirSpark/AirManip-Bench platform effort and interfaces directly with D07 at the manipulation phase. In the present paper, NtM matters because it provides the most demanding late-stage test of whether a navigation packet remains executable when handed off toward local interaction.

Operationally, D06 is responsible for the navigation-side output up to the `approach → align / contact-ready` boundary. Once semantic confidence, target pose covariance, and local safety-shell conditions jointly satisfy the handoff rule, the planner emits a packet tagged as `interaction-ready`, which can then be consumed by the downstream AirSpark/D07 local interaction stack. If later evidence invalidates the packet, the system is allowed to repair or downgrade it back to `approach` or `search`, but those downstream behaviors are evaluated here only insofar as they expose whether the packet interface was robust enough for handoff.

### 3.5 Consume-Time Packet Contracts and Claim Routing

A packet that is semantically correct at planner time should not be assumed to retain the same execution privilege under delayed controller consumption. We therefore distinguish planner-time, verifier-bind, and controller-consumption windows, and we track four consume-time packet fields: **directional prior survival** `A^{dir}`, **context-anchor survival** `A^{ctx}`, **manipulation-shell survival** `A^{shell}`, and **guarded-prefix survival** `A^{guard}`.

We further attach a reviewer-facing routing tuple
\[
\Gamma = (w^+,\; w^{\dagger},\; r^*,\; q^*)
\]
where `w+` is the earliest positive window, `w†` is the last honesty-preserving window, `r*` is the dominant evidence route, and `q*` is the weakest honest claim allowed by that route. This prevents D06 from merging planner-side clause gains, verifier-side continuity gains, and controller-side rescue gains into one inflated packet-first story.


### 3.6 World-Action-Subtracted Packet Proposal Ranking

Because aerial VLN may benefit strongly from short-horizon latent world-transition prediction even before any packet is consumed, we explicitly add a **world-action subtraction gate** before packet-level promotion. Let `Q^{wam}_{plan}` denote the planner-side gain attributable to WorldVLN-style autoregressive consequence modeling: if a method wins mainly because it predicts the next few world-action steps more coherently, that gain should first be credited to world-action planning support rather than directly to packet accountability.

Concretely, we extend the packet proposal score with a diagnostic decomposition
\[
G_{proposal}=G_{reason}+G_{search}+G_{unc}+G_{world}+G_{comp}+G_{wam}+G_{shell}+G_{runtime}+G_{packet},
\]
where `G_{wam}` captures gains explainable by better short-horizon world-transition anticipation and action-segment rollout quality. During evaluation, packet promotion is allowed only after matched world-action baselines have been subtracted. If the residual gain after removing `G_{wam}` still preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}`, the row may be promoted to packet-contract language; if it additionally preserves `A^{shell}`, it may support NtM handoff language. Otherwise, the row freezes as **world-action planning support** or, at most, **refresh-stable packet support**.

This rule keeps D06 honest against a very plausible alternative explanation: a model may simply be better at predicting what happens next, not yet better at preserving a reusable, direction-honest, context-stable, controller-consumable packet until the actual handoff boundary.

### 3.7 Refresh-Stable Packet Gating after Outdoor Search Narrowing

We extend the packet contract with a **refresh-stability gate** that activates after an outdoor-search module has already narrowed the candidate target zone. Intuitively, once the system believes it is searching in the right region, the key question is no longer only whether the next frontier is semantically promising, but whether repeated packet refreshes still preserve the same directional clause, context anchor, guarded execution prefix, and manipulation shell assumptions. We therefore define a refresh episode sequence `r_1, r_2, \dots, r_K` between `w^+` and `w^{\dagger}_{consume}` and require packet survival to be judged across the sequence rather than at one isolated window.

For each refresh episode `r_k`, the planner emits an updated packet \(\mathcal{W}_t^{(k)}\) while inheriting the previous packet's active semantic thread unless explicit evidence forces thread switching. The verifier then checks not only geometric executability and budget legality, but also whether the refreshed packet still preserves (i) directional clause consistency, (ii) context-anchor identity, (iii) guarded-prefix continuity, and (iv) local handoff shell validity. This turns refresh from a purely reactive runtime trick into a controlled packet-accountability mechanism.

We further distinguish two upstream narrowing priors before refresh is even invoked. A **geometry-grounded narrowing prior** uses multi-view depth or stereo-consistent target anchoring to stabilize where the packet should attach after the target first becomes partially observable. A **language-prior-guided narrowing prior** uses instruction-conditioned semantic search bias to stabilize which regions should be explored before direct target confirmation exists. These two priors improve different failure surfaces and therefore should remain separate in our method accounting: the former reduces cross-view anchor drift before packet update, while the latter reduces wasted search coverage before packet emission. Neither is allowed to count as consume-time packet superiority unless the resulting packet still passes the refresh-stability and handoff gates.

Concretely, we attach two auxiliary diagnostics to the pre-refresh stage: `Q^{geo}_{narrow}` for geometry-grounded narrowing quality and `Q^{lang}_{narrow}` for language-prior-guided narrowing quality. These terms are used only to explain earlier improvement in `w+` or reduced search inefficiency; they do not enter the promotion contract unless the downstream packet fields remain stable through `w^{\dagger}_{consume}`. This preserves a clean boundary between *helping the system search in a better place* and *proving that the packet stayed valid until use time*.

In parallel, we define a **latent-execution route tag** for compact end-to-end baselines such as AerialVLA. This tag is activated when the system improves trajectory stability or oracle-free autonomy through a direct observation-language-to-control mapping without explicitly exposing reusable packet fields. Such gains are allowed to raise the evidence ceiling from planner-time reasoning to **latent execution sufficiency**, but not to packet-contract preservation unless consume-time packet fields are still audited and preserved. This prevents D06 from misreading a strong minimalist VLA control baseline as if it had already validated a structured packet interface.

Concretely, we define a **refresh-stable packet gate**
\[
G_{\text{refresh}} = \mathbb{1}\Big[ \prod_{k=1}^{K} A^{dir}_{(k)} A^{ctx}_{(k)} A^{guard}_{(k)} = 1 \Big]
\]
for packet-contract promotion, and a stricter handoff gate
\[
G_{\text{NtM}} = \mathbb{1}\Big[ \prod_{k=1}^{K} A^{dir}_{(k)} A^{ctx}_{(k)} A^{guard}_{(k)} A^{shell}_{(k)} = 1 \Big]
\]
for manipulation-ready claims. Under this design, a method may benefit from WildOS-style outdoor narrowing or LiveVLN-style runtime continuity, but it still cannot be promoted to packet-contract or NtM language unless the packet survives refresh episodes all the way to consume time.

### 3.7 Reasoning-Route-Aware Packet Promotion Gate

We add a dedicated **reasoning-route-aware promotion gate** to prevent planner-side modular intelligence from being over-promoted into consume-time packet claims. Recent modular VLM-as-Brain systems such as AgentVLN show that stronger instruction decomposition, cleaner skill routing, and lighter edge-side planning can already improve search-time waypoint quality without yet proving that the resulting packet survives refresh episodes or controller consumption more honestly. We therefore treat the reasoning route as a separate evidence ladder that must be exhausted before any packet-interface promotion is allowed.

Concretely, after the planner emits a candidate packet, we log a reasoning-family diagnostic
\[
Q^{reason}_{route} = \Phi(\text{clause decomposition},\; \text{skill-routing fidelity},\; \text{pre-verifier semantic mismatch reduction})
\]
which measures how much of the gain is still attributable to modular reasoning before execution. This diagnostic may justify earlier `w+`, lower planner-side search entropy, or better pre-verifier waypoint proposals, but it does not by itself raise the claim ceiling beyond **planner-side modular reasoning gain**. Only after subtracting the reasoning-route explanation do we continue to the later gates for search-prior subtraction, compact-control subtraction, and consume-time packet survival.

### 3.8 Progress-Aware Escalation Control, Semantic-Geometric Decoupling, World-Prior Audits, World-Action Matching, POI-Grounded Final-Meters Matching, and Executor-View Packet Reduction

We further add two mid-level audits motivated by AwareVLN and Fly0, because both can create convincing improvements before full packet accountability is closed. The first audit is a **progress-aware escalation control** that tracks whether the planner promotes the agent from `search` to `approach` or from `approach` to `manipulate-ready` at the correct moment. Concretely, we maintain a stage-confidence vector
\[
\pi_t = (\pi_t^{search},\; \pi_t^{approach},\; \pi_t^{handoff})
\]
and define a wrong-escalation penalty when the active stage advances without sufficient support from packet fields, verifier evidence, and recent progress trend. This gives D06 a way to capture AwareVLN-style gains as **progress-consistent planning support** rather than letting them silently inflate packet-contract claims.

The second audit is a **semantic-geometric decoupling check** that asks whether a method's gain is mainly due to better role separation between semantic grounding and geometric execution. After a target hypothesis is grounded, we split downstream gains into a semantic-side term for target interpretation and a geometric-side term for controller-feasible realization. This yields a decoupling diagnostic
\[
Q^{dec}_{sg} = \Psi(\text{target grounding stability},\; \text{trajectory oscillation reduction},\; \text{goal-to-path realization fidelity}),
\]
which is allowed to justify stronger zero-shot execution stability and lower control jitter. However, `Q^{dec}_{sg}` does not by itself raise the claim ceiling beyond **semantic-geometric decoupling support** unless the packet still preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}`. In this way, D06 can absorb AwareVLN-style planner self-awareness and Fly0-style semantic-geometric separation without confusing either route with evidence that the reusable packet interface itself has already survived delayed use.

We additionally introduce a **world-prior audit** for SAGE- and PLMD-like gains, because stronger upstream world abstractions or semantic map completion can make the planner look substantially better before any packet is truly consumed. We define two diagnostics. The first is a world-prior transfer term
\[
Q^{world}_{prior} = \Upsilon(\text{open-world transfer gain},\; \text{physics-grounded prior quality},\; \text{early-stage search-entropy reduction}),
\]
which captures how much of the improvement is still attributable to learning in a physics-grounded semantic sandbox rather than to a better packet interface. The second is a semantic-completion term
\[
Q^{comp}_{sem} = \Omega(\text{unknown-region semantic completion quality},\; \text{goal-map sharpening},\; \text{exploration-waste reduction}),
\]
which captures how much of the gain still comes from predicting useful unseen semantics before direct confirmation. These diagnostics are allowed to justify stronger world-prior transfer and semantic-completion support, but they do not by themselves raise the claim ceiling beyond pre-consumption support families unless the packet still preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}` after those upstream advantages have already been subtracted.

We further make **world-action matching** explicit at the same audit layer, because WorldVLN-style autoregressive rollout gains are close enough to packet-centric planning that they can otherwise be misread as packet superiority. We summarize this route with
\[
Q^{wam}_{plan} = \Xi(\text{short-horizon consequence prediction},\; \text{rollout consistency},\; \text{prediction-conditioned waypoint proposal quality}),
\]
which measures how much of the gain is still explained by world-action planning quality before delayed consumption is audited. `Q^{wam}_{plan}` may justify a stronger **world-action planning support** interpretation, but it does not by itself promote a row to packet-contract language. In the current draft, this matched subtraction is especially important because **WorldVLN** already shows that an autoregressive WAM can directly decode short-horizon aerial world transitions into executable waypoint actions with 12%+ success-rate improvement and zero-shot real-drone transfer. That makes it a strong adjacent family for D06: if our packet-centric method still wins after matching WorldVLN, the residual should more credibly reflect planner→verifier→executor interface value rather than merely better next-step consequence prediction.

We also add a **POI-grounded final-meters audit** motivated by POINav, because late-stage arrival precision can create a strong but still bounded explanation before packet survival is closed. We summarize this route with
\[
Q^{poi}_{final} = \Theta(\text{POI-level arrival precision},\; \text{final-meters offset reduction},\; \text{late-stage target commitment stability}),
\]
which measures how much of the gain is still explained by stronger POI-grounded endpoint reasoning rather than by stronger packet serialization, verifier accountability, or executor-side semantic preservation. `Q^{poi}_{final}` may justify a stronger **POI-grounded final-meters support** interpretation, but it does not by itself promote a row to packet-contract language.

We additionally introduce a **planner–executor decoupling and constraint-enforcement audit** motivated by PEACE, because cleaner typed mission plans and external safety validators can also improve deployment behavior without yet proving packet-trace preservation. We summarize this route with
\[
Q^{pe}_{bind} = \Psi_{pe}(\text{typed plan completeness},\; \text{constraint-enforced bind stability},\; \text{planner–executor API clarity}),
\]
which measures how much of the gain is still explained by a better planner–executor split rather than by a stronger refreshable packet interface. `Q^{pe}_{bind}` may justify stronger **executor-interface support** or **constraint-enforced bind-time support**, but it does not by itself promote a row to packet-contract language. In the current draft, this matched subtraction is important because **PEACE** already shows that UAV mission execution can become safer and more auditable through a one-shot planner, deterministic executor, and independent constraint layer, even when online packet refresh and consume-time semantic-thread preservation remain under-specified.

We additionally introduce a **responsive-agent audit** motivated by QuadAgent, because faster reaction and more agile module orchestration can also create strong behavioral gains before packet accountability is closed. We summarize this route with
\[
Q^{resp}_{agent} = \Psi_{resp}(\text{reaction latency reduction},\; \text{instruction-conditioned replanning agility},\; \text{edge-side responsiveness stability}),
\]
which measures how much of the gain is still explained by planner-side responsiveness and low-latency orchestration rather than by stronger typed packet preservation. `Q^{resp}_{agent}` may justify stronger **responsive-agent support**, but it does not by itself promote a row to packet-contract language. In the current draft, this subtraction matters because **QuadAgent** already shows that a training-free responsive aerial agent can look substantially more deployable through better agility and responsiveness even when schema-aligned packet serialization and consume-time trace preservation remain unspecified.

We additionally introduce a **controller-interface abstraction audit** motivated by HANDOFF, because a cleaner planner-to-controller command space can also create strong executor-facing gains before packet accountability is closed. We summarize this route with
\[
Q^{cmd}_{iface} = \Psi_{cmd}(\text{controller-consumable command compactness},\; \text{planner-to-controller translation burden reduction},\; \text{downstream controllability under fixed planner}),
\]
which measures how much of the gain is still explained by a better intermediate command abstraction rather than by stronger packet serialization, verifier accountability, or replay-level trace preservation. `Q^{cmd}_{iface}` may justify stronger **controller-interface abstraction support**, but it does not by itself promote a row to packet-contract language. In the current draft, this subtraction matters because **HANDOFF** already shows that much of deployment robustness can come from optimizing the command abstraction itself, even when platform-native schema alignment and delayed consume-time packet replay are still unspecified.

We additionally introduce an **asynchronous long-horizon planning-control audit** motivated by FLIGHT VLA, because stronger task-state reasoning and better continuous-control stability can create very convincing long-horizon gains before any executor-facing packet contract is actually closed. We summarize this route with
\[
Q^{async}_{lh} = \Psi_{async}(\text{multi-stage progress adherence},\; \text{subgoal transition stability},\; \text{high/low-frequency control decoupling benefit}),
\]
which measures how much of the gain is still explained by a better asynchronous planning-control split rather than by stronger packet serialization, delayed consume-time fidelity, or replay-level accountability. `Q^{async}_{lh}` may justify stronger **long-horizon asynchronous support**, but it does not by itself promote a row to packet-contract language. In the current draft, this subtraction matters because **FLIGHT VLA** already shows that a pilot-style low-frequency reasoning module plus a high-frequency diffusion controller can substantially improve long-horizon aerial task completion without yet proving that the intermediate object survives as a schema-aligned, verifier-authorized, WaypointExecutor-consumable packet.

We additionally introduce a **grid-history search audit** motivated by grid-based view-selection aerial search systems, because stronger planner-side 3D search structure can also create convincing gains before any executor-facing packet claim is closed. We summarize this route with
\[
Q^{grid}_{hist} = \Psi_{grid}(\text{grid-view selection quality},\; \text{history-map utilization},\; \text{altitude-sensitive revisit discipline}),
\]
which measures how much of the gain is still explained by better planner-side volumetric search organization rather than by stronger packet serialization, bind stability, or consume-time semantic-thread preservation. `Q^{grid}_{hist}` may justify stronger **grid-history search support**, but it does not by itself promote a row to packet-contract language. In the current draft, this subtraction matters because **Grid-based View Selection and Map Construction for Air Ground Search and Rescue via a Vision-Language Model** already shows that explicit view-grid structure and history-map reasoning can materially improve aerial search quality without yet proving an AirSpark-consumable packet contract.

Finally, we make the **executor-view packet reduction** explicit, because AerialVLA and OnFly together expose a core ambiguity in D06's deployment story. AerialVLA shows that a compact end-to-end controller may already win through **latent execution sufficiency** without exposing reusable packet structure: its dual-view minimalist VLA maps visual observations and fuzzy directional prompts directly to continuous 3-DoF control and achieves strong oracle-free TravelUAV performance, so some apparent packet gains may actually come from a better end-to-end control manifold rather than from a stronger planner-controller interface. OnFly shows that a runtime stack may already win through **verifier-backed continuity and monitoring** even when the planner-to-executor object is only weakly formalized: its dual-agent decoupling, semantic-geometric verifier, and mixed keyframe memory improve onboard continuity and safety under latency. To separate these routes from genuine packet-interface gains, we define an executor-view reduction diagnostic
\[
Q^{exec}_{view} = \Lambda(\text{control-facing field sufficiency},\; \text{bind-time schema completeness},\; \text{consume-time trace preservation}),
\]
which asks whether the narrowed executor-facing packet slice remains sufficient for `WaypointExecutor` consumption without silently relying on planner-only latent state. If a row wins only because a minimalist end-to-end controller executes better under matched onboard budgets, it should freeze at **latent execution sufficiency**. If it wins because a stronger monitoring-and-verification stack preserves continuity while the executor-facing fields remain under-specified, it should freeze at **runtime continuity support**. Only rows that still preserve `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, and `c^{trace}` after these alternative explanations are matched may support stronger packet-contract language.

This round's local retrieval reinforces why `Q^{exec}_{view}` should remain a first-class audit rather than a prose-only caveat. The strongest direct hit is still **AerialVLA**, while QMD mostly recovers existing D06 notes instead of a mature executor-facing literature. That asymmetry suggests the field currently has much stronger evidence for **planner-side or end-to-end aerial control quality** than for explicit **planner→verifier→WaypointExecutor traceability**. We therefore treat executor-view reduction as a required residual test: if gains collapse once planner-only latent state is stripped away and only the control-facing fields actually consumed by AirSpark remain, the result must freeze below packet-contract promotion.

Operationally, we insert the following gate before packet promotion:
\[
G_{reason} = \mathbb{1}[\text{residual gain remains after conditioning on } Q^{reason}_{route}, Q^{world}_{prior}, Q^{comp}_{sem}, Q^{wam}_{plan}, Q^{poi}_{final}, Q^{pe}_{bind}, Q^{resp}_{agent}, Q^{cmd}_{iface}, Q^{grid}_{hist}, Q^{exec}_{view}].
\]
If `G_{reason}=0`, the row freezes at **reasoning-route support**, **world-prior transfer support**, **semantic-completion support**, **world-action planning support**, **POI-grounded final-meters support**, **executor-interface support**, **constraint-enforced bind-time support**, **responsive-agent support**, **controller-interface abstraction support**, **grid-history search support**, **latent execution sufficiency**, or **runtime continuity support**, depending on the dominant residual explanation. If `G_{reason}=1`, the row may proceed to search-prior subtraction, latent-execution matching, runtime continuity matching, and finally the packet-survival gates `A^{dir}`, `A^{ctx}`, `A^{guard}`, and `A^{shell}`. This ordering ensures that D06 only claims packet-contract superiority when the evidence cannot already be honestly exhausted by modular planner intelligence, upstream world priors, semantic map completion, autoregressive world-action planning, POI-grounded final-meters reasoning, planner–executor decoupling with constraint enforcement, responsive-agent agility, controller-interface abstraction, grid-history search structure, minimalist end-to-end execution, or verifier-backed runtime continuity alone.

### 3.9 Consume-Time Executor Interface Contract for AirSpark WaypointExecutor

To answer the owner-note requirement more directly, we further collapse the packet into the exact subset that must survive until **WaypointExecutor consume time**. Let the control-facing executor slice be
\[
\mathcal{W}^{exec}_t = (g_t^{type},\; p_t^{goal},\; \psi_t^{yaw},\; b_t^{alt},\; u_t^{budget},\; s_t^{prog},\; \rho_t^{guard}),
\]
where semantic confidence is retained in the verifier/logging path but not treated as a low-level actuator command. D06's central claim is therefore not merely that the planner can emit a rich semantic packet, but that this packet admits a stable **executor reduction** that remains sufficient for AirSpark `WaypointExecutor` to bind and consume without silently depending on planner-only latent state.

We make this explicit with a consume-time interface map
\[
\Gamma_{wp}: \mathcal{W}_t \rightarrow (\texttt{manifest.json},\; \texttt{frames.jsonl},\; \texttt{actions.jsonl},\; \texttt{WaypointExecutor API}),
\]
and require every executor-facing field to be both serializable and replayable. Concretely, `goal_type` and packet identity must appear in `manifest.json`; `progress_score`, verifier state, and semantic-thread annotations must remain traceable in `frames.jsonl`; and `target_pose`, `yaw_hint`, `altitude_band`, guarded-prefix length, and safety-budget realization must be represented in `actions.jsonl` in a form that can actually be consumed by `WaypointExecutor` rather than reconstructed post hoc from planner logs.

This yields a stricter consume-time criterion than ordinary packet logging. We define
\[
\kappa_{consume}(\mathcal{W}_t)=\mathbb{1}[C_{ser}=1]\cdot \mathbb{1}[e^{auth}=1]\cdot \mathbb{1}[e^{bind}=1]\cdot \mathbb{1}[e^{consume}=1]\cdot c^{trace},
\]
where `c^{trace}` measures whether the same packet instance remains identifiable across planner emission, verifier authorization, executor binding, and actual guarded-prefix consumption. A row may only be described as an **executor-consumable packet contract** if `\kappa_{consume}` remains high under refresh episodes and delayed use. Otherwise, the strongest honest explanation must fall back to planner-side support, runtime continuity, or latent execution sufficiency.

This section deliberately narrows D06 away from abstract route accumulation and back toward AirSpark engineering reality. In practice, the question becomes: after all planner-side advantages are matched and subtracted, does the remaining packet still survive as a stable `WaypointExecutor` object at the `approach → align → contact-ready` boundary? If not, the method may still be useful, but it has not yet delivered the planner→executor interface that this paper claims.

### 3.10 Search-Route-Subtracted Packet Proposal Ranking

Our proposal layer explicitly ranks candidate packets under a **search-route-subtracted** objective so that earlier narrowing gains are not silently counted twice. We first parse the instruction into clause units, directional relations, and target-environment hints, then derive route-specific priors from three upstream families: language-mediated region narrowing (`Q^{lang}_{narrow}`), geometry-grounded target anchoring (`Q^{geo}_{narrow}`), and modular reasoning-route support (`Q^{reason}_{route}`). These priors are useful because they reduce wasted search coverage, pre-refresh drift, and pre-verifier mismatch, but they remain planner-side support signals rather than consume-time evidence.

For each candidate packet `\mathcal{W}_i`, we define a ranking score
\[
S(\mathcal{W}_i)=S_{sem}+\lambda_{dir}S_{dir}+\lambda_{geo}Q^{geo}_{narrow}+\lambda_{lang}Q^{lang}_{narrow}+\lambda_{reason}Q^{reason}_{route}-\lambda_{risk}C_{dyn},
\]

where `S_{sem}` is semantic relevance, `S_{dir}` is direction-conditioned adherence, and `C_{dyn}` is the dynamics-aware flight risk and budget cost. Crucially, the first three auxiliary route terms are only used to rank *which packet to test next*; they are not allowed to raise the final evidence ceiling unless the selected packet later survives refresh and controller consumption.

We further add a **local-shell feasibility term** `Q^{shell}_{local}` to capture whether a KIO-style controller shell can safely realize the proposed packet under near-field obstacle geometry and onboard latency constraints. This term may improve packet ranking by filtering proposals that are semantically strong but geometrically brittle in cluttered spaces. However, `Q^{shell}_{local}` is still an execution-support signal rather than a consume-time packet-survival proof: it may justify stronger Geometric Executability Gate performance, but not by itself stronger `A^{dir}`, `A^{ctx}`, or `A^{guard}` preservation.

We further add a **planner-side uncertainty and active-perception utility term** `Q^{unc}_{plan}` motivated by local evidence from LMPath, StereoNav, WildOS, and AgentVLN. LMPath shows that language-mediated priors can shrink the search region early, StereoNav shows that geometry-grounded target-location priors stabilize attachment once partial observation begins, WildOS shows that open-vocabulary outdoor search wins often come from choosing better observation frontiers rather than stronger late packet contracts, and AgentVLN shows that modular planner intelligence can improve which sensing/planning skill should be invoked before execution. In our framework, these signals naturally meet at a single planner-side question: **when should the agent spend one more sensing action to reduce ambiguity before committing the next packet?**

Concretely, we let `Q^{unc}_{plan}` summarize the expected value of one-step active perception under matched budget:
\[
Q^{unc}_{plan}=\Delta H_{target}+\eta_{dir}\Delta A^{dir}_{pre}+\eta_{ctx}\Delta A^{ctx}_{pre}-\eta_{time}C_{sense}-\eta_{risk}C_{hover},
\]
where `\Delta H_{target}` is the predicted reduction in target-zone uncertainty, `\Delta A^{dir}_{pre}` is the expected improvement in directional-clause stability before packet emission, `\Delta A^{ctx}_{pre}` is the expected improvement in anchor consistency before refresh, `C_{sense}` is the additional sensing-time cost, and `C_{hover}` is the exposure or hover risk incurred by delaying commitment. This term can raise proposal quality by explicitly preferring packets whose ambiguity can be cheaply reduced via one more look, a small altitude shift, or a short verification orbit.

Importantly, `Q^{unc}_{plan}` remains a **planner-side active-perception support signal**, not a packet-survival certificate. A row whose gain is exhausted by better uncertainty reduction or safer information gathering must freeze at **active-perception support** rather than packet-contract superiority. Only after subtracting this route together with reasoning, search-prior, latent-control, and local-shell explanations may a residual gain continue to the runtime and consume-time packet gates.

We therefore apply a subtraction rule before narrative promotion: if a row's gain disappears after conditioning on these upstream ranking routes, the row is frozen as **search-route improvement only**, **execution-shell support only**, or **active-perception support only**, depending on the dominant residual explanation. If residual gain remains, the packet must still pass `G_{reason}`, survive refresh episodes, and preserve `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}` before it may be promoted to packet-contract language; `A^{shell}` remains the last gate for manipulation-ready handoff claims. This design gives D06 a cleaner contract: better search, better local shells, and better ambiguity-aware sensing are welcome, but only consume-time packet survival earns deployment-grade language.

### 4.1 Setup

We evaluate the method on **AirSpark**, using matched sensing, latency, and controller budgets inside its UE5.7 city-scale aerial simulation stack. AirSpark serves here as the experimental platform rather than as the claimed benchmark contribution: episode generation, execution, replay, and logging are inherited from the platform, while this paper focuses on what the D06 planner emits and whether those emitted packet fields remain valid through execution.

Each evaluation episode begins from a language instruction, an initial UAV pose, and an AirSpark scene configuration drawn from city-scale routes that contain strong occlusion, corner turns, altitude changes, and target ambiguity. To keep this paper centered on the navigation-layer interface rather than full-benchmark expansion, we group instructions into three packet-stress regimes: **direction-dominant routing** (e.g., left/right/behind/after-turn commitments), **landmark-anchored search** (e.g., storefront, sign, entrance, rooftop equipment), and **late-stage POI disambiguation** where the system has already reached the correct semantic region but must still commit to the final object-facing anchor. For each regime, we report both ordinary navigation outcomes and whether the same semantic packet remains valid across refresh, verifier approval, and delayed executor consumption.

Concretely, the evaluation loop is driven by the AirSpark batch/waypoint execution pipeline. Language instructions are converted into Semantic Waypoint Packets, packets are verified and executed by the AirSpark waypoint backend, and all refresh episodes are logged against packet fields for later route-closure analysis. We keep sensing and control budgets matched across baselines: the RGB-D observation rate, maximum allowed refresh frequency, controller update budget, and safety-envelope thresholds are fixed per regime so that gains from better packet accounting are not conflated with looser runtime resources.

We organize baselines into five evidence families that mirror the claim ladder of this paper. **AerialVLA** and **GRaD-Nav++** anchor compact onboard VLA execution; **OnFly** anchors verifier-backed runtime continuity; **WorldFly** and **WorldVLN** anchor world-model / world-action proposal quality; **PEACE** anchors typed planner-executor decoupling with external constraints; and **POINav** anchors POI-grounded final-meters commitment. The goal of this setup is not to crown one universal aerial VLN winner, but to ask a narrower question: after these nearby explanations are matched, does the residual D06 gain still survive as an AirSpark-native `WaypointExecutor` packet?

Metrics are therefore recorded in four layers. The first layer is **task outcome**: success rate, path efficiency, collision rate, and instruction-completion score. The second layer is **planner/verifier health**: verifier pass rate, refresh count before consume, stale-packet rejection rate, and progress-stage consistency. The third layer is **executor-interface integrity**: `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, `c^{trace}`, consume-time field loss, and packet-ID replayability across `manifest.json`, `frames.jsonl`, and `actions.jsonl`. The fourth layer is **downstream handoff readiness**: approach-stage arrival offset, final-meters POI accuracy, guarded-prefix survival, and `interaction-ready` promotion rate. Full benchmark design, task taxonomy, and broader Search→Track→Approach→Contact→Recovery coverage belong to the companion AirManip-Bench paper and are not claimed as this paper's standalone contribution.

To keep the evaluation submission-ready, every reported row is built from the same AirSpark episode artifact bundle: scene config, instruction text, packet emission log, verifier decisions, executor traces, and post-hoc replay output. This requirement is important for D06 because it makes the main table falsifiable at the artifact level. A row that improves success but cannot be replayed as the same packet instance is allowed to remain useful engineering evidence, but it is not allowed to support full packet-contract language.

### 4.2 Main Results

Our main-result philosophy is conservative: a late-window gain is only allowed to support the strongest claim whose evidence route is fully closed at controller-consumption time.

### 4.2.5 Executor-View Packet Reduction before Packet Promotion

AerialVLA and OnFly together expose a concrete alternative explanation that D06 must explicitly subtract before claiming packet-interface superiority. AerialVLA can improve aerial navigation through **minimalist end-to-end latent control**, where a compact observation-language-to-action mapping already yields stronger unseen-scene execution without exposing a reusable planner-side packet. OnFly can improve deployment through **runtime continuity and verifier-backed monitoring**, where a semantically plausible target hypothesis is kept alive more honestly under onboard latency and dual-agent supervision even if the final executor-facing object remains only weakly formalized. If D06 does not separate these routes, a row may be over-credited as a packet-interface win when it is actually explained by better latent execution or better runtime continuity.

We therefore require every candidate packet-centric row to log an **executor-view reduction** before packet promotion. At minimum, the row should expose `Executor-View Match`, `Q^{exec}_{view}`, `Control-Facing Field Sufficiency`, `Bind-Time Schema Completeness`, `Consume-Time Trace Preservation`, and `Executor-View-Only Explanation Remaining`. If the dominant gain is exhausted after matched executor-view subtraction, the row must freeze as **latent execution sufficiency** or **runtime continuity support** rather than packet-contract superiority.

### 4.2.6 Current Evidence-Consistent Freeze after AerialVLA / OnFly Matching

Under the current evidence base, rows whose gains are primarily explained by AerialVLA-style compact end-to-end control should be reported as **latent execution sufficiency**, while rows whose gains are primarily explained by OnFly-style verifier-backed online continuity should be reported as **runtime continuity support**. Only rows that still preserve `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, and `c^{trace}` after these explanations are matched and subtracted may be promoted to packet-contract language; only rows that additionally preserve `A^{shell}` may support NtM handoff language.

### 4.2.7 Search-Prior Route-Closure Readout before Packet Promotion

To keep D06 honest when evaluating better search guidance, we introduce a dedicated readout for **search-prior route closure**. The key question is not whether a method finds promising regions earlier, but whether that earlier narrowing actually survives the packet lifecycle up to consume time. We therefore separate two upstream diagnostics from the later packet-contract fields: `Q^{lang}_{narrow}` measures how much language-mediated priors improve region narrowing before target confirmation, and `Q^{geo}_{narrow}` measures how much geometry-grounded anchoring reduces pre-refresh drift after partial target observation.

These two diagnostics may explain earlier `w+`, lower search inefficiency, or better target-zone concentration, but they are not allowed to directly promote a row into packet-contract or NtM language. A row is only allowed to leave the **search-prior support** bucket if the resulting refreshed packet still preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{dagger}_{consume}`; only rows that additionally preserve `A^{shell}` may support manipulation-ready handoff claims. In practice, this creates a subtraction discipline for LMPath- and StereoNav-style gains: first test whether the gain is exhausted by better narrowing, then check whether any residual benefit survives the refresh episodes and consume-time boundary.

### 4.2.8 Reasoning-Family Subtraction before Packet-Contract Promotion

We introduce a dedicated subtraction step for **reasoning-and-skill-routing gains** motivated by AgentVLN-style modular navigation. The key question is whether a row improves because the system reasons better *before* execution—e.g., better clause decomposition, lower pre-verifier semantic mismatch, or cleaner skill dispatch—or because the emitted packet truly survives refresh and controller consumption more honestly. To separate these cases, we log a reasoning-family diagnostic `Q^{reason}_{route}` alongside the existing narrowing and packet-survival fields.

A row may use `Q^{reason}_{route}` to justify stronger planner-side semantic routing, earlier `w+`, or lower pre-verifier mismatch, but it is not allowed to claim packet-contract superiority unless the same row still preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{dagger}_{consume}` after conditioning on reasoning-family gains. Only rows that additionally preserve `A^{shell}` may support manipulation-ready handoff language. In practice, this creates a matched subtraction discipline for AgentVLN-like baselines: first explain what was gained by modular reasoning and skill routing alone, then check whether any residual improvement survives the packet lifecycle to actual consume time. This prevents D06 from mistaking a better search-time VLM-as-Brain route for a solved packet-first deployment interface.

### 4.2.9 Active-Perception and World-Prior Route Closure before Packet Promotion

Recent local anchors show that aerial VLN can also improve before packet consumption through one more look, a stronger world prior, or a better completion of unknown regions. `Q^{unc}_{plan}` captures planner-side active perception: it estimates whether the agent should spend one more sensing action, a short verification orbit, or a slight altitude shift before committing a packet. `Q^{world}_{prior}` captures how much a physics-grounded semantic sandbox or world prior improves early search entropy and open-world transfer. `Q^{comp}_{sem}` captures semantic-completion gains that sharpen goal maps and reduce wasted exploration when the target region is still partially observed.

These signals are valuable, but they still live on the route `upstream support win → refresh-stable packet support → consume-time packet preservation`. If the gain vanishes after conditioning on active-perception, world-prior, and semantic-completion explanations, the row must freeze as **active-perception support**, **world-prior transfer support**, or **semantic-completion support**. Only residual evidence that still preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{dagger}_{consume}` may continue toward packet-contract language, and only rows that additionally preserve `A^{shell}` may support NtM handoff claims.

### 4.2.10 World-Action Planning Subtraction before Packet Promotion

WorldVLN-style short-horizon action-world prediction can make aerial navigation look much better without yet proving stronger packet accountability. We therefore log `Q^{wam}_{plan}` as a dedicated diagnostic for **world-action planning support**: it captures whether the gain comes from better rollout consistency, better consequence prediction, or better waypoint proposal quality before delayed consumption is audited. This diagnostic is allowed to raise `w+` or reduce local rollout inconsistency, but it is not allowed to skip the packet lifecycle.

After subtracting reasoning, search-prior, active-perception, world-prior, and world-action explanations, a row may still support packet-contract language only if `A^{dir}`, `A^{ctx}`, and `A^{guard}` survive until `w^{dagger}_{consume}`. `A^{shell}` remains the final gate for manipulation-ready handoff claims. This is especially important for D06 because a strong planner can now look good for many different reasons before the controller ever consumes its packet; the paper should make those reasons explicit rather than collapsing them into one inflated navigation headline.

### 4.2.11 Executor-Consumable Packet Contract on AirSpark

To satisfy the AirSpark-facing owner guidance, the main results must now answer a stricter question than "did the planner choose a good waypoint?" We require every strong D06 claim to be backed by an **executor-consumable packet contract** row that reports whether the same packet instance survived `serialization → verifier authorization → WaypointExecutor bind → consume time` as an auditable control-facing object. At minimum, the main table should expose `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, `c^{trace}`, `Executor API Field Coverage`, and `Consume-Time Packet Contract Status`.

This readout is intentionally narrower than generic success-rate reporting. A row may still improve success, reduce search cost, or raise verifier pass rate, but unless the remaining packet also survives as a stable AirSpark `WaypointExecutor` object, the strongest honest explanation must remain below packet-contract promotion. In other words, D06 only earns planner→executor interface language when the packet is not merely semantically plausible, but actually consumable and traceable on the AirSpark backend.

### 4.2.12 AirSpark Log-Surface Alignment Row

Because the packet contract is grounded in concrete AirSpark artifacts, we further require a log-surface alignment row that checks field-by-field preservation across `manifest.json`, `frames.jsonl`, and `actions.jsonl`. This row should report `Manifest Field Coverage`, `Frame Trace Continuity`, `Action Trace Completeness`, `Packet-ID Replayability`, and `Cross-Surface Mismatch Count`. These fields let us distinguish planner wins from bookkeeping failures: a method that navigates well but loses packet identity between planner emission and executor consumption should not be narrated as having solved the interface problem.

### 4.2.13 Current Evidence-Consistent Freeze after AirSpark Executor Matching

Under the current local evidence, the most honest freeze remains conservative. AerialVLA still anchors the strongest direct evidence for compact end-to-end control, OnFly still anchors verifier-backed runtime continuity, and local retrieval still returns only limited executor-facing literature beyond D06's own notes. Therefore, any row whose gain disappears once AirSpark executor matching and cross-surface traceability are enforced should freeze as **latent execution sufficiency**, **runtime continuity support**, or **planner-side support only**. Only residual rows that still preserve `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, `c^{trace}`, `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}` may be promoted to packet-contract language; only rows additionally preserving `A^{shell}` may support NtM handoff claims.

### 4.2.14 Submission-Ready AirSpark Main Table Layout

To keep the result section executable rather than aspirational, we require the main empirical readout to be decomposed into a fixed **five-block table layout**. Block I is **schema alignment** and reports `Executor-Schema Match`, `goal_type-valid`, `pose-bind-valid`, `yaw-altitude-bind-valid`, `budget-envelope-valid`, and `Manifest/Frame/Action Field Coverage`. Block II is **consume-time trace** and reports `e^{auth}`, `e^{bind}`, `e^{consume}`, `Consume-Time Trace Match`, `emit→bind latency`, `bind→consume latency`, `trace gap count`, and `stale-field-at-consume`. Block III is **planner-executor subtraction** and reports `Planner–Executor Family Match`, `Q^{pe}_{bind}`, `Executor-Ambiguity Reduction`, and `Decoupling-Only Explanation Remaining`. Block IV is **POI/final-meters subtraction** and reports `POI-Family Match`, `Q^{poi}_{final}`, `Final-Meters Offset Reduction`, and `Packet-Promotion Eligibility after POI Matching`. Block V is **reconstruction audit** and reports `Packet-Reconstructability Match`, `Manifest↔Frames↔Actions Joint Recoverability`, `Reconstructed-Packet Identity Consistency`, `Hidden-Planner-State Dependence`, and `Weakest Honest Claim after Reconstruction Audit`.

This five-block layout is intentionally stricter than a single summary table because D06's claim can fail at several different layers while still looking strong on endpoint success. A row that wins only in Block I is a **schema-aligned serialization support** row. A row that reaches Block II but collapses in Block III or IV is an **executor-interface** or **final-meters** support row. A row that survives through Block V is the only one that may honestly be narrated as `full packet-contract accountability`. This keeps the result section aligned with the method claim that AirSpark packet quality should be judged at the whole `serialization → bind → consume → replay` chain.

### 4.2.15 Minimal Comparison Protocol for D06 vs. Nearby Baselines

For each baseline family, we report one matched row under identical AirSpark budget settings and one residual row after family subtraction. In practice, AerialVLA and GRaD-Nav++ populate the compact-control comparison, OnFly populates the runtime-continuity comparison, WorldFly and WorldVLN populate the world-model/world-action comparison, PEACE populates the planner-executor decoupling comparison, and POINav populates the final-meters comparison. The D06 row is only allowed to inherit the strongest claim that still survives after these matched rows are read in order.

This protocol matters because it prevents the paper from mixing different kinds of strength into one headline. If D06 wins mainly on unseen-scene control smoothness relative to AerialVLA or GRaD-Nav++, the row should freeze at **latent execution sufficiency**. If it wins mainly on long-horizon continuity relative to OnFly, it should freeze at **runtime continuity support**. If it wins mainly because PEACE-style typed plans reduce bind ambiguity or because POINav-style endpoint reasoning sharpens the last segment, it should freeze accordingly. Only when the residual D06 row still preserves AirSpark packet identity after all matched comparisons does the paper earn stronger planner→verifier→WaypointExecutor language.

### 4.2.16 World-Model Foresight and Onboard Reactive-Shell Families must be Subtracted before Packet Promotion

WorldFly [REF: 2606.06147] and GRaD-Nav++ [REF: 2506.14009] together expose another nearby explanation that D06 must subtract explicitly before promoting any executor-facing packet claim. WorldFly can make aerial navigation look substantially stronger because imagined future video and flow-matching world-model foresight reduce short-horizon consequence error and stabilize unseen-scene proposal quality before the packet is ever consumed. GRaD-Nav++ can make a system look much more deployable because lightweight onboard VLA execution, differentiable-dynamics training, and 3DGS-conditioned compact control improve real-time local behavior even when the packet object itself remains weakly formalized. In other words, one route wins by making the planner foresee better; the other route wins by making the onboard shell react better. Neither route, by itself, proves that the same semantic packet remained AirSpark-native, trace-preserving, and WaypointExecutor-consumable.

We therefore require one additional matched readout before packet promotion. At minimum, every strong D06 row should report `World-Model Family Match`, `Q^{wam}_{plan}`, `Onboard Reactive-Shell Match`, `Q^{shell}_{local}`, `Compact-Control Explanation Remaining`, and `Residual Packet-ID Survival after World/Shell Matching`. If a row's gain is exhausted once WorldFly-style foresight or GRaD-Nav++-style onboard shell quality is matched, the row must freeze as **world-model foresight support**, **compact-control support**, or **local-shell support** rather than being narrated as a stronger packet interface. This rule keeps D06 aligned with the owner-note requirement that the paper be judged at the controller-consumable packet boundary, not merely at the level of better prediction or better onboard actuation.

### 4.2.17 Current Evidence-Consistent Freeze after WorldFly / GRaD-Nav++ Matching

Under the current local evidence, any D06 row whose improvement is mainly explained by WorldFly-style future-state anticipation should freeze as **world-model foresight support** or, at most, **world-action planning support** if the gain is still dominated by better short-horizon consequence modeling. Likewise, any row whose improvement is mainly explained by GRaD-Nav++-style lightweight onboard control should freeze as **compact-control support**, **local-shell support**, or **latent execution sufficiency** depending on whether the dominant residual comes from tighter onboard responsiveness, better geometric executability, or a cleaner end-to-end control manifold.

Only rows that still preserve `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, `c^{trace}`, and `A^{dir}/A^{ctx}/A^{guard}` after both WorldFly-style foresight and GRaD-Nav++-style onboard-shell explanations have been matched and subtracted may be promoted to **deployable packet interface** language. Rows that additionally preserve `A^{shell}` across the `approach → align → contact-ready` boundary may support stronger NtM handoff wording. This freeze is intentionally strict: better world-model rollout quality or better onboard control smoothness may still be valuable engineering wins, but they are not yet evidence that the packet itself survived as an auditable AirSpark executor object.

### 4.2.26 POI-Grounded Final-Meters Family must be Subtracted before Packet Promotion

POINav [REF: 2605.28237] shows that a method may win mainly by becoming better at **final-meters arrival** rather than by becoming a stronger packet interface. We therefore require every row that improves late-stage precision to explicitly log a **POI-grounded family match** before packet promotion. At minimum, the main table should include `POI-Family Match`, `Q^{poi}_{final}`, `Final-Meters Offset Reduction`, `POI-Level Arrival Gain`, and `Packet-Promotion Eligibility after POI Matching`. If these fields are absent, the row may not be promoted beyond **POI-grounded final-meters support**.

This rule is especially important for D06 because a packet-centric system will often look stronger exactly in the same late stage where POI-grounded reasoning helps most. Our reporting must therefore separate `better coarse long-horizon search`, `better POI-level endpoint commitment`, and `better consume-time packet preservation` rather than letting them collapse into one late-window success story.

### 4.2.27 Current Evidence-Consistent Freeze after POINav Matching

Under the current evidence base, any D06 row whose late-stage gain can already be honestly explained by stronger POI-level endpoint reasoning should freeze as **POI-grounded final-meters support** unless it still preserves `A^{dir}`, `A^{ctx}`, and `A^{guard}` through `w^{\dagger}_{consume}` after the POINav-style route has been matched and subtracted. Only rows that survive this subtraction may be promoted to packet-contract language; only rows that additionally preserve `A^{shell}` may support NtM handoff language.

### 4.2.29 Packet–Executor Schema Alignment must be Logged before Contract Promotion

Any D06 row that aims to claim a stronger packet interface must first expose the full **schema-alignment block** rather than only endpoint success or planner-side plausibility. At minimum, the row should report `Executor-Schema Match`, `goal_type-valid`, `pose-bind-valid`, `yaw-altitude-bind-valid`, `budget-envelope-valid`, `Manifest Field Coverage`, `Frame Trace Continuity`, and `Action Trace Completeness`. If these fields are absent, the row may not be promoted beyond **schema-aligned serialization support** because the packet is still under-specified as an AirSpark-native executor object.

This rule matters because AirSpark packet claims are stricter than ordinary typed-waypoint claims. A packet can be semantically plausible and even verifier-compatible while still failing to preserve the exact controller-facing fields that `WaypointExecutor` must later consume and that the replay stack must later audit. By forcing schema-alignment fields into the main table, D06 prevents planner-side cleanliness from being over-read as packet-contract evidence.

### 4.2.30 Five-Block Main Table should be read in a fixed reviewer-facing order

We freeze the reviewer-facing reading order of the D06 main table as **schema alignment → consume-time trace → planner–executor subtraction → POI final-meters subtraction → reconstruction audit**. The table is therefore not a flat scoreboard but a promotion ladder. A row that survives only the first block is reported as **schema-aligned serialization support**. A row that survives the first two blocks but collapses once planner–executor matching is enforced is reported as **executor-interface support** or **constraint-enforced bind-time support**. A row that additionally survives planner–executor subtraction but collapses after POI matching is reported as **POI-grounded final-meters support**. Only a row that also survives reconstruction audit may be narrated as a **full Semantic Waypoint Packet contract**.

This fixed reading order matters because late-window gains in aerial VLN are otherwise too easy to over-upgrade. Better typed plans, cleaner bind-time safety, stronger endpoint snapping, and stronger replay discipline all improve deployment realism, but they do not provide the same evidential weight. D06 therefore treats the five blocks as a nested contract rather than as interchangeable metrics.

### 4.2.31 Current Evidence-Consistent Next Step after Re-reading PEACE, POINav, and AirSpark Executor Constraints

Under the current local evidence, the most valuable next step is no longer expanding related-work coverage, but making the first D06 result bundle exportable under the five-block table itself. In particular, future runs should directly emit `schema alignment / consume-time trace / planner–executor subtraction / POI final-meters subtraction / reconstruction audit` rows from the same AirSpark artifact bundle, together with a single `Weakest Honest Claim` column. This keeps the draft aligned with the owner-note requirement that D06 be judged at the `Semantic Waypoint Packet → WaypointExecutor` boundary rather than at generic aerial VLN success.

Any D06 row that aims to support **deployable packet interface**, **full packet-contract accountability**, or **NtM handoff preservation** must explicitly report whether the emitted packet is aligned with the AirSpark execution schema. At minimum, the main table should expose `Executor-Schema Match`, `goal_type-valid`, `pose-bind-valid`, `yaw-altitude-bind-valid`, `progress-thread-consistency`, `budget-envelope-valid`, and `Consume-Time Field Loss Cause`, together with the concrete mapping route into `manifest.json`, `frames.jsonl`, and `actions.jsonl`. If these fields are absent, the result may still be credited as a planner-time semantic improvement or a verifier-compatible abstraction, but it must not be narrated as a true planner-controller contract.

### 4.2.30 Consume-Time Trace Consistency is Required beyond Bind-Time Success

Schema alignment alone is insufficient. A packet that binds cleanly can still fail when consumed later by the WaypointExecutor due to stale fields, semantic-thread drift, or trace gaps between planner emission and delayed execution. We therefore require every executor-facing result to additionally report `Consume-Time Trace Match`, `emit→bind latency`, `bind→consume latency`, `trace gap count`, `stale-field-at-consume`, `consume-time semantic-thread drift`, and `First Consume-Time Failure Cause`. If the row is trace-complete and preserves the same semantic thread through delayed consumption, it may continue upward in the promotion ladder; otherwise it must freeze at **bind-stable packet output** or **consume-fragile support**.

### 4.2.31 Current Evidence-Consistent Freeze after Packet–Executor and Trace Alignment

Under the current evidence state, the strongest D06 claims should remain intentionally narrow. Systems with stronger planning quality but no explicit AirSpark field mapping freeze at **planner-side support**. Systems with clear packet serialization but no delayed consumption audit freeze at **verifier-compatible or bind-stable packet output**. Only rows that preserve schema-aligned fields and trace-complete semantic-thread consistency through `e^{consume}` may support **deployable packet interface** language, and only rows that also preserve `A^{shell}` across the Approach→Align→Contact boundary may support stronger NtM handoff wording. This rule keeps the paper aligned with the owner's instruction that D06 should stop accumulating paper-only route rules and instead move toward a controller-consumable, auditable interface.

### 4.2.32 Reconstruction-Aware Executor Contract before Full Packet Promotion

To prevent bind-stable or consume-stable packets from being overstated as fully deployable interfaces, we require one final executor-facing audit layer: **post-hoc packet reconstruction**. A result row is not allowed to claim `full packet-contract accountability` unless the same semantic packet instance can be recovered from AirSpark's `manifest.json`, `frames.jsonl`, and `actions.jsonl` without hidden planner-side memory. At minimum, the main table should expose `Packet-Reconstructability Match`, `Manifest↔Frames↔Actions Joint Recoverability`, `Reconstructed-Packet Identity Consistency`, `Hidden-Planner-State Dependence`, `Reconstruction-Only Failure Cause`, and `Weakest Honest Claim after Reconstruction Audit`.

These fields are intentionally orthogonal to `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, and `c^{trace}`. A packet may serialize correctly, bind cleanly, and even be consumed, while still failing if cross-log replay cannot recover the same object/anchor/clause/thread tuple. If a row passes serialization but fails reconstruction, it freezes as **schema-aligned serialization support**. If it survives consume but not replay identity, it freezes as **consume-stable yet audit-fragile packet support** or **bind-stable but reconstruction-fragile support**. Only rows whose reconstructed packet still preserves the same semantic thread across all three logs may be promoted to **full packet-contract accountability**; `A^{shell}` remains the final gate for any manipulation-ready handoff claim.

### 4.2.33 Current Evidence-Consistent Freeze after Reconstruction Audit

Under the current evidence base, the most honest D06 freeze remains conservative: executor-facing rows that do not yet close post-hoc reconstruction should stop below full packet-contract language even if they already look strong on `e^{bind}`, `e^{consume}`, or `c^{trace}`. This keeps the paper aligned with the owner-note requirement that the contribution be judged at the **AirSpark WaypointExecutor-consumable and auditable packet interface**, rather than at the level of planner-side neatness or bind-time success alone.

## 5. Conclusion

We present a packet-centric aerial VLN framework that reframes aerial navigation as a deployable interface problem rather than only a waypoint prediction problem. By combining 3D semantic frontier mapping, dynamics-aware exploration, and a Navigate-then-Manipulate handoff architecture, the framework exposes where language grounding, runtime continuity, delayed controller consumption, and local interaction readiness truly succeed or fail. More importantly, the paper argues for a stricter evidence discipline: planner-time gains, runtime continuity gains, latent execution sufficiency, and full packet-contract preservation should not be conflated. This framing gives D06 a clearer path toward honest evaluation and, eventually, a stronger aerial embodied intelligence system.
