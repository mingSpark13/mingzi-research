# D06 experiments


## R925 本轮推进结论：把 D06 从“reliability-guided safety 与 latent-safety 已单列扣减”继续压到“Explore From Sketch 这类 structural-prior exploration 也必须单列扣减，否则 packet gain 仍可能只是更会利用草图/平面图先验来缩小搜索空间，而不是同一 Semantic Waypoint Packet 真正活过 AirSpark 的 schema→bind→consume→replay→reconstruct 链路”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换切回 **D06**，满足近三轮继续覆盖 `D01/D04/D06` 至少一个，也避免与上一轮非 D06 方向连续重复。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md / experiments.md`，回扫近 30 天本地 L1 命中 **Guided Diffusion with Distilled Vision-Language Reliability for Aerial Navigation (2606.13883)**、**Sensitivity Shaping for Latent Modeling (2606.14585)**、**Explore From Sketch (2606.11708)**，并补跑 QMD `aerial vision language navigation reliability diffusion latent sensitivity shaping --no-rerank`，结果仍主要回流既有 D06 报告与 AerialVLA/source 条目，没有形成需要完整入库的新 executor-facing 论文。因此本轮 **高价值扫描命中 3 个本地核心锚点（2606.13883、2606.14585、2606.11708）**、**正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **PAPER.md 推进**：本轮继续推进 **Related Work / Limitations**。在既有 `reliability-guided safety support` 与 `latent-safety support` 之后，新增 **2.32–2.33**，把 **Explore From Sketch** 明确冻结为 `structural-prior exploration support`：它可以解释更强的草图/平面图条件探索效率、更低的搜索熵、更稳的全局 corridor commitment 与 relocalization 组织，但若没有同时穿过 `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, `c^{trace}` 与 `R_{pkt}`，仍不得升级成 AirSpark 的 full packet-contract 语言。
- **experiments 收束**：从现在起，D06 主表若匹配这类 prior-map / sketch-guided baseline，至少要新增 `Structural-Prior Family Match / Q^{prior}_{struct} / Search-Entropy Reduction / Corridor-Commitment Gain / Relocalization-Waste Reduction / Prior-Only Explanation Remaining / Residual Packet-ID Survival after Structural-Prior Matching`。若增益在这些字段扣减后消失，结果必须冻结在 **structural-prior exploration support**，不得直接写成 packet survives `serialization → bind → consume → replay → reconstruct`。
- **核心价值**：这一步把 D06 从“知道要扣可靠性安全 shaping 与 latent safety shaping”继续推进到“连最像工程上立竿见影的草图/平面图结构先验增益也必须单列扣减”。也就是说，D06 现在更清楚地区分 **planner 更会借先验地图找路**，和 **同一 Semantic Waypoint Packet 真正跨过 AirSpark schema / bind / consume / replay / reconstruct 全链路**，这两类证据不能再混写成一种 executor-facing 优势。
- **下轮建议**：按轮换优先切 **D01_世界模型** 或 **D07_Isaac强化学习机械臂控制**；若继续回到 D06，下一步最值钱的是把 `Q^{prior}_{struct}` 真正映射成 AirSpark 日志导出键和 submission-ready 主表列，而不是继续扩 related work。


## R923 本轮推进结论：把 D06 从“PAPER 已明确扣除 typed bind / pixel interface / async progress / planner-side world structuring”继续压到“experiments 主表也必须把这些近邻解释映射成 submission-ready 扣减列，否则所谓 packet gain 仍可能只是更会表达目标、更会保持进度、或更会组织世界，而不是同一 Semantic Waypoint Packet 真正活过 AirSpark 的 schema→bind→consume→replay→reconstruct 链路”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换切回 **D06**，满足近三轮继续覆盖 `D01/D04/D06` 至少一个，也避免与上一轮非 D06 方向连续重复。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md / experiments.md`，回扫近 30 天本地 L1 命中 **Goal2Pixel (2606.01621)**、**FLIGHT VLA (2606.06836)**、**AgentVLN (2603.17670)**、**MacroNav (2511.04320)** 与既有 **PEACE (2606.00104)**；QMD 对 `aerial visual language navigation packet contract chain serialization grounding navigation --no-rerank` 仍主要回流既有 D06 报告与 survey/source，没有形成需要完整入库的新 executor-facing 论文。因此本轮 **高价值扫描命中 5 个本地核心锚点（Goal2Pixel、FLIGHT VLA、PEACE、AgentVLN、MacroNav）**、**正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **PAPER.md 推进**：本轮继续推进 **Related Work / Limitations**。在既有 interface / progress / world-structuring 扣减链之后，新增 **2.30–2.31**，把 **Guided Diffusion with Distilled Vision-Language Reliability for Aerial Navigation (2606.13883)** 冻结为 `reliability-guided safety support`，把 **Sensitivity Shaping for Latent Modeling (2606.14585)** 冻结为 `latent-safety support`，明确这两条路线只能先解释更安全的 proposal shaping、OOD 更诚实的 latent filtering、和更低的 planner-side collision risk；若没有同时穿过 `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, `c^{trace}` 与 `A^{dir}/A^{ctx}/A^{guard}`，仍不得升级成 AirSpark 的 full packet-contract 语言。
- **experiments 收束**：从现在起，D06 主表若匹配这两类 baseline，至少要新增 `Reliability-Safety Family Match / Q^{rel}_{safe} / Unreliable-Region Avoidance Gain / Reflective/Overexposed Failure Reduction / Safety-Only Explanation Remaining / Latent-Sensitivity Family Match / Q^{sens}_{latent} / OOD-Alert Fidelity / False-Safe Rollout Suppression / Residual Packet-ID Survival after Safety Matching`。若增益在这些字段扣减后消失，结果必须冻结在 **reliability-guided safety support** 或 **latent-safety support**，不得直接写成 packet survives `serialization → bind → consume → replay → reconstruct`。
- **核心价值**：这一步把 D06 从“知道要扣 typed bind、像素接口、async progress、scene-graph/context world structuring、world-model imagination”继续推进到“连最像‘真实部署更安全’的 reliability-guided diffusion 与 sensitivity-shaped latent safety 也必须单列扣减”。也就是说，D06 现在更清楚地区分 **更会避开感知不可信区 / 更会做 support-aware latent filtering**，和 **同一 Semantic Waypoint Packet 真正跨过 AirSpark schema / bind / consume / replay / reconstruct 全链路**，这两类证据不能再混写成一种 executor-facing 优势。
- **下轮建议**：按轮换优先切 **D01_世界模型** 或 **D07_Isaac强化学习机械臂控制**；若继续回到 D06，下一步最值钱的是把 `Q^{rel}_{safe}` 与 `Q^{sens}_{latent}` 真正映射成 AirSpark 日志导出键和 submission-ready 主表列，而不是继续扩 related work。


## R924 本轮推进结论：把 D06 从“interface / progress / world-structuring 家族已写进 PAPER 与 experiments”继续压到“reliability-guided diffusion 与 sensitivity-shaped latent safety 也必须单列扣减，否则 packet gain 仍可能只是更会避开感知不可信区域、或更会做 support-aware latent filtering，而不是同一 Semantic Waypoint Packet 真正活过 AirSpark 的 schema→bind→consume→replay→reconstruct 链路”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换切回 **D06**，满足近三轮继续覆盖 `D01/D04/D06` 至少一个，也避免与上一轮非 D06 方向连续重复。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md / experiments.md`，回扫近 30 天本地 L1 命中 **Guided Diffusion with Distilled Vision-Language Reliability for Aerial Navigation (2606.13883)**、**Sensitivity Shaping for Latent Modeling (2606.14585)**，并补跑 QMD `aerial vision-language navigation reliability diffusion latent modeling UAV navigation --no-rerank`，结果主要回流既有 D06 报告与 survey/source，未形成需要完整入库的新 executor-facing 论文。因此本轮 **高价值扫描命中 2 个本地核心锚点（2606.13883、2606.14585）**、**正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **PAPER.md 推进**：本轮继续推进 **Method / Evidence Boundary**。在 `3.4 WaypointExecutor-Aligned Packet Serialization...` 之前，新增一段桥接论证：D06 的 strongest claim 现在必须同时穿过 `C_{ser}`, `e^{auth}`, `e^{bind}`, `e^{consume}`, `c^{trace}` 与 `R_{pkt}`，并且在解释层面先扣除四类 June-local 邻近增益——**PEACE** 的 typed planner–executor decoupling、**Goal2Pixel** 的 pixel-grounded goal interface、**FLIGHT VLA** 的 long-horizon asynchronous progress discipline、以及 **AgentVLN/MacroNav** 的 planner-side world structuring。也就是说，即使 planner 更会拆技能、更会组织场景图、更会保持长时程进度，若同一 packet 不能在 AirSpark 的 `manifest.json / frames.jsonl / actions.jsonl` 中保持 schema-aligned、bind-stable、consume-faithful、replay-reconstructable semantic thread，就不得升级成 full packet-contract language。
- **experiments 收束**：从现在起，D06 主表若匹配这些近邻基线，至少要新增 `Typed-Bind Family Match / Q^{pe}_{bind} / Pixel-Interface Family Match / Q^{pix}_{iface} / Async-Progress Family Match / Q^{async}_{lh} / Scene-Graph-Route Match / Q^{sg}_{route} / Context-Encoder Match / Q^{ctx}_{enc} / World-Structuring-Only Explanation Remaining / Residual Packet-ID Survival after Interface/Progress/World-Structuring Matching`。若增益在这些字段扣减后消失，结果必须冻结在 **executor-interface support**、**pixel-grounding support**、**asynchronous progress-discipline support**、或 **planner-side world-structuring support**，不得直接写成 packet survives `serialization → bind → consume → replay`。
- **核心价值**：这一步把 D06 从“知道要扣 world-model imagination-to-trajectory family”继续推进到“连最像部署能力的 typed bind、像素目标接口、长时程进度纪律、scene-graph/context world structuring 都必须单列扣减”。也就是说，D06 现在更清楚地区分 **planner 更会组织世界**、**接口更干净**、**进度更稳**，和 **同一 Semantic Waypoint Packet 真正跨过 AirSpark schema / bind / consume / replay 全链路**，这些证据不能再混写成一种部署优势。
- **下轮建议**：按轮换优先切 **D01_世界模型** 或 **D07_Isaac强化学习机械臂控制**；若继续回到 D06，下一步最值钱的是把 `Q^{pe}_{bind}`、`Q^{pix}_{iface}`、`Q^{async}_{lh}`、`Q^{sg}_{route}`、`Q^{ctx}_{enc}` 真正映射成 AirSpark 日志导出键和 submission-ready 主表列，而不是继续扩 related work。


## R921 本轮推进结论：把 D06 从“tool-calling / lifelong-memory / pixel-interface / runtime-orchestration 扣减已成形”继续压到“ImagineUAV 这类 world-model imagination-to-trajectory 路线也必须单列扣减，否则 packet gain 仍可能只是更会做短视界未来想象与 kinodynamic proposal，而不是同一 Semantic Waypoint Packet 真正活过 AirSpark 的 serialization→bind→consume→replay 链路”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换继续主推进 **D06**，满足近三轮继续覆盖 `D01/D04/D06` 至少一个，也避免与上一轮把 D06 收束点放在 Goal2Pixel / AgenticNav / AerialClaw 后完全原地重复。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md / experiments.md`，回扫近 30 天本地 L1 中与当前 executor-facing 主张最相邻的锚点 **ImagineUAV (2606.01205)** 与既有 **UAV-VLN (2504.21432)**；同时补跑 QMD `aerial visual language navigation UAV embodied VLM --no-rerank`，结果仍主要回流既有 D06 报告与 survey/source 条目，没有形成需要完整入库的新 executor-facing 论文。因此本轮 **高价值扫描命中 1 篇本地核心锚点（ImagineUAV）**、**正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **PAPER.md 推进**：本轮继续推进 **Related Work**，在 `2.2 Aerial Navigation Systems` 明确补入 **ImagineUAV** 的 family 定位：它能解释更强的 **planner-side foresight**、**short-horizon consequence prediction** 与 **kinodynamically feasible proposal generation**，但若没有 `C_{ser}`、`e^{bind}`、`e^{consume}`、`c^{trace}` 闭环，就只能冻结为 **world-model imagination-to-trajectory support**，不得直接升级成 AirSpark 的 Semantic Waypoint Packet contract。
- **experiments 收束**：从现在起，D06 主表若匹配 ImagineUAV/WorldFly 这一类邻近基线，至少要新增 `World-Imagination Family Match / Q^{imag}_{plan} / Imagined-Future Consistency / Kinodynamic-Proposal Gain / Proposal-Only Explanation Remaining / Residual Packet-ID Survival after Imagination Matching`。若增益在这些字段扣减后消失，结果必须冻结在 **world-model imagination-to-trajectory support**，不得直接写成 packet survives `serialization → bind → consume → replay`。
- **核心价值**：这一步把 D06 从“知道要扣目标接口、信息获取、runtime 编排”继续推进到“连最像方法收益的 world-model imagination 近邻解释也要单列扣减”。也就是说，D06 现在更清楚地区分 **想得更远、轨迹提案更安全**，和 **同一 Semantic Waypoint Packet 真正跨过 AirSpark schema / bind / consume / replay 全链路**，这两类证据不能再混写成一种部署优势。
- **下轮建议**：按轮换优先切 **D01_世界模型** 或 **D07_Isaac强化学习机械臂控制**；若继续回到 D06，下一步最值钱的是把 `Q^{imag}_{plan}` 真正映射成 AirSpark 日志导出键和 submission-ready 主表列，而不是继续扩 related work。


## R920 本轮推进结论：把 D06 从“tool-calling / lifelong-memory 两类 support family 已写进 PAPER”继续压到“experiments 主表也必须显式扣除 Goal2Pixel 像素接口、AgenticNav 工具调用、AerialClaw brain-skill-runtime 三类 planner-side interface gain，否则 packet gain 仍可能只是更会表达目标、更会查信息、或更会编排 runtime，而不是同一 Semantic Waypoint Packet 真正活过 AirSpark 的 schema→bind→consume→replay 链路”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换继续主推进 **D06**，满足近三轮继续覆盖 `D01/D04/D06` 至少一个，也避免与上一轮把 D06 收束点放在 AgenticNav / AllDayNav 后原地空转。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md / experiments.md`，回扫近 30 天本地 L1 命中 **Goal2Pixel (2606.01621)**、**AgenticNav (2606.10577)**、**AerialClaw (2606.12142)** 三个近邻锚点；QMD 对 `airborne vision-language navigation scene graph waypoint-free --no-rerank` 主要回流既有 `D06/REPORT.md`、AerialVLA source 与 survey/source 条目，没有形成需要完整入库的新 executor-facing 论文。因此本轮 **高价值扫描命中 3 篇本地核心锚点（Goal2Pixel、AgenticNav、AerialClaw）**、**正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **PAPER.md 推进**：本轮继续推进 **Related Work / Limitations**。在 2.22–2.24 已有 tool-calling 与 lifelong-memory 扣减链上，进一步用 Goal2Pixel 作为 **pixel-grounding interface** 邻近线，用 AerialClaw 作为 **brain-skill-runtime orchestration** 邻近线来校准解释边界：前者可以解释更干净的目标表达与更经济的目标像素接口，后者可以解释更强的高层任务编排、skill 抽象与 runtime validation，但两者都还不足以单独升级成 AirSpark 的 executor-facing packet contract。与此同时，2.29 Limitations 保留并强化了“更会表达目标 / 更会查信息 / 更会调度 runtime”与“同一 packet 真正跨过 serialization、bind、consume、replay”的证据分界。
- **experiments 收束**：从现在起，D06 主表若匹配这些近邻基线，至少要新增 `Pixel-Interface Family Match / Q^{pix}_{iface} / Goal-Expression Efficiency / Trajectory-Binding Gap / Tool-Acquisition Family Match / Q^{tool}_{acq} / Depth-Query Efficiency / Memory-Recall Precision / Runtime-Orchestration Family Match / Q^{rt}_{orch} / Skill-Routing Stability / Validation-Only Explanation Remaining`。若增益在这些字段扣减后消失，结果必须冻结在 **pixel-grounding interface support**、**tool-calling information-acquisition support** 或 **runtime-orchestration support**，不得直接写成 packet survives `serialization → bind → consume → replay`。
- **核心价值**：这一步把 D06 从“知道要扣 tool-calling 和 lifelong-memory”继续推进到“连 planner-side 目标表达接口和 brain-skill-runtime 编排层也必须单列扣减”。也就是说，D06 现在更清楚地区分 **目标接口更好**、**信息获取更聪明**、**runtime 编排更稳**，和 **同一 Semantic Waypoint Packet 真正跨过 AirSpark schema / bind / consume / replay 全链路**，这几类证据不能再混写成一种部署优势。
- **下轮建议**：按轮换优先切 **D01_世界模型** 或 **D07_Isaac强化学习机械臂控制**；若继续回到 D06，下一步最值钱的是把 `Q^{pix}_{iface}`、`Q^{tool}_{acq}`、`Q^{rt}_{orch}` 真正映射成 AirSpark 日志导出键和 submission-ready 主表列，而不是继续扩 related work。


## R919 本轮推进结论：把 D06 从“world-model / local-shell / planner–executor / POI 扣减已成形”继续压到“tool-calling information acquisition 与 lifelong memory persistence 也必须单列扣减，否则 packet gain 仍可能只是更会查、更会记，而不是同一 Semantic Waypoint Packet 真正活过 AirSpark consume-time/replay 链路”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换主推进 **D06**，满足近三轮继续覆盖 `D01/D04/D06` 至少一个，也避免与前一轮明确写成 D06 的主收束点只做同类重复。严格先做本地优先：复核 `README.md / PAPER.md / experiments.md`，回扫近 30 天本地 L1 命中 **AgenticNav (2606.10577)**、**AllDayNav (2606.10927)**、**Goal2Pixel (2606.01621)**、**Beyond Waypoints (2606.07244)**、**FLIGHT VLA (2606.06836)** 等锚点；QMD 对 `aerial visual language navigation tool calling lifelong memory packet --no-rerank` 主要回流既有 `D06/REPORT.md` 与 survey/source 条目，没有形成需要完整入库的新 executor-facing 论文。因此本轮 **高价值扫描命中 2 篇本地核心锚点（AgenticNav、AllDayNav）**、**正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **PAPER.md 推进**：本轮继续推进 **Related Work / Limitations**，新增 `2.25–2.27` 三个小节：把 **AgenticNav** 明确冻结为 `tool-calling information-acquisition family`，把 **AllDayNav** 明确冻结为 `lifelong memory persistence family`，并规定只有在 `tool-acquisition → schema serialization → delayed consume-time preservation` 与 `memory-persistence → context-stable packet support → delayed consume-time preservation` 两条路线都扣减后仍保留 residual，结果才允许升级成更强的 AirSpark packet-contract 语言；同时在 Limitations 里新增对应缺口，承认“更会查 / 更会记”仍可能耗尽所谓 packet gain。
- **experiments 收束**：从现在起，D06 主表若匹配这两类 baseline，至少要新增 `Tool-Acquisition Family Match / Q^{tool}_{acq} / Depth-Query Efficiency / Memory-Recall Precision / Memory-Persistence Family Match / Q^{mem}_{persist} / Memory-Staleness Audit / Replay-Consistent Context Retention`。若增益在这些字段扣减后消失，结果必须冻结在 **tool-calling information-acquisition support** 或 **memory-persistence support**，不得直接写成 packet survives `serialization → bind → consume → replay`。
- **核心价值**：这一步把 D06 从“知道要扣 planner–executor decoupling、POI final-meters、world-model foresight、compact onboard shell”继续推进到“连 planner 更会按需查 depth 和记忆、策略更会跨回合保留上下文”也必须单列扣减。也就是说，D06 现在更清楚地区分 **planner-side information access 更聪明**、**long-horizon memory substrate 更强**，和 **同一 Semantic Waypoint Packet 真正跨过 AirSpark schema / bind / consume / replay 全链路**，这三类证据不能再混写成一种部署优势。
- **下轮建议**：按轮换优先切 **D01_世界模型** 或 **D07_Isaac强化学习机械臂控制**；若继续回到 D06，下一步最值钱的是把 `Q^{tool}_{acq}`、memory-staleness audit、以及 replay-consistent context retention 真正映射成 AirSpark 日志导出键和 submission-ready 主表列，而不是继续扩 related work。

> 最后更新：2026-06-12 R917

## R918 本轮推进结论：把 D06 从“已有五子表 + PEACE/POI subtraction + WorldFly/GRaD-Nav++扣减框架”继续压到“PAPER 的 Limitations 也必须显式承认 world-model foresight 与 compact onboard shell 是最近邻替代解释，否则 Semantic Waypoint Packet 的 executor-facing gain 仍可能只是 better proposal 或 better local shell，不得升级成 full packet-contract claim”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换切回 **D06**，既避免与最近主推进的非 D06 方向连续重复，也继续满足近三轮覆盖 `D01/D04/D06` 至少一个。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md / experiments.md`，并通过 QMD 回扫本地方向文档、研究报告和既有锚点；近 30 天本地 L1 已有 **UAV-VLN Survey (2604.13654)**、**Unified Aerial VLN (2512.08639)** 等核心笔记，未发现需要完整入库的新 executor-facing 论文。补充 arXiv 最新 3 篇后，返回结果主要落在 manipulation / generalist / world-model 宽查询噪声，未改变 D06 当前 evidence boundary 判断，因此本轮 **高价值扫描命中 2 篇本地核心锚点（2604.13654、2512.08639）**、**正式新增入库 0 篇**，不触发 Tavily。
- **PAPER.md 推进**：本轮继续推进 **Related Work → Limitations** 的证据收束，把 **2.22 Limitations of Existing Work** 从 19 条扩到 **21 条**，新增两条明确缺口：一是现有 **world-model-driven UAV agents** 虽能显著提升 planner-side foresight / route imagination / short-horizon consequence prediction，但仍很少证明收益最终活成 schema-aligned、executor-consumable、replay-reconstructable packet；二是现有 **compact onboard reactive-control systems** 虽能显著提升低时延跟踪、避障和局部可执行性，但仍没证明 semantic packet identity 本身稳定穿过 `serialization → bind → consume → replay`，而不是被更强 local shell 替代。
- **experiments 收束**：这一改动与上一轮 `WorldFly / GRaD-Nav++` 扣减字段保持同序，进一步要求后续主表与日志解释必须同时回答：这个增益究竟是 **planner foresight 更强**、**onboard reactive shell 更稳**，还是 **同一个 Semantic Waypoint Packet 真正跨过 AirSpark 的 schema / bind / consume / replay 全链路**。若任一前两类解释足以耗尽增益，结果必须冻结在 **world-model foresight support / compact-control support / local-shell support**。
- **核心价值**：这一步把 D06 从“实验设计里知道要扣 WorldFly / GRaD-Nav++”继续推进到“论文叙事本身也公开承认这两条最近邻替代解释”。这样后续 reviewer-facing 结果就更难把 **better world prior**、**better short-horizon prediction**、或 **better onboard shell** 误写成 packet-contract 胜利。
- **下轮建议**：按轮换优先切 **D07_Isaac强化学习机械臂控制** 或 **D01_世界模型**；若继续回到 D06，下一步最值钱的是把 `World-Model Family Match / Q^{wam}_{plan} / Onboard Reactive-Shell Match / Q^{shell}_{local}` 真正映射成 AirSpark 日志导出键和 submission-ready 主表列，而不是继续扩 related work。

## R917 本轮推进结论：把 D06 从“已固定五子表与 PEACE/POI subtraction”继续压到“WorldFly 的 world-model foresight 与 GRaD-Nav++ 的机载实时壳也必须单列扣除，否则 packet gain 仍可能只是 better proposal or better onboard shell，不得升级成 full packet-contract claim”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换切回 **D06**，既避免与最近明确记录的主推进 **D04(2026-06-12 04:22)** 连续重复，也继续满足近三轮覆盖 `D01/D04/D06` 至少一个。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md / experiments.md / 研究状态追踪`，并回扫近 30 天本地 L1 与 QMD 命中 **WorldFly (2606.06147)**、**GRaD-Nav++ (2506.14009)**、**PEACE (2606.00104)**、**POINav (2605.28237)**。QMD 仍主要回流 D06 既有报告与方向 README，没有形成需要完整入库的新高价值论文，因此本轮 **高价值扫描命中 4 个本地核心锚点（WorldFly、GRaD-Nav++、PEACE、POINav）**、**正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **PAPER.md 推进**：本轮继续推进 **Experiments / Main Results**，新增 `4.2.16–4.2.17`，把 **WorldFly** 明确冻结为 `world-model foresight / world-action-adjacent support family`，把 **GRaD-Nav++** 明确冻结为 `compact onboard control / local-shell support family`，并要求主表至少补齐 `World-Model Family Match / Q^{wam}_{plan} / Onboard Reactive-Shell Match / Q^{shell}_{local} / Compact-Control Explanation Remaining / Residual Packet-ID Survival after World/Shell Matching` 后，结果才允许继续升级成 packet-contract 语言。
- **experiments 收束**：从现在起，所有想升级成 **deployable planner→verifier→WaypointExecutor interface / full packet-contract accountability / NtM handoff preservation** 的 D06 结果，不仅要通过既有五子表和 `PEACE / POINav` 扣减，还必须额外说明：这个 gain 不是因为 **WorldFly-style better future consequence modeling**，也不是因为 **GRaD-Nav++-style better onboard reactive shell**。若这两条解释任一足以耗尽增益，结果必须冻结在 **world-model foresight support / compact-control support / local-shell support / latent execution sufficiency**。
- **核心价值**：这一步把 D06 从“知道要做 schema/trace/reconstruction 审计”继续推进到“知道还必须把最像方法收益的两条近邻解释单独扣掉”。也就是说，D06 现在更明确地区分 **planner foresight 更强**、**机载实时壳更稳**、和 **同一个 Semantic Waypoint Packet 真的活过 `serialization → bind → consume → replay` 全链路**，这三类证据不能再混写成同一种部署优势。
- **下轮建议**：按轮换优先切 **D07_Isaac强化学习机械臂控制** 或 **D01_世界模型**；若继续回到 D06，下一步最值钱的是把这组 `WorldFly / GRaD-Nav++` 扣减字段真正映射进 AirSpark 日志导出键和 reviewer-facing 主表模板，而不是继续扩 related work。

## R916 本轮推进结论：把 D06 从“PAPER 里已有五子表”继续压到“experiments 也必须按 `schema alignment / consume-time trace / planner–executor subtraction / POI final-meters subtraction / reconstruction audit` 五块同序落表，否则任何 executor-facing 正结果都不得升级成 full packet-contract claim”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换切回 **D06**，既避免与最近明确记录的主推进 **D01(2026-06-11 01:56)** 连续重复，也继续满足近三轮覆盖 `D01/D04/D06` 至少一个。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md / experiments.md / 研究状态追踪`，并回扫近 30 天本地 L1 与 QMD 命中 **PEACE (2606.00104)**、**POINav (2605.28237)**、**AerialVLA (2603.14363)**、**OnFly (2603.10682)**。QMD 结果仍主要回流 D06 既有报告与 AerialVLA source，没有形成需要完整入库的新高价值论文，因此本轮 **高价值扫描命中 4 个本地核心锚点（PEACE、POINav、AerialVLA、OnFly）**、**正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **PAPER.md 推进**：本轮继续推进 **Experiments / Main Results**，在论文中补实 `4.2.29–4.2.31`，把 D06 主表正式冻结为 **五子表固定阅读顺序**：`schema alignment → consume-time trace → planner–executor subtraction → POI final-meters subtraction → reconstruction audit`，并明确只有五块都站住时，结果才允许升级成 **full Semantic Waypoint Packet contract**。
- **experiments 收束**：从现在起，所有想升级成 **deployable planner→verifier→WaypointExecutor interface / full packet-contract accountability / NtM handoff preservation** 的 D06 结果，必须至少补齐以下五块字段：
  1. **schema alignment**：`Executor-Schema Match / goal_type-valid / pose-bind-valid / yaw-altitude-bind-valid / budget-envelope-valid / Manifest Field Coverage / Frame Trace Continuity / Action Trace Completeness`
  2. **consume-time trace**：`e^{auth} / e^{bind} / e^{consume} / Consume-Time Trace Match / emit→bind latency / bind→consume latency / trace gap count / stale-field-at-consume`
  3. **planner–executor subtraction**：`Planner–Executor Family Match / Q^{pe}_{bind} / Typed-Plan Completeness / Constraint-Enforced Bind Success / Executor-Ambiguity Reduction / Decoupling-Only Explanation Remaining`
  4. **POI final-meters subtraction**：`POI-Family Match / Q^{poi}_{final} / Final-Meters Offset Reduction / POI-Level Arrival Gain / Packet-Promotion Eligibility after POI Matching`
  5. **reconstruction audit**：`Packet-Reconstructability Match / Manifest↔Frames↔Actions Joint Recoverability / Reconstructed-Packet Identity Consistency / Hidden-Planner-State Dependence / Reconstruction-Only Failure Cause / Weakest Honest Claim after Reconstruction Audit`
- **核心价值**：这一步把 D06 从“知道要做 reconstruction-aware contract、planner–executor subtraction、POI subtraction”继续推进到“知道这些东西必须按固定顺序落在同一张 submission-ready 主表里”。也就是说，D06 现在更明确地区分 **schema 对齐成功**、**consume-time 真消费成功**、**只是 typed planner–executor 更干净**、**只是最后几米 POI 更准**、以及 **事后能否只靠 AirSpark 日志重建同一 packet** 这五种完全不同的证据等级。
- **下轮建议**：按轮换优先切 **D04_跨载体泛化** 或 **D07_Isaac强化学习机械臂控制**；若继续回到 D06，下一步最值钱的是把这五块字段真正映射进 AirSpark 训练/回放日志导出脚本与主表模板，而不是继续扩 related work。

## R915 本轮推进结论：把 D06 从“reconstruction-aware executor contract 已进入 experiments”继续压到“PEACE 这类 typed planner–executor baseline 必须在 PAPER 的 Method/Related Work 里和 `C_{ser}→e^{auth}→e^{bind}→e^{consume}→c^{trace}→R^{pkt}` 一起闭环，否则 executor-facing增益仍可能只是 decoupling gain，不得升级成 full packet-contract claim”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换主推进 **D06**，满足“近三轮必须覆盖 D01/D04/D06 至少一个”，同时避免与最近明确记录的 D07 连续重复。严格先做本地优先：复核 `README.md / PAPER.md / experiments.md`，并回扫近 30 天本地 L1 命中 **PEACE (2606.00104)**、**WorldVLN (2605.15964)** 与既有 **OnFly / AerialVLA / QuadAgent**。QMD 对 `aerial visual language navigation waypoint executor packet AirSpark semantic waypoint --no-rerank` 因索引老旧与资源限制未返回新的高价值 executor-facing 文献，因此不触发 arXiv / Tavily 外扩。
- **扫描结论**：本轮 **高价值扫描命中 2 篇核心本地锚点（PEACE、WorldVLN）**、**正式新增入库 0 篇**。当前最值钱的增量不在新增论文，而在把 PEACE 这类 **typed planner–executor + external constraint layer** 的替代解释，进一步压进 D06 的 packet-contract 写法里，防止把 decoupling/bind-time cleanliness 误写成 packet 自身更诚实地活到了 consume time。
- **PAPER.md 推进**：本轮直接推进 **Related Work + Method**。一是在 `2.20 AirSpark-Schema-Aligned Waypoint Execution...` 中显式吸收 **PEACE**，把它冻结为 **planner–executor decoupling with constraint enforcement family**；二是在 `3.4 WaypointExecutor-Aligned Packet Serialization, Consume-Time Logging, and Post-hoc Reconstruction Contract` 中补强 executor-facing 论证：D06 的 strongest claim 只有在同一行同时穿过 `C_{ser}`、`e^{auth}`、`e^{bind}`、`e^{consume}`、`c^{trace}` 与 `R^{pkt}`，且做完 PEACE-style decoupling subtraction 后仍有残差，才允许升级成 **full Semantic Waypoint Packet contract**。
- **核心价值**：这一步把 D06 从“实验表里知道要审 reconstruction-aware contract”继续推进到“论文方法论本身也明确承认 PEACE-style typed plan/safety validator 是最近邻替代解释”。也就是说，D06 现在不仅要求 packet 可序列化、可绑定、可消费、可追踪、可重建，还进一步要求说明：这些优势不是仅仅因为 **planner–executor split 更干净、typed mission object 更明确、constraint layer 更审计友好**。
- **下轮建议**：按轮换优先切 **D01_世界模型** 或 **D04_跨载体泛化**；若继续回到 D06，下一步最值钱的是把 `experiments.md` 现有的 `planner–executor subtraction / POI final-meters subtraction / reconstruction audit` 真正整理成 submission-ready 五子表结构，并把 `R^{pkt}` failure bucket 映射到可直接记录的 AirSpark 日志键。
