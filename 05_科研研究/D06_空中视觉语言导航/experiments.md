# D06 experiments

> 最后更新：2026-06-12 R917

## R917 本轮推进结论：把 D06 从“已固定五子表与 PEACE/POI subtraction”继续压到“WorldFly 的 world-model foresight 与 GRaD-Nav++ 的机载实时壳也必须单列扣除，否则 packet gain 仍可能只是 better proposal or better onboard shell，不得升级成 full packet-contract claim”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换切回 **D06**，既避免与最近明确记录的主推进 **D04(2026-06-12 04:22)** 连续重复，也继续满足近三轮覆盖 `D01/D04/D06` 至少一个。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md / experiments.md / 研究状态追踪`，并回扫近 30 天本地 L1 与 QMD 命中 **WorldFly (2606.06147)**、**GRaD-Nav++ (2506.14009)**、**PEACE (2606.00104)**、**POINav (2605.28237)**。QMD 仍主要回流 D06 既有报告与方向 README，没有形成需要完整入库的新高价值论文，因此本轮 **高价值扫描命中 4 个本地核心锚点（WorldFly、GRaD-Nav++、PEACE、POINav）**、**正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **PAPER.md 推进**：本轮继续推进 **Experiments / Main Results**，新增 `4.2.16–4.2.17`，把 **WorldFly** 明确冻结为 `world-model foresight / world-action-adjacent support family`，把 **GRaD-Nav++** 明确冻结为 `compact onboard control / local-shell support family`，并要求主表至少补齐 `World-Model Family Match / Q^{wam}_{plan} / Onboard Reactive-Shell Match / Q^{shell}_{local} / Compact-Control Explanation Remaining / Residual Packet-ID Survival after World/Shell Matching` 后，结果才允许继续升级成 packet-contract 语言。
- **experiments 收束**：从现在起，所有想升级成 **deployable planner→verifier→WaypointExecutor interface / full packet-contract accountability / NtM handoff preservation** 的 D06 结果，不仅要通过既有五子表和 `PEACE / POINav` 扣减，还必须额外说明：这个 gain 不是因为 **WorldFly-style better future consequence modeling**，也不是因为 **GRaD-Nav++-style better onboard reactive shell**。若这两条解释任一足以耗尽增益，结果必须冻结在 **world-model foresight support / compact-control support / local-shell support / latent execution sufficiency**。
- **核心价值**：这一步把 D06 从“知道要做 schema/trace/reconstruction 审计”继续推进到“知道还必须把最像方法收益的两条近邻解释单独扣掉”。也就是说，D06 现在更明确地区分 **planner foresight 更强**、**机载实时壳更稳**、和 **同一个 Semantic Waypoint Packet 真的活过 `serialization → bind → consume → replay` 全链路**，这三类证据不能再混写成同一种部署优势。
- **下轮建议**：按轮换优先切 **D07_Isaac强化学习机械臂控制** 或 **D01_世界模型**；若继续回到 D06，下一步最值钱的是把这组 `WorldFly / GRaD-Nav++` 扣减字段真正映射进 AirSpark 日志导出键和 reviewer-facing 主表模板，而不是继续扩 related work。

## R916 本轮推进结论：把 D06 从“PAPER 里已有五子表”继续压到“experiments 也必须按 `schema alignment / consume-time trace / planner–executor subtraction / POI final-meters subtraction / reconstruction audit` 五块同序落表，否则任何 executor-facing 正结果都不得升级成 full packet-contract claim”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换切回 **D06**，既避免与最近明确记录的主推进 **D01(2026-06-11 01:56)** 连续重复，也继续满足近三轮覆盖 `D01/D04/D06` 至少一个。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md / experiments.md / 研究状态追踪`，并回扫近 30 天本地 L1 与 QMD 命中 **PEACE (2606.00104)**、**POINav (2605.28237)**、**AerialVLA (2603.14363)**、**OnFly (2603.10682)**。QMD 结果仍主要回流 D06 既有报告与 AerialVLA source，没有形成需要完整入库的新高价值论文，因此本轮 **高价值扫描命中 4 个本地核心锚点（PEACE、POINav、AerialVLA、OnFly）**、**正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **PAPER.md 推进**：本轮继续推进 **Experiments / Main Results**，在论文中补实 `4.2.29–4.2.31`，把 D06 主表正式冻结为 **五子表固定阅读顺序**：`schema alignment → consume-time trace → planner–executor subtraction → POI final-meters subtraction → reconstruction audit`，并明确只有五块都站住时，结果才允许升级成 **full Semantic Waypoint Packet contract**。
- **experiments 收束**：从现在起，所有想升级成 **deployable planner→verifier→WaypointExecutor interface / full packet-contract accountability / NtM handoff preservation** 的 D06 结果，必须至少补齐以下五块字段：
  1. **schema alignment**：`Executor-Schema Match / goal_type-valid / pose-bind-valid / yaw-altitude-bind-valid / budget-envelope-valid / Manifest Field Coverage / Frame Trace Continuity / Action Trace Completeness`
  2. **consume-time trace**：`e^{auth} / e^{bind} / e^{consume} / Consume-Time Trace Match / emit→bind latency / bind→consume latency / trace gap count / stale-field-at-consume`
  3. **planner–executor subtraction**：`Planner–Executor Family Match / Q^{pe}_{bind} / Typed-Plan Completeness / Constraint-Enforced Bind Success / Executor-Ambiguity Reduction / Decoupling-Only Explanation Remaining`
  4. **POI final-meters subtraction**：`POI-Family Match / Q^{poi}_{final} / Final-Meters Offset Reduction / POI-Level Arrival Gain / Packet-Promotion Eligibility after POI Matching`
  5. **reconstruction audit**：`Packet-Reconstructability Match / Manifest↔Frames↔Actions Joint Recoverability / Reconstructed-Packet Identity Consistency / Hidden-Planner-State Dependence / Reconstruction-Only Failure Cause / Weakest Honest Claim after Reconstruction Audit`
- **核心价值**：这一步把 D06 从“知道要做 reconstruction-aware contract、planner–executor subtraction、POI subtraction”继续推进到“知道这些东西必须按固定顺序落在同一张 submission-ready 主表里”。也就是说，D06 现在更明确地区分 **schema 对齐成功**、**consume-time 真消费成功**、**只是 typed planner–executor 更干净**、**只是最后几米 POI 更准**、以及 **事后能否只靠 AirSpark 日志重建同一 packet** 这五种完全不同的证据等级。
- **下轮建议**：按轮换优先切 **D04_跨载体泛化** 或 **D07_Isaac强化学习机械臂控制**；若继续回到 D06，下一步最值钱的是把这五块字段真正映射进 AirSpark 训练/回放日志导出脚本与主表模板，而不是继续扩 related work。

## R915 本轮推进结论：把 D06 从“reconstruction-aware executor contract 已进入 experiments”继续压到“PEACE 这类 typed planner–executor baseline 必须在 PAPER 的 Method/Related Work 里和 `C_{ser}→e^{auth}→e^{bind}→e^{consume}→c^{trace}→R^{pkt}` 一起闭环，否则 executor-facing增益仍可能只是 decoupling gain，不得升级成 full packet-contract claim”
- **本轮方向**：D06 空中视觉语言导航
- **本地依据**：本轮按轮换主推进 **D06**，满足“近三轮必须覆盖 D01/D04/D06 至少一个”，同时避免与最近明确记录的 D07 连续重复。严格先做本地优先：复核 `README.md / PAPER.md / experiments.md`，并回扫近 30 天本地 L1 命中 **PEACE (2606.00104)**、**WorldVLN (2605.15964)** 与既有 **OnFly / AerialVLA / QuadAgent**。QMD 对 `aerial visual language navigation waypoint executor packet AirSpark semantic waypoint --no-rerank` 因索引老旧与资源限制未返回新的高价值 executor-facing 文献，因此不触发 arXiv / Tavily 外扩。
- **扫描结论**：本轮 **高价值扫描命中 2 篇核心本地锚点（PEACE、WorldVLN）**、**正式新增入库 0 篇**。当前最值钱的增量不在新增论文，而在把 PEACE 这类 **typed planner–executor + external constraint layer** 的替代解释，进一步压进 D06 的 packet-contract 写法里，防止把 decoupling/bind-time cleanliness 误写成 packet 自身更诚实地活到了 consume time。
- **PAPER.md 推进**：本轮直接推进 **Related Work + Method**。一是在 `2.20 AirSpark-Schema-Aligned Waypoint Execution...` 中显式吸收 **PEACE**，把它冻结为 **planner–executor decoupling with constraint enforcement family**；二是在 `3.4 WaypointExecutor-Aligned Packet Serialization, Consume-Time Logging, and Post-hoc Reconstruction Contract` 中补强 executor-facing 论证：D06 的 strongest claim 只有在同一行同时穿过 `C_{ser}`、`e^{auth}`、`e^{bind}`、`e^{consume}`、`c^{trace}` 与 `R^{pkt}`，且做完 PEACE-style decoupling subtraction 后仍有残差，才允许升级成 **full Semantic Waypoint Packet contract**。
- **核心价值**：这一步把 D06 从“实验表里知道要审 reconstruction-aware contract”继续推进到“论文方法论本身也明确承认 PEACE-style typed plan/safety validator 是最近邻替代解释”。也就是说，D06 现在不仅要求 packet 可序列化、可绑定、可消费、可追踪、可重建，还进一步要求说明：这些优势不是仅仅因为 **planner–executor split 更干净、typed mission object 更明确、constraint layer 更审计友好**。
- **下轮建议**：按轮换优先切 **D01_世界模型** 或 **D04_跨载体泛化**；若继续回到 D06，下一步最值钱的是把 `experiments.md` 现有的 `planner–executor subtraction / POI final-meters subtraction / reconstruction audit` 真正整理成 submission-ready 五子表结构，并把 `R^{pkt}` failure bucket 映射到可直接记录的 AirSpark 日志键。
