# D04: 跨载体泛化 研究报告

> 最后更新：2026-06-15 | 成熟度：🟡中期（R851 已把接口通过后的首轮结果继续收束到“共享接口先成立，然后只允许在 geometry / morphology compensation / dynamics compensation 中冻结一条主补偿叙事”）
> 状态：🟡 推进中

## Round 2026-06-15 13:56 CST | 方向: D04_跨载体泛化
- **D04_轻量扫描**：本轮按轮换主推进 **D04 跨载体泛化**，既避开上一轮已明确记录的主推进 **D06(2026-06-15 03:56)** 连续重复，也继续满足近三轮覆盖 `D01/D04/D06` 至少一个。严格先做本地优先：复核 `README.md / PAPER.md / REPORT.md`，回扫近 30 天本地 L1 锚点 **Human-Robot Copilot (2604.03613)**、**D-CLING (2605.19690)**、**Any2Any (2605.23733)**、**RIO (2605.11564)**，并补跑 QMD `cross-embodiment bounded adaptation copilot specialist promotion reviewer-facing main table --no-rerank`；QMD 仍主要回流 D04 README、跨载体概念页与既有研究状态，没有形成比这四篇更直接命中当前 experiments 缺口的新证据。因此本轮 **高价值扫描命中 4 篇本地核心锚点（Human-Robot Copilot、D-CLING、Any2Any、RIO）**、**正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **D04_PAPER.md 推进**：本轮继续主推进 `PAPER.md` 的 **Experiments 4.2 / 4.3**，把先前已存在但还没真正进入 reviewer-facing 主表的 **bounded adaptation / bounded supervision** 两条 family 压进唯一 schema。具体做了三件事：① 在 `4.2 Main Results` 中把主表字段统一为 `\Upsilon_{row}^{D04}=(TR,IGS,LTS,KAG,DAG,RIG,BPA,BSC,WPS,PRS^{air},GGS,JGS,CAS,LLS,HRS^{js},SRS,CW,PC)`，明确 **D-CLING** 对应 `BPA`、**Human-Robot Copilot** 对应 `BSC`；② 把 canonical rows 从 7 行扩成 9 行，新增 **Row D (D-CLING control)** 与 **Row E (Human-Robot Copilot control)**，要求 reviewer 先判断 uplift 是否仍只是 prior-preserving side adaptation 或 sparse human rescue；③ 在 `4.3 Ablation Study` 中把 control bank 扩成 `A_{-bpa}` / `A_{-bsc}` 与扩展后的 `\mathcal{A}^{D04,+js}`，并把 budget witness tuple 补成包含 **intervention budget** 的统一记账口径。
- **核心价值**：这一步把 D04 从“知道 alignment / infrastructure / aerial supports / joint-space executability 都不能冒领 shared latent”继续推进到更完整的一层——**低数据 target uplift 还可能只是 prior-preserving bounded adaptation，部署成功还可能只是 sparse copilot rescue。** 现在 D04 的 experiments 已能更明确地区分 **对齐支持 / I/O 曝露 / bounded adaptation / bounded supervision / aerial structuring / executability support** 与真正的 **shared latent sufficiency / remaining embodiment structure**。
- **下轮建议**：按轮换优先切 **D07_Isaac强化学习机械臂控制** 或 **D01_世界模型**；若继续回到 D04，下一步最值钱的是把 `\Upsilon_{row}^{D04}` 真正同步成一张 reviewer-facing 主表草图，并补 `Row D/E/I` 的默认结果句式模板。

## Round 2026-06-14 00:06 CST | 方向: D04_跨载体泛化
- **D04_轻量扫描**：本轮按轮换主推进 **D04 跨载体泛化**，满足“避免连续两轮同方向”与“近三轮覆盖 D01/D04/D06 至少一个”的约束，也继续执行主人对 **Method 3.x** 的优先要求。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md / REPORT.md`，回扫近 30 天本地 L1 命中 **EmbodiSteer / Any2Any / RIO / Human2Humanoid / PointAction / SeeTraceAct**，并补跑 QMD `cross-embodiment generalization joint-space guidance executability morphology transfer --no-rerank`；QMD 仍主要回流 D04 README 与跨载体技术路线图，仅形成 **EmbodiSteer 已入库锚点的二次确认**，未触发 arXiv / Tavily 外扩。本轮因此记为 **高价值扫描命中 1 篇本地锚点（EmbodiSteer，已入库）、正式新增入库 0 篇**。
- **D04_PAPER.md 推进**：本轮继续主推进 `PAPER.md` 的 **Method 3.x / Experiments**，围绕 `EmbodiSteer + Any2Any + RIO` 三类本地锚点，把 **3.5 Weakest-Honest Escalation Rule** 与 **4.2/4.3** 压得更 submission-ready：新增并写实 **post-latent executability support** 的冻结规则，明确 `\bar a_t^\tau=\mathcal{E}_{js}(\tilde a_t^\tau, E^\tau, B^\tau, \mathcal{K}^\tau, \mathcal{C}^\tau)` 只能解释 **joint-space guidance / collision-aware steering / limit-aware stabilization** 三类部署期补偿；同时把 **`HRS^{js}=\Delta_{row}\setminus(JGS\cup CAS\cup LLS)`**、**`A_{-js}`** 与 **`\mathcal{A}^{D04,+js}`** 一起接入主文，使 reviewer 可以直接检查“去掉 executable correction 后，shared latent claim 是否还活着”。
- **核心价值**：这一步把 D04 从“训练期共享接口 vs 推理期 context residual”再往下压了一层——**就算 coarse geometry、latent transition、甚至部分 context residual 都成立，也还可能只是 target robot 靠 inference-time joint-space recourse 才真正可执行。** 现在 D04 已能把 **alignment-support / infrastructure-support / aerial-support / joint-space executability-support** 这几类更弱解释分开冻结，不让它们冒领 shared latent sufficiency 或 remaining embodiment structure。
- **下轮建议**：按轮换优先切 **D06_空中视觉语言导航** 或 **D07_Isaac强化学习机械臂控制**；若继续回到 D04，下一步最值钱的是把 `\Theta_t / HRS^{js}` 完整压进 **4.2 Main Results** 的唯一主表 schema，与 `\Upsilon_{row}^{D04}` 字段完全对齐，并补一段 reviewer-facing 结果句式模板。

## Round 2026-06-13 21:56 CST | 方向: D04_跨载体泛化
- **D04_轻量扫描**：本轮按轮换主推进 **D04 跨载体泛化**，满足“避免连续两轮同方向”与“近三轮覆盖 D01/D04/D06 至少一个”的约束，并延续主人 2026-05-27 对 **Method 3.x** 的最高优先级要求。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md`，回扫近 30 天本地 L1 命中 **EmbodiSteer / Human2Humanoid / Any2Any / WorldFly / FLIGHT / WildRoadBench**，并补跑 QMD `cross-embodiment generalization latent executability UAV ground robot shared latent --no-rerank`；QMD 继续主要回流 D04 README 与跨载体概念页，没有触发需要完整入库的新论文。本轮因此记为 **高价值扫描命中 1 篇新增本地锚点（EmbodiSteer，已入库）、正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **D04_PAPER.md 推进**：本轮继续主推进 `PAPER.md` 的 **Method + Experiments**，补强 **3.11 Joint-Space Guidance as a Post-Latent Executability Guard**，把 EmbodiSteer 形式化为 `\Theta_t=(JGS,CAS,LLS,LTS,HRS^{js},CW,PC,PB_{js})`，并修正 `\tilde a_t^\tau / \bar a_t^\tau / \mathcal{E}_{js}` 的 deployment-time steering 公式，明确 joint-space guidance、collision-aware steering、limit-aware stabilization 只能先记为 **post-latent executability support**。同时在 **4.3 Ablation Study** 新增 `A_{-js}` 与 `\mathcal{A}^{D04,+js}`，要求任何零样本部署增益若主要在去掉 executable correction 后消失，就必须停在 **joint-space executability support**，不能越级冒领成 shared latent sufficiency 或 remaining embodiment structure。
- **核心价值**：这一步把 D04 从“共享 geometry / latent 已经成立”再往下压实一层：**就算 coarse intent 和 contact progression 已经对了，也可能只是 target robot 需要 inference-time joint-space recourse 才能安全执行。** 也就是说，D04 现在不仅区分训练期共享接口与推理期上下文/对齐/支持路线，还单独把 **部署期可执行性补丁** 从真正的表征突破里剥出来了。
- **下轮建议**：按轮换优先切 **D01_世界模型** 或 **D07_Isaac强化学习机械臂控制**；若继续回到 D04，下一步最值钱的是把 `\Theta_t` 真正同步进 **4.2 Main Results** 的唯一主表字段，与 `\Upsilon_{row}^{D04}` 完全对齐，并补 reviewer-facing 的 `A_{-js}` 结果句式模板。

## Round 2026-06-07 01:56 CST | 方向: D04_跨载体泛化
- **D04_轻量扫描**：本轮按轮换切回 **D04 跨载体泛化**，既避免与最近明确记录的主推进 **D06(2026-06-06 19:56)** 连续重复，也继续满足近三轮覆盖 `D01/D04/D06` 至少一个。严格先做本地优先：复核 `README.md / PAPER.md / OWNER_NOTES.md / 研究状态追踪`，确认主人 2026-05-27 对 **Method 3.x** 的推进要求仍是最高优先级；回扫近 30 天本地 L1 锚点 **Human2Humanoid / RIO / JoyAI-RA 0.1**，并补跑 QMD `cross embodiment generalization specialist distillation bounded adaptation shared latent survival --no-rerank`，但本地索引提示 **last updated 50 days ago** 且进程被终止，未返回足以改写判断的新证据。因此本轮 **高价值扫描命中 3 篇核心本地锚点（Human2Humanoid、RIO、JoyAI-RA 0.1）**、**正式新增入库 0 篇**，不触发 arXiv / Tavily 外扩。
- **D04_PAPER.md 推进**：本轮直接推进 `PAPER.md` 的 **Method 3.x**，把 **Human2Humanoid (2606.03476)** 正式固化进 **3.12 Human-to-Humanoid Geometry Anchors without Paired Demonstrations**。新增 `\Psi_t=(UGA, PEC, MIE, LTS, HRS^{hum}, CW, PC)`，明确无配对 human→humanoid 提升必须先拆成 **unpaired geometry-anchor gain / physics-executability completion / morphology-invariant end-effector alignment** 三类上游监督来源，只有在 matched no-anchor control 下仍保留增益时，才允许晋升为 shared latent sufficiency；同时清理重复编号，把后续 **Data-First Interface Formation** 顺延为 **3.13**。
- **核心价值**：这一步把 D04 从“human anchors 能不能帮助跨载体”继续压实到更诚实的一层：**先分清是更好的无配对几何监督、更物理可执行的 retargeting，还是 latent interface 本身真的更强。** 也就是说，D04 现在不再把 human→humanoid 提升直接混进 embodiment-agnostic transfer，而是要求先经过 `UGA / PEC / MIE` 三档上游扣账。
- **下轮建议**：按轮换优先切 **D01_世界模型** 或 **D07_Isaac强化学习机械臂控制**；若继续回到 D04，下一步最值钱的是把 `\Psi_t` 真正同步进 `Experiments 4.x` 的主表字段，与 `\Lambda_t / \Sigma_t / \Xi_t` 形成统一的 submission-ready 审计模板。

> **R759 阶段更新**：① 新增 LAC-WM (ICLR 2026) — 统一 latent action space 机器人基础世界模型，验证 latent action 对齐在 scaling 下的 +46.7% 增益；② 创新点从 C1-C7 收束为四大轴；三/四节结构化重构；③ 新增统一实验对照表（Section 4）


> **R763 阶段更新**：① 新增 LAD (ICRA 2026) — Latent Action Diffusion for Cross-Embodiment Manipulation，扩散模型+latent action统一接口，ICRA 2026已接收；② 新增 Unified Latent Space for Humanoid Cross-Embodiment（arXiv:2601.15419），验证人型机器人共享latent表示可扩展性；③ 创新轴C（latent统一）获ICRA 2026直接验证，支撑更强；④ 深化轴C与轴D互补性论证
